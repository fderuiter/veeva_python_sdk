#' Veeva Vault API v25.1
#'
#' The latest GA version of the Vault REST API.
#'
#' The version of the OpenAPI document: 1.0.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Default operations
#' @description DefaultApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  ApiGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve API Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlComponentsComponentTypeAndRecordNameFilesGet  ####################
#'
#' library(openapi)
#' var_component_type_and_record_name <- "component_type_and_record_name_example" # character | The component type of the record followed by the name of the record from which to retrieve the content file. The format is `{Componenttype}.{record_name}`. For example, `Formattedoutput.my_formatted_output__c`.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Content File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlComponentsComponentTypeAndRecordNameFilesGet(var_component_type_and_record_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlComponentsComponentTypeAndRecordNameGet  ####################
#'
#' library(openapi)
#' var_component_type_and_record_name <- "component_type_and_record_name_example" # character | The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Component Record (MDL)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlComponentsComponentTypeAndRecordNameGet(var_component_type_and_record_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlExecuteAsyncJobIdResultsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Asynchronous MDL Script Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlExecuteAsyncJobIdResultsGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlExecuteAsyncPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Execute MDL Script Asynchronously
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlExecuteAsyncPost(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlExecutePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Execute MDL Script
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlExecutePost(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ApiMdlFilesPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Upload Content File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ApiMdlFilesPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  AudittrailAuditTrailTypeGet  ####################
#'
#' library(openapi)
#' var_audit_trail_type <- "audit_trail_type_example" # character | The name of the specified audit type (document_audit_trail, object_audit_trail, etc). Use the Retrieve Audit Types API to retrieve types available in your Vault. Requests for login_audit_trail only accept one request per user at a time.
#' var_start_date <- "start_date_example" # character | Specify a start date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days. (Optional)
#' var_end_date <- "end_date_example" # character | Specify an end date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days. (Optional)
#' var_all_dates <- "all_dates_example" # character | Set to true to request audit information for all dates. You must leave start_date and end_date blank when requesting an export of a full audit trail. (Optional)
#' var_format_result <- "format_result_example" # character | To request a downloadable CSV file of your audit details, use csv. The response contains a jobId to retrieve the job status, which contains a link to download the CSV file. If omitted, the API returns a JSON response and does not start a job. If all_dates is true, this parameter is required. (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0. (Optional)
#' var_objects <- "objects_example" # character | This is an optional parameter when specifying object_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more object names to retrieve their audit details. For example, objects=product__v,country__v. If omitted, defaults to all objects. (Optional)
#' var_events <- "events_example" # character | This is an optional parameter when specifying object_audit_trail or document_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more audit events to retrieve their audit details. For example, events=Edit,Delete,TaskAssignment. If omitted, defaults to all audit events. See Vault Help for full lists of object audit events and document audit events. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Audit Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$AudittrailAuditTrailTypeGet(var_audit_trail_type, start_date = var_start_date, end_date = var_end_date, all_dates = var_all_dates, format_result = var_format_result, limit = var_limit, offset = var_offset, objects = var_objects, events = var_events, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  AuthDiscoveryPost  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Authentication Type Discovery
#' api_instance <- DefaultApi$new()
#'
#' api_instance$AuthDiscoveryPost(accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  AuthOauthSessionOathOidcProfileIdPost  ####################
#'
#' library(openapi)
#' var_oath_oidc_profile_id <- "{{oath_oidc_profile_id}}" # character | 
#' var_authorization <- "Bearer {{access_token}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #OAuth 2.0 / OpenID Connect
#' api_instance <- DefaultApi$new()
#'
#' api_instance$AuthOauthSessionOathOidcProfileIdPost(var_oath_oidc_profile_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  AuthPost  ####################
#'
#' library(openapi)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #User Name and Password
#' api_instance <- DefaultApi$new()
#'
#' api_instance$AuthPost(content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeClassNameDelete  ####################
#'
#' library(openapi)
#' var_class_name <- "class_name_example" # character | The fully qualified class name of your file.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Source Code File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeClassNameDelete(var_class_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeClassNameDisablePut  ####################
#'
#' library(openapi)
#' var_class_name <- "class_name_example" # character | The fully qualified class name of your file.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Disable Vault Extension
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeClassNameDisablePut(var_class_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeClassNameEnablePut  ####################
#'
#' library(openapi)
#' var_class_name <- "class_name_example" # character | The fully qualified class name of your file.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Enable Vault Extension
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeClassNameEnablePut(var_class_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeClassNameGet  ####################
#'
#' library(openapi)
#' var_class_name <- "class_name_example" # character | The fully qualified class name of your file.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single Source Code File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeClassNameGet(var_class_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Profiling Sessions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Profiling Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerSessionNameActionsEndPost  ####################
#'
#' library(openapi)
#' var_session_name <- "session_name_example" # character | The name of the session, for example,  baseline__c.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #End Profiling Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerSessionNameActionsEndPost(var_session_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerSessionNameDelete  ####################
#'
#' library(openapi)
#' var_session_name <- "session_name_example" # character | The name of the session, for example,  baseline__c.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Profiling Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerSessionNameDelete(var_session_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerSessionNameGet  ####################
#'
#' library(openapi)
#' var_session_name <- "session_name_example" # character | The name of the session, for example,  baseline__c.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Profiling Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerSessionNameGet(var_session_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodeProfilerSessionNameResultsGet  ####################
#'
#' library(openapi)
#' var_session_name <- "session_name_example" # character | The name of the session, for example,  baseline__c.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Profiling Session Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodeProfilerSessionNameResultsGet(var_session_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CodePut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add or Replace Single Source Code File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CodePut(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CompositesTreesEdlHierarchyOrTemplateActionsListnodesPost  ####################
#'
#' library(openapi)
#' var_edl_hierarchy_or_template <- "edl_hierarchy_or_template_example" # character | Choose to retrieve either edl_hierarchy__v or edl_template__v
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Specific Root Nodes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CompositesTreesEdlHierarchyOrTemplateActionsListnodesPost(var_edl_hierarchy_or_template, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CompositesTreesEdlHierarchyOrTemplateGet  ####################
#'
#' library(openapi)
#' var_edl_hierarchy_or_template <- "edl_hierarchy_or_template_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Root Nodes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CompositesTreesEdlHierarchyOrTemplateGet(var_edl_hierarchy_or_template, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CompositesTreesEdlHierarchyVParentNodeIdChildrenGet  ####################
#'
#' library(openapi)
#' var_parent_node_id <- "parent_node_id_example" # character | The ID of a parent node in the hierarchy.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve a Node's Children
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CompositesTreesEdlHierarchyVParentNodeIdChildrenGet(var_parent_node_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  CompositesTreesEdlHierarchyVParentNodeIdChildrenPut  ####################
#'
#' library(openapi)
#' var_parent_node_id <- "parent_node_id_example" # character | The ID of a parent node in the hierarchy.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Node Order
#' api_instance <- DefaultApi$new()
#'
#' api_instance$CompositesTreesEdlHierarchyVParentNodeIdChildrenPut(var_parent_node_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationComponentTypeAndRecordNameGet  ####################
#'
#' library(openapi)
#' var_component_type_and_record_name <- "component_type_and_record_name_example" # character | The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by setting loc to true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Component Record (XML/JSON)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationComponentTypeAndRecordNameGet(var_component_type_and_record_name, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationComponentTypeGet  ####################
#'
#' library(openapi)
#' var_component_type <- "component_type_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Component Record Collection
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationComponentTypeGet(var_component_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationObjectNameAndObjectTypeGet  ####################
#'
#' library(openapi)
#' var_object_name_and_object_type <- "object_name_and_object_type_example" # character | The object name followed by the object type in the format `Objecttype.{object_name}.{object_type}`. For example, `Objecttype.product__v.base__v`.
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by setting loc to true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Details from a Specific Object
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationObjectNameAndObjectTypeGet(var_object_name_and_object_type, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationObjecttypeGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Details from All Object Types
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationObjecttypeGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationRoleAssignmentRuleDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Lifecycle Role Assignment Override Rules
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationRoleAssignmentRuleDelete(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationRoleAssignmentRuleGet  ####################
#'
#' library(openapi)
#' var_lifecycle__v <- "lifecycle__v_example" # character | Include the name of the lifecycle from which to retrieve information. For example: lifecycle_v=general_lifecycle__c (Optional)
#' var_role__v <- "role__v_example" # character | Include the name of the role from which to retrieve information. For example: role__v=editor__c (Optional)
#' var_product__v <- "product__v_example" # character | Include the ID/name of a specific product to see product-based override rules to default users/allowed users for the lifecycle role. For example: product__v=0PR0011001 or product__v.name__v=CholeCap (Optional)
#' var_country__v <- "country__v_example" # character | Include the ID/name of a specific country to see country-based override rules to default users/allowed users for the lifecycle role. For example: country__v=0CR0022002 or country__v.name__v=United States (Optional)
#' var_study__v <- "study__v_example" # character | In eTMF Vaults only. Include the ID/name of a specific study to see study-based override rules to default users/allowed users for the lifecycle role. For example: study__v=0ST0021J01 or study__v.name__v=CholeCap Study (Optional)
#' var_study_country__v <- "study_country__v_example" # character | In eTMF Vaults only. Include the ID/name of a specific study country to see study country-based override rules to default users/allowed users for the lifecycle role. For example: study_country__v=0SC0001001 or study_country__v.name__v=Germany (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Lifecycle Role Assignment Rules (Default & Override)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationRoleAssignmentRuleGet(lifecycle__v = var_lifecycle__v, role__v = var_role__v, product__v = var_product__v, country__v = var_country__v, study__v = var_study__v, study_country__v = var_study_country__v, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationRoleAssignmentRulePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Lifecycle Role Assignment Override Rules
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationRoleAssignmentRulePost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ConfigurationRoleAssignmentRulePut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Lifecycle Role Assignment Rules (Default & Override)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ConfigurationRoleAssignmentRulePut(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  DelegationLoginPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character | The sessionId of the currently authenticated user who will initiate the delegated session. Cannot be a delegated_sessionid. (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Delegated Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$DelegationLoginPost(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  DelegationVaultsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Delegations
#' api_instance <- DefaultApi$new()
#'
#' api_instance$DelegationVaultsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  KeepAlivePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Session Keep Alive
#' api_instance <- DefaultApi$new()
#'
#' api_instance$KeepAlivePost(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LimitsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Limits on Objects
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LimitsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsApiUsageGet  ####################
#'
#' library(openapi)
#' var_date <- "date_example" # character | The day to retrieve the API Usage log. Date is in UTC and follows the format YYYY-MM-DD. Date cannot be more than 30 days in the past. (Optional)
#' var_log_format <- "log_format_example" # character | Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Daily API Usage
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsApiUsageGet(date = var_date, log_format = var_log_format, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugGet  ####################
#'
#' library(openapi)
#' var_user_id <- "user_id_example" # character | Filter results to retrieve the debug log for this user ID only. If omitted, this request retrieves debug logs for all users in the Vault. (Optional)
#' var_include_inactive <- "true" # character | Set to `true` to include debug log sessions with a status of `inactive__sys` in the response. If omitted, defaults to `false` and inactive sessions are not included in the response. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Debug Logs
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugGet(user_id = var_user_id, include_inactive = var_include_inactive, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugIdActionsResetDelete  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of the debug log to delete.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Debug Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugIdActionsResetDelete(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugIdActionsResetPost  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of the debug log to delete.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reset Debug Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugIdActionsResetPost(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugIdFilesGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of the debug log to download.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Debug Log Files
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugIdFilesGet(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugIdGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of the debug log to retrieve.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single Debug Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugIdGet(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeDebugPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Debug Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeDebugPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  LogsCodeRuntimeGet  ####################
#'
#' library(openapi)
#' var_date <- "YYYY-MM-DD" # character |  (Optional)
#' var_log_format <- "log_format_example" # character | Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download SDK Runtime Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$LogsCodeRuntimeGet(date = var_date, log_format = var_log_format, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MessagesMessageTypeActionsImportPost  ####################
#'
#' library(openapi)
#' var_message_type <- "message_type_example" # character | The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Import Bulk Translation File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MessagesMessageTypeActionsImportPost(var_message_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MessagesMessageTypeLanguageLangActionsExportPost  ####################
#'
#' library(openapi)
#' var_message_type <- "message_type_example" # character | The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
#' var_lang <- "lang_example" # character | A valid language code value, for example, en. Retrieve available values from the Admin Key (admin_key__sys) field on the Language (language__sys) object. Active and Inactive languages are both valid. 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Bulk Translation File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MessagesMessageTypeLanguageLangActionsExportPost(var_message_type, var_lang, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataAudittrailAuditTrailTypeGet  ####################
#'
#' library(openapi)
#' var_audit_trail_type <- "audit_trail_type_example" # character | The name of the specified audit type (document_audit_trail, object_audit_trail, etc).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Audit Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataAudittrailAuditTrailTypeGet(var_audit_trail_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataAudittrailGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Audit Types
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataAudittrailGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataComponentsComponentTypeGet  ####################
#'
#' library(openapi)
#' var_component_type <- "component_type_example" # character | The component type name (Picklist, Docfield, Doctype, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Component Type Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataComponentsComponentTypeGet(var_component_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataComponentsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Component Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataComponentsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsBindersTemplatesBindernodesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Template Node Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsBindersTemplatesBindernodesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsBindersTemplatesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Template Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsBindersTemplatesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsAnnotationsPlacemarksTypesPlacemarkTypeGet  ####################
#'
#' library(openapi)
#' var_placemark_type <- "placemark_type_example" # character | The name of the placemark type. For example, sticky__sys.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Annotation Placemark Type Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsAnnotationsPlacemarksTypesPlacemarkTypeGet(var_placemark_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsAnnotationsReferencesTypesReferenceTypeGet  ####################
#'
#' library(openapi)
#' var_reference_type <- "reference_type_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Annotation Reference Type Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsAnnotationsReferencesTypesReferenceTypeGet(var_reference_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsAnnotationsTypesAnnotationTypeGet  ####################
#'
#' library(openapi)
#' var_annotation_type <- "annotation_type_example" # character | The name of the annotation type. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in Medical and PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys`
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Annotation Type Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsAnnotationsTypesAnnotationTypeGet(var_annotation_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsEventsEventTypeTypesEventSubtypeGet  ####################
#'
#' library(openapi)
#' var_event_type <- "event_type_example" # character | The event type. For example, distribution__v.
#' var_event_subtype <- "event_subtype_example" # character | The event subtype. For example, approved_email__v.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Event SubType Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsEventsEventTypeTypesEventSubtypeGet(var_event_type, var_event_subtype, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsEventsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Event Types and Subtypes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsEventsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsLockGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Lock Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsLockGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsPropertiesFindCommonPost  ####################
#'
#' library(openapi)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Common Document Fields
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsPropertiesFindCommonPost(content_type = var_content_type, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsPropertiesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Document Fields
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsPropertiesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTemplatesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Template Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTemplatesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTypesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Document Types
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTypesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTypesTypeGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The document type. See Retrieve Document Types.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Type
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTypesTypeGet(var_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTypesTypeRelationshipsGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The document type. See Retrieve Document Types.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Type Relationships
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTypesTypeRelationshipsGet(var_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTypesTypeSubtypesSubtypeClassificationsClassificationGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The document type. See Retrieve Document Types.
#' var_subtype <- "subtype_example" # character | The document subtype. See Retrieve Document Types.
#' var_classification <- "classification_example" # character | The document classification. See Retrieve Document Types.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Classification
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTypesTypeSubtypesSubtypeClassificationsClassificationGet(var_type, var_subtype, var_classification, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsDocumentsTypesTypeSubtypesSubtypeGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The document type. See Retrieve Document Types.
#' var_subtype <- "subtype_example" # character | The document subtype. See Retrieve Document Types.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Subtype
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsDocumentsTypesTypeSubtypesSubtypeGet(var_type, var_subtype, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsGroupsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Group Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsGroupsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsSecuritypoliciesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Security Policy Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsSecuritypoliciesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataObjectsUsersGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve User Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataObjectsUsersGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataQueryArchivedDocumentsRelationshipsDocumentSignatureSysrGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Archived Document Signature Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataQueryArchivedDocumentsRelationshipsDocumentSignatureSysrGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataQueryDocumentsRelationshipsDocumentSignatureSysrGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Signature Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataQueryDocumentsRelationshipsDocumentSignatureSysrGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsGet  ####################
#'
#' library(openapi)
#' var_loc <- "true" # character | To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Collection
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsGet(loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsObjectNameActionsCanceldeploymentPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Cancel Raw Object Deployment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsObjectNameActionsCanceldeploymentPost(var_object_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsObjectNameFieldsObjectFieldNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_field_name <- "object_field_name_example" # character | The object field name value (id, name__v, external_id__v, etc.).
#' var_loc <- "true" # character | To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Field Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsObjectNameFieldsObjectFieldNameGet(var_object_name, var_object_field_name, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsObjectNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_loc <- "true" # character | To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsObjectNameGet(var_object_name, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsObjectNamePageLayoutsGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Page Layouts
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsObjectNamePageLayoutsGet(var_object_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  MetadataVobjectsObjectNamePageLayoutsLayoutNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object from which to retrieve page layout metadata.
#' var_layout_name <- "layout_name_example" # character | The name of the page layout from which to retrieve metadata.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Page Layout Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$MetadataVobjectsObjectNamePageLayoutsLayoutNameGet(var_object_name, var_layout_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  NotificationsHistoriesGet  ####################
#'
#' library(openapi)
#' var_start_date <- "YYYY-MM-DD" # character | Specify a start date to retrieve notification history. This date cannot be more than 2 years ago. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the start of the day. If start_date is omitted entirely, defaults to the start of the previous day. If you’ve specified a start_date, you must also specify an end_date. (Optional)
#' var_end_date <- "YYYY-MM-DD" # character | Specify an end date to retrieve notification history. This date cannot be more than 30 days away from the specified start_date. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the time of the API request. If you’ve specified an end_date, you must also specify a start_date. (Optional)
#' var_all_dates <- "true" # character | Set to true to request notification history for all dates. This is the same as requesting a full CSV export from the Vault UI. When requesting a full notification history, you must leave start_date and end_date blank and set format_result to csv. You can request an export of notification history for all_dates once every 24 hours. (Optional)
#' var_format_result <- "csv" # character | To request a downloadable CSV file of your notification history, set this parameter to csv. The response contains a jobId to retrieve the job status, which provides a link to download the CSV file. If omitted, the API returns a JSON response with notification history and does not start a job. If all_dates is true, this parameter must be csv. (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the entry returned. If omitted, defaults to 0. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Email Notification Histories
#' api_instance <- DefaultApi$new()
#'
#' api_instance$NotificationsHistoriesGet(start_date = var_start_date, end_date = var_end_date, all_dates = var_all_dates, format_result = var_format_result, limit = var_limit, offset = var_offset, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsActionGet  ####################
#'
#' library(openapi)
#' var_action <- "action_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Bulk Workflow Action Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsActionGet(var_action, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsActionPost  ####################
#'
#' library(openapi)
#' var_action <- "action_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Workflow Actions on Multiple Workflows
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsActionPost(var_action, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsCanceltasksPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Cancel Workflow Tasks
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsCanceltasksPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsCancelworkflowsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Cancel Workflows
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsCancelworkflowsPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Bulk Workflow Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsReassigntasksPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reassign Workflow Tasks
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsReassigntasksPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectWorkflowActionsReplaceworkflowownerPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Replace Workflow Owner
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectWorkflowActionsReplaceworkflowownerPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersActionsExportJobIdResultsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested export job. This is returned with the export binder requests above.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Export Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersActionsExportJobIdResultsGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdActionsExportPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_source <- "true" # character | to include source content or not (Optional)
#' var_renditiontype <- "viewable_rendition__v" # character | to include viewable renditions (Optional)
#' var_docversion <- "major" # character | to include all major versions (Optional)
#' var_attachments <- "all" # character | to include all versions of attachments (Optional)
#' var_export <- "name__v,title__v,document_number__v" # character | configurable filename metadata (Optional)
#' var_docfield <- "false" # character | to exclude document metadata csv (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Binder (Latest Version)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdActionsExportPost(var_binder_id, source = var_source, renditiontype = var_renditiontype, docversion = var_docversion, attachments = var_attachments, export = var_export, docfield = var_docfield, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdActionsPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Refresh Binder Auto-Filing
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdActionsPost(var_binder_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdBindingRulePut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binding Rule
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdBindingRulePut(var_binder_id, content_type = var_content_type, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdDelete(var_binder_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdDocumentsNodeIdBindingRulePut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_node_id <- "node_id_example" # character | The binder node id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder Document Binding Rule
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdDocumentsNodeIdBindingRulePut(var_binder_id, var_node_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdDocumentsPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add Document to Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdDocumentsPost(var_binder_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdDocumentsSectionIdDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_section_id <- "section_id_example" # character | The binder node id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove Document from Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdDocumentsSectionIdDelete(var_binder_id, var_section_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdDocumentsSectionIdPut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_section_id <- "section_id_example" # character | The binder node id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Move Document in Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdDocumentsSectionIdPut(var_binder_id, var_section_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_depth <- "depth_example" # character | To retrieve all information in all levels of the binder, set this to all. By default, only one level is returned. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdGet(var_binder_id, depth = var_depth, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdPost(var_binder_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdPut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdPut(var_binder_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdRolesRoleNameAndUserOrGroupIdDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The id value of the binder from which to remove roles.
#' var_role_name_and_user_or_group <- "role_name_and_user_or_group_example" # character | The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
#' var_id <- "id_example" # character | The id value of the user or group to remove from the role.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove Users & Groups from Roles on a Single Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdRolesRoleNameAndUserOrGroupIdDelete(var_binder_id, var_role_name_and_user_or_group, var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdSectionsNodeIdBindingRulePut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_node_id <- "node_id_example" # character | The binder node id field value.
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder Section Binding Rule
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdSectionsNodeIdBindingRulePut(var_binder_id, var_node_id, content_type = var_content_type, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdSectionsNodeIdPut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_node_id <- "node_id_example" # character | The binder node id of the section.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder Section
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdSectionsNodeIdPut(var_binder_id, var_node_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdSectionsPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder Section
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdSectionsPost(var_binder_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdSectionsSectionIdDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_section_id <- "section_id_example" # character | The binder node id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Binder Section
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdSectionsSectionIdDelete(var_binder_id, var_section_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdSectionsSectionIdGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_section_id <- "section_id_example" # character | The binder node id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Sections
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdSectionsSectionIdGet(var_binder_id, var_section_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Binder Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsGet(var_binder_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionActionsExportPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_source <- "true" # character | to include source content or not (Optional)
#' var_renditiontype <- "viewable_rendition__v" # character | to include viewable renditions (Optional)
#' var_docversion <- "major" # character | to include all major versions (Optional)
#' var_attachments <- "all" # character | to include all versions of attachments (Optional)
#' var_export <- "name__v,title__v,document_number__v" # character | configurable filename metadata (Optional)
#' var_docfield <- "false" # character | to exclude document metadata csv (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Binder (Specific Version)
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionActionsExportPost(var_binder_id, var_major_version, var_minor_version, source = var_source, renditiontype = var_renditiontype, docversion = var_docversion, attachments = var_attachments, export = var_export, docfield = var_docfield, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Binder Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionDelete(var_binder_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionGet(var_binder_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionPut  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionPut(var_binder_id, var_major_version, var_minor_version, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsPost  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsPost(var_binder_id, var_major_version, var_minor_version, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_relationship_id <- "relationship_id_example" # character | The binder relationship id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Binder Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete(var_binder_id, var_major_version, var_minor_version, var_relationship_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_relationship_id <- "relationship_id_example" # character | The binder relationship id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet(var_binder_id, var_major_version, var_minor_version, var_relationship_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersBinderIdVersionsMajorVersionMinorVersionSectionsSectionIdGet  ####################
#'
#' library(openapi)
#' var_binder_id <- "binder_id_example" # character | The binder id field value.
#' var_major_version <- "major_version_example" # character | The binder major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The binder minor_version_number__v field value.
#' var_section_id <- "section_id_example" # character | Retrieve all sections (documents and subsections) in a binder’s sub-level node. If not included, all sections from the binder’s top-level root node will be returned.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Version Section
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionSectionsSectionIdGet(var_binder_id, var_major_version, var_minor_version, var_section_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdRolesGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Binder Roles
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdRolesGet(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdRolesPost  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Assign Users & Groups to Roles on a Single Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdRolesPost(var_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdRolesRoleNameGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The binder `id`.
#' var_role_name <- "role_name_example" # character | The name of the role to retrieve. For example, `owner__v`.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Role
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdRolesRoleNameGet(var_id, var_role_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The binder id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the binder.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the binder.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder User Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsGet(var_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The binder id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the binder.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the binder.
#' var_name__v <- "name__v_example" # character | The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Entry Criteria
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet(var_id, var_major_version, var_minor_version, var_name__v, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The binder id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the binder.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the binder.
#' var_name__v <- "name__v_example" # character | The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Binder User Action
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut(var_id, var_major_version, var_minor_version, var_name__v, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersLifecycleActionsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve User Actions on Multiple Binders
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersLifecycleActionsPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersLifecycleActionsUserActionNamePut  ####################
#'
#' library(openapi)
#' var_user_action_name <- "user_action_name_example" # character | The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Binders endpoint.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Bulk Binder User Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersLifecycleActionsUserActionNamePut(var_user_action_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersPost  ####################
#'
#' library(openapi)
#' var_async <- "true" # character | When creating a binder, the binder metadata is indexed synchronously by default. To process the indexing asynchronously, include a query parameter async set to true (objects/binders?async=true). This helps speed up the response time from Vault when processing large amounts of data.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersPost(async = var_async, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Template Collection
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Binder Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesPut(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesTemplateNameBindernodesGet  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The binder template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Template Node Attributes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesTemplateNameBindernodesGet(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesTemplateNameBindernodesPost  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The binder template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Binder Template Node
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesTemplateNameBindernodesPost(var_template_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesTemplateNameBindernodesPut  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The binder template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Replace Binder Template Nodes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesTemplateNameBindernodesPut(var_template_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesTemplateNameDelete  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The binder template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Binder Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesTemplateNameDelete(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsBindersTemplatesTemplateNameGet  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The binder template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Binder Template Attributes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsBindersTemplatesTemplateNameGet(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDeletionsDocumentsGet  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Specify a date (no more than 30 days past) after which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z (Optional)
#' var_end_date <- "end_date_example" # character | Specify a date (no more than 30 days past) before which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Deleted Document IDs
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDeletionsDocumentsGet(start_date = var_start_date, end_date = var_end_date, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDeletionsVobjectsObjectNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Deleted Object Record ID
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDeletionsVobjectsObjectNameGet(var_object_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsActionsGet  ####################
#'
#' library(openapi)
#' var_loc <- "true" # character | When localized (translated) strings are available, retrieve them by setting loc to true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Document Workflows
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsActionsGet(loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsActionsLifecycleAndStateAndActionJobIdResultsGet  ####################
#'
#' library(openapi)
#' var_lifecycle_and_state_and_action <- "lifecycle_and_state_and_action_example" # character | The `name__v` values for the lifecycle, state, and action in the format `{lifecycle_name}.{state_name}.{action_name}`. To get this value, [Retrieve the Job Status](https://developer.veevavault.com/api/24.3#RetrieveJobStatus) and find the `href` under the `artifacts` link.
#' var_job_id <- "job_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Controlled Copy Job Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsActionsLifecycleAndStateAndActionJobIdResultsGet(var_lifecycle_and_state_and_action, var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsActionsWorkflowNameGet  ####################
#'
#' library(openapi)
#' var_workflow_name <- "workflow_name_example" # character | The document workflow name value.
#' var_loc <- "true" # character | When localized (translated) strings are available, retrieve them by setting loc to true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Workflow Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsActionsWorkflowNameGet(var_workflow_name, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsActionsWorkflowNamePost  ####################
#'
#' library(openapi)
#' var_workflow_name <- "workflow_name_example" # character | The document workflow name value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Document Workflow
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsActionsWorkflowNamePost(var_workflow_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAnnotationsBatchDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Annotations
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAnnotationsBatchDelete(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAnnotationsBatchPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character | 
#' var_content_type <- "application/json" # character | 
#' var_accept <- "application/json" # character | 
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
#'
#' #Create Multiple Annotations
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAnnotationsBatchPost(var_authorization, var_content_type, var_accept, var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAnnotationsBatchPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Annotations
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAnnotationsBatchPut(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAnnotationsRepliesBatchPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add Annotation Replies
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAnnotationsRepliesBatchPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAttachmentsBatchDelete  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Document Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAttachmentsBatchDelete(accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAttachmentsBatchPost  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Multiple Document Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAttachmentsBatchPost(accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsAttachmentsBatchPut  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Document Attachment Descriptions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsAttachmentsBatchPut(accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchActionsFileextractJobIdResultsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested export job. This is returned with the export document requests.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Export Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchActionsFileextractJobIdResultsGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchActionsFileextractPost  ####################
#'
#' library(openapi)
#' var_source <- "true" # character | Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true. (Optional)
#' var_renditions <- "false" # character | Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false. (Optional)
#' var_allversions <- "true" # character | Optional: To include all versions or latest version, include a query parameter allversions=true. If omitted, defaults to false. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchActionsFileextractPost(source = var_source, renditions = var_renditions, allversions = var_allversions, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchActionsReclassifyPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true, Vault allows you to manually set the document number and to update documents to any lifecycle state using the status__v field. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message.  You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reclassify Multiple Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchActionsReclassifyPut(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchActionsRerenderPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Document Renditions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchActionsRerenderPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchDelete  ####################
#'
#' library(openapi)
#' var_id_param <- "id_param_example" # character | If you’re identifying documents in your input by their external ID (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchDelete(id_param = var_id_param, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchLockDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Undo Collaborative Authoring Checkout
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchLockDelete(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true, Vault allows you to create documents in any lifecycle state using the status__v field, and to manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Multiple Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsBatchPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header.Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsBatchPut(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAnchorsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Anchor IDs
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAnchorsGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAnnotationsFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Document Annotations to PDF
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAnnotationsFileGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAnnotationsFilePost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Import Document Annotations from PDF
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAnnotationsFilePost(var_doc_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document Attachment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdDelete(var_doc_id, var_attachment_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Attachment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdFileGet(var_doc_id, var_attachment_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Attachment Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdGet(var_doc_id, var_attachment_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdPut  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Document Attachment Description
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdPut(var_doc_id, var_attachment_id, accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete(var_doc_id, var_attachment_id, var_attachment_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet(var_doc_id, var_attachment_id, var_attachment_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Attachment Version Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionGet(var_doc_id, var_attachment_id, var_attachment_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_restore <- "true" # character | The parameter restore must be set to true.  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Restore Document Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionPost(var_doc_id, var_attachment_id, var_attachment_version, restore = var_restore, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Attachment Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsGet(var_doc_id, var_attachment_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download All Document Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsFileGet(var_doc_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsGet(var_doc_id, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAttachmentsPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Document Attachment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAttachmentsPost(var_doc_id, accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdAudittrailGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document ID for which to retrieve audit history.
#' var_start_date <- "start_date_example" # character | Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vault’s creation date.  (Optional)
#' var_end_date <- "end_date_example" # character | Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date.  (Optional)
#' var_format_result <- "format_result_example" # character | To request a CSV file of your audit history, use csv.  (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0. (Optional)
#' var_events <- "events_example" # character | Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of document audit events. The values passed to this parameter are case sensitive. For example, events=WorkflowCompletion,TaskAssignment. If omitted, defaults to all audit events. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Complete Audit History for a Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdAudittrailGet(var_doc_id, start_date = var_start_date, end_date = var_end_date, format_result = var_format_result, limit = var_limit, offset = var_offset, events = var_events, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdDelete(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdEventsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Events
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdEventsGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_lock_document <- "false" # character | Set to true to Check Out this document before retrieval. If omitted, defaults to false. (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdFileGet(var_doc_id, lock_document = var_lock_document, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdLockDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Document Lock
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdLockDelete(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdLockGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Lock
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdLockGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdLockPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Document Lock
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdLockPost(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdMajorVersionMinorVersionAttachmentsFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download All Document Version Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdMajorVersionMinorVersionAttachmentsFileGet(var_doc_id, var_major_version, var_minor_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_suppress_rendition <- "suppress_rendition_example" # character | Set to true to suppress automatic generation of the viewable rendition. If omitted, defaults to false (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Single Document Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdPost(var_doc_id, suppress_rendition = var_suppress_rendition, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdPut  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdPut(var_doc_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRenditionsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Renditions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRenditionsGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRenditionsRenditionTypeDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRenditionsRenditionTypeDelete(var_doc_id, var_rendition_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRenditionsRenditionTypeGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_steady_state <- "steady_state_example" # character | Set to true to download a rendition (file) from the latest steady state version (1.0, 2.0, etc.) of a document.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Rendition File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRenditionsRenditionTypeGet(var_doc_id, var_rendition_type, steady_state = var_steady_state, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRenditionsRenditionTypePost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add Single Document Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRenditionsRenditionTypePost(var_doc_id, var_rendition_type, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRenditionsRenditionTypePut  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Replace Document Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRenditionsRenditionTypePut(var_doc_id, var_rendition_type, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdRolesRoleNameAndUserOrGroupIdDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The id value of the document from which to remove roles.
#' var_role_name_and_user_or_group <- "role_name_and_user_or_group_example" # character | The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
#' var_id <- "id_example" # character | The id value of the user or group to remove from the role.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove Users & Groups from Roles on a Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdRolesRoleNameAndUserOrGroupIdDelete(var_doc_id, var_role_name_and_user_or_group, var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionMajorVersionMinorVersionAttachmentsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Version Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionMajorVersionMinorVersionAttachmentsGet(var_doc_id, var_major_version, var_minor_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsGet(var_doc_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_annotation_id <- "annotation_id_example" # character | The annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Read Annotations by ID
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdGet(var_doc_id, var_major_version, var_minor_version, var_annotation_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdRepliesGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_annotation_id <- "annotation_id_example" # character | The parent annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Read Replies of Parent Annotation
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdRepliesGet(var_doc_id, var_major_version, var_minor_version, var_annotation_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Document Version Annotations to PDF
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFileGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFilePost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Import Document Version Annotations from PDF
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFilePost(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 500. If omitted, defaults to 500. Values greater than 500 are ignored. (Optional)
#' var_offset <- "offset_example" # character | This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to offset=201. (Optional)
#' var_annotation_types <- "annotation_types_example" # character | The type(s) of annotations to retrieve. For example, note__sys,anchor__sys. If omitted, Vault returns all annotations. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys`  (Optional)
#' var_pagination_id <- "pagination_id_example" # character | A unique identifier used to load requests with paginated results. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Read Annotations by Document Version and Type
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsGet(var_doc_id, var_major_version, var_minor_version, limit = var_limit, offset = var_offset, annotation_types = var_annotation_types, pagination_id = var_pagination_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_attachment_id <- "attachment_id_example" # character | The id field value of the attachment.
#' var_attachment_version <- "attachment_version_example" # character | The version of the attachment.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Version Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionFileGet(var_doc_id, var_major_version, var_minor_version, var_attachment_id, var_attachment_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_attachment_id <- "attachment_id_example" # character | The id of the document attachment to retrieve.
#' var_attachment_version <- "attachment_version_example" # character | Optional: The version of the attachment to retrieve. If omitted, the endpoint retrieves all versions of the specified attachment.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Version Attachment Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionGet(var_doc_id, var_major_version, var_minor_version, var_attachment_id, var_attachment_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDelete(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDocExportAnnotationsToCsvGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Version Notes as CSV
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDocExportAnnotationsToCsvGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionEventsPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Document Event
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionEventsPost(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionExportVideoAnnotationsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The video document id field value.
#' var_major_version <- "major_version_example" # character | The video document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The video document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character | This `Accept` header only changes the format of the response in the case of an error. On `SUCCESS`, the HTTP Response Header `Content-Type` is set to `text/plain;charset=UnicodeLittle`. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Video Annotations
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionExportVideoAnnotationsGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionFileGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Version File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionFileGet(var_doc_id, var_major_version, var_minor_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionPut  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true, Vault allows you to manually set the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Document Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionPut(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Relationships
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsPost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Single Document Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsPost(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_relationship_id <- "relationship_id_example" # character | The relationship id field value. See Retrieve Document Relationships.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Document Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete(var_doc_id, var_major_version, var_minor_version, var_relationship_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_relationship_id <- "relationship_id_example" # character | The relationship id field value. See Retrieve Document Relationships.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Relationship
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet(var_doc_id, var_major_version, var_minor_version, var_relationship_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Version Renditions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsGet(var_doc_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeDelete  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Document Version Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeDelete(var_doc_id, var_major_version, var_minor_version, var_rendition_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Version Rendition File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeGet(var_doc_id, var_major_version, var_minor_version, var_rendition_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePost  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Upload Document Version Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePost(var_doc_id, var_major_version, var_minor_version, var_rendition_type, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePut  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_rendition_type <- "rendition_type_example" # character | The document rendition type.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Replace Document Version Rendition
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePut(var_doc_id, var_major_version, var_minor_version, var_rendition_type, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionThumbnailGet  ####################
#'
#' library(openapi)
#' var_doc_id <- "doc_id_example" # character | The document id field value.
#' var_major_version <- "major_version_example" # character | The document major_version_number__v field value.
#' var_minor_version <- "minor_version_example" # character | The document minor_version_number__v field value.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Version Thumbnail File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionThumbnailGet(var_doc_id, var_major_version, var_minor_version, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsGet  ####################
#'
#' library(openapi)
#' var_named_filter <- "My Documents" # character | Retrieves only documents which you have created. (Optional)
#' var_scope <- "contents" # character | Searches only within the document content. (Optional)
#' var_versionscope <- "all" # character | Retrieves all document versions, rather than only the latest version. (Optional)
#' var_search <- "{keyword}" # character | Search for documents based on a {keyword} in searchable document fields. (Optional)
#' var_limit <- "limit_example" # character | See VQL documentation for more information. (Optional)
#' var_sort <- "sort_example" # character | See VQL documentation for more information. (Optional)
#' var_start <- "start_example" # character | See VQL documentation for more information. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsGet(named_filter = var_named_filter, scope = var_scope, versionscope = var_versionscope, search = var_search, limit = var_limit, sort = var_sort, start = var_start, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdRolesGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Document Roles
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdRolesGet(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdRolesPost  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Assign Users & Groups to Roles on a Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdRolesPost(var_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdRolesRoleNameGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The document `id`.
#' var_role_name <- "role_name_example" # character | The name of the role to retrieve. For example, `owner__v`.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Role
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdRolesRoleNameGet(var_id, var_role_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The document id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the document.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the document.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document User Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsGet(var_id, var_major_version, var_minor_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The document id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the document.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the document.
#' var_name__v <- "name__v_example" # character | The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Entry Criteria
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet(var_id, var_major_version, var_minor_version, var_name__v, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The document id field value from which to retrieve available user actions.
#' var_major_version <- "major_version_example" # character | The major version number of the document.
#' var_minor_version <- "minor_version_example" # character | The minor version number of the document.
#' var_name__v <- "name__v_example" # character | The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Document User Action
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut(var_id, var_major_version, var_minor_version, var_name__v, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsLifecycleActionsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve User Actions on Multiple Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsLifecycleActionsPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsLifecycleActionsUserActionNamePut  ####################
#'
#' library(openapi)
#' var_user_action_name <- "user_action_name_example" # character | The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Documents endpoint.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Bulk Document User Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsLifecycleActionsUserActionNamePut(var_user_action_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | When set to true,  you can use the status__v field to create documents in any lifecycle state. Additionally, you can manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Single Document
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsPost(authorization = var_authorization, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRelationshipsBatchDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Document Relationships
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRelationshipsBatchDelete(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRelationshipsBatchPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Multiple Document Relationships
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRelationshipsBatchPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRenditionsBatchDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Document Renditions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRenditionsBatchDelete(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRenditionsBatchPost  ####################
#'
#' library(openapi)
#' var_id_param <- "id_param_example" # character | If you’re identifying documents in your input by their external ID (Optional)
#' var_large_size_asset <- "large_size_asset_example" # character | If set to true, indicates that the renditions to add are of the Large Size Asset (large_size_asset__v) rendition type. Vault applies Document Migration Mode limitations to renditions created with the request, but Document Migration permission is not required and your vault need not be in Migration Mode to use the parameter. Note that the request results in an error if the CSV contains any rendition type other than large_size_asset__v. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | Must be set to true when importing any rendition type other than large_size_asset__v. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add Multiple Document Renditions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRenditionsBatchPost(id_param = var_id_param, large_size_asset = var_large_size_asset, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRolesBatchDelete  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove Users and Groups from Roles on Multiple Documents & Binders
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRolesBatchDelete(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsRolesBatchPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Assign Users & Groups to Roles on Multiple Documents & Binders
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsRolesBatchPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Template Collection
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Single Document Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Document Templates
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesPut(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesTemplateNameDelete  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The document template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Basic Document Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesTemplateNameDelete(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesTemplateNameFileGet  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The document template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Document Template File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesTemplateNameFileGet(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesTemplateNameGet  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The document template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Document Template Attributes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesTemplateNameGet(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTemplatesTemplateNamePut  ####################
#'
#' library(openapi)
#' var_template_name <- "template_name_example" # character | The document template name__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Single Document Template
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTemplatesTemplateNamePut(var_template_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsTokensPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Document Tokens
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsTokensPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsVersionsBatchActionsFileextractPost  ####################
#'
#' library(openapi)
#' var_source <- "true" # character | Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true. (Optional)
#' var_renditions <- "false" # character | Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Document Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsVersionsBatchActionsFileextractPost(source = var_source, renditions = var_renditions, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsVersionsBatchDelete  ####################
#'
#' library(openapi)
#' var_id_param <- "id_param_example" # character | If you’re identifying documents in your input by their external ID (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Document Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsVersionsBatchDelete(id_param = var_id_param, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDocumentsVersionsBatchPost  ####################
#'
#' library(openapi)
#' var_id_param <- "external_id__v" # character | If you’re identifying documents in your input by their external ID (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_migration_mode <- "x_vault_api_migration_mode_example" # character | Must be set to true. Vault allows you to manually set the name and version number and to create documents in any lifecycle state using the `status__v` field, but does not allow you to change the document number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a> (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Multiple Document Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDocumentsVersionsBatchPost(id_param = var_id_param, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDomainGet  ####################
#'
#' library(openapi)
#' var_include_application <- "true" # character | To include Vault application type information in the response, set include_application to true. If omitted, defaults to false and application information is not included. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Domain Information
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDomainGet(include_application = var_include_application, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsDomainsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Domains
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsDomainsGet(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsEdlMatchedDocumentsBatchActionsAddPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add EDL Matched Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsEdlMatchedDocumentsBatchActionsAddPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsEdlMatchedDocumentsBatchActionsRemovePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove EDL Matched Documents
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsEdlMatchedDocumentsBatchActionsRemovePost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsAutoGet  ####################
#'
#' library(openapi)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 1000.  (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Auto Managed Groups
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsAutoGet(limit = var_limit, offset = var_offset, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsGet  ####################
#'
#' library(openapi)
#' var_include_implied <- "include_implied_example" # character | When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. If omitted, the response includes only the members__v field. These users are individually added to a group by an Admin. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Groups
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsGet(include_implied = var_include_implied, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsGroupIdDelete  ####################
#'
#' library(openapi)
#' var_group_id <- "group_id_example" # character | The group id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Group
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsGroupIdDelete(var_group_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsGroupIdGet  ####################
#'
#' library(openapi)
#' var_group_id <- "group_id_example" # character | The group id field value.
#' var_include_implied <- "true" # character | When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. When not used, the response includes only the members__v field. These users are individually added to a group by Admin. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Group
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsGroupIdGet(var_group_id, include_implied = var_include_implied, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsGroupIdPut  ####################
#'
#' library(openapi)
#' var_group_id <- "group_id_example" # character | The group id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Group
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsGroupIdPut(var_group_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsGroupsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Group 
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsGroupsPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsLicensesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Application License Usage
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsLicensesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsActionsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Multi-Record Workflows
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsActionsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsActionsWorkflowNameGet  ####################
#'
#' library(openapi)
#' var_workflow_name <- "workflow_name_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Multi-Record Workflow Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsActionsWorkflowNameGet(var_workflow_name, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsActionsWorkflowNamePost  ####################
#'
#' library(openapi)
#' var_workflow_name <- "workflow_name_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Multi-Record Workflow
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsActionsWorkflowNamePost(var_workflow_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsGet  ####################
#'
#' library(openapi)
#' var_object__v <- "object__v_example" # character | To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used.  (Optional)
#' var_record_id__v <- "record_id__v_example" # character | To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used.  (Optional)
#' var_participant <- "participant_example" # character | To retrieve all workflows available to a particular user, include the user id field value as ?participant={id}. To retrieve your own workflows, set this value to ?participant=me. This parameter is required when the object__v and record_id__v parameters are not used.  (Optional)
#' var_status__v <- "status__v_example" # character | To retrieve all workflows with specific statuses, include one or more status name__v field values. For example: status__v=active__v, status__v=active__v,completed__v. Workflows with `status__v=active__v` are in progress for the indicated object record. Valid statuses include: active__v completed__v cancelled__v (Optional)
#' var_offset <- "offset_example" # character | This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201.  (Optional)
#' var_page_size <- "page_size_example" # character | This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000.  (Optional)
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by including ?loc=true.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflows
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsGet(object__v = var_object__v, record_id__v = var_record_id__v, participant = var_participant, status__v = var_status__v, offset = var_offset, page_size = var_page_size, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksGet  ####################
#'
#' library(openapi)
#' var_object__v <- "object__v_example" # character | To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used. (Optional)
#' var_record_id__v <- "record_id__v_example" # character | To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used. (Optional)
#' var_assignee__v <- "assignee__v_example" # character | To retrieve all workflow tasks available to a particular user, include the user id field value as ?assignee__v={id}. To retrieve your own workflow tasks, set this value to ?assignee__v=me. This parameter is required when the object__v and record_id__v parameters are not used.  (Optional)
#' var_status__v <- "status__v_example" # character | To retrieve all workflow tasks with specific statuses, include one or more status name__v field values. For example: ?status__v=available__v or ?status__v=available__v,completed__v.  (Optional)
#' var_offset <- "offset_example" # character | This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201.  (Optional)
#' var_page_size <- "page_size_example" # character | This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000.  (Optional)
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by including ?loc=true.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Tasks
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksGet(object__v = var_object__v, record_id__v = var_record_id__v, assignee__v = var_assignee__v, status__v = var_status__v, offset = var_offset, page_size = var_page_size, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsAcceptPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Accept Single Record Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsAcceptPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsCancelPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Cancel Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsCancelPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsCompletePost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Complete Single Record Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsCompletePost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsGet  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Task Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsGet(var_task_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsMdwacceptPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Accept Multi-item Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsMdwacceptPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsMdwcompletePost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Complete Multi-item Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsMdwcompletePost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsMdwmanagecontentPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Manage Multi-Item Workflow Content
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsMdwmanagecontentPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsMdwreassignPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The id of the task to reassign.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reassign Multi-item Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsMdwreassignPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsReassignPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The id of the task to reassign.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reassign Single Record Workflow Task
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsReassignPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsTaskActionGet  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_task_action <- "task_action_example" # character | The name of the task action retrieved from Retrieve Workflow Task Actions.
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by including loc=true.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Task Action Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsTaskActionGet(var_task_id, var_task_action, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsUndoacceptPost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Undo Workflow Task Acceptance
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsUndoacceptPost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdActionsUpdateduedatePost  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The id of the task.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Workflow Task Due Date
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdActionsUpdateduedatePost(var_task_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsTasksTaskIdGet  ####################
#'
#' library(openapi)
#' var_task_id <- "task_id_example" # character | The task id field value.
#' var_loc <- "true" # character | When localized (translated) strings are available, retrieve them by including loc=true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Task Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsTasksTaskIdGet(var_task_id, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsWorkflowIdActionsGet  ####################
#'
#' library(openapi)
#' var_workflow_id <- "workflow_id_example" # character | The workflow id field value.
#' var_loc <- "loc_example" # character | When localized (translated) strings are available, retrieve them by including loc=true.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsWorkflowIdActionsGet(var_workflow_id, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionGet  ####################
#'
#' library(openapi)
#' var_workflow_id <- "workflow_id_example" # character | The workflow id field value.
#' var_workflow_action <- "workflow_action_example" # character | The workflow action name retrieved from Retrieve Workflow Actions.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Action Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionGet(var_workflow_id, var_workflow_action, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionPost  ####################
#'
#' library(openapi)
#' var_workflow_id <- "workflow_id_example" # character | The workflow id field value.
#' var_workflow_action <- "workflow_action_example" # character | The workflow action name retrieved from Retrieve Workflow Actions.
#' var_documents__sys <- "documents__sys_example" # character | Include the id or ids as a comma-separated list of the document(s) to be removed from a document workflow when using the removecontent action. If your workflow_action is remove_content, include the id of the document to remove the workflow. To remove multiple documents, use a comma-separated list of ids. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Workflow Action
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionPost(var_workflow_id, var_workflow_action, documents__sys = var_documents__sys, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsObjectworkflowsWorkflowIdGet  ####################
#'
#' library(openapi)
#' var_workflow_id <- "workflow_id_example" # character | The workflow id field value.
#' var_loc <- "true" # character | When localized (translated) strings are available, retrieve them by including loc=true. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Workflow Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsObjectworkflowsWorkflowIdGet(var_workflow_id, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Picklists
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsPicklistNameGet  ####################
#'
#' library(openapi)
#' var_picklist_name <- "picklist_name_example" # character | The picklist name field value (license_type__v, product_family__c, region__c, etc.)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Picklist Values
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsPicklistNameGet(var_picklist_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsPicklistNamePicklistValueNameDelete  ####################
#'
#' library(openapi)
#' var_picklist_name <- "picklist_name_example" # character | The picklist name field value (license_type__v, product_family__c, region__c, etc.)
#' var_picklist_value_name <- "picklist_value_name_example" # character | The picklist value name field value (north_america__c, south_america__c, etc.)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Inactivate Picklist Value
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsPicklistNamePicklistValueNameDelete(var_picklist_name, var_picklist_value_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsPicklistNamePicklistValueNamePut  ####################
#'
#' library(openapi)
#' var_picklist_name <- "picklist_name_example" # character | The picklist name field value (license_type__v, product_family__c, region__c, etc.)
#' var_picklist_value_name <- "picklist_value_name_example" # character | The picklist value name field value (north_america__c, south_america__c, etc.)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Picklist Value
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsPicklistNamePicklistValueNamePut(var_picklist_name, var_picklist_value_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsPicklistNamePost  ####################
#'
#' library(openapi)
#' var_picklist_name <- "picklist_name_example" # character | The picklist name field value (license_type__v, product_family__c, region__c, etc.)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Picklist Values
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsPicklistNamePost(var_picklist_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsPicklistsPicklistNamePut  ####################
#'
#' library(openapi)
#' var_picklist_name <- "picklist_name_example" # character | The picklist name field value (license_type__v, product_family__c, region__c, etc.)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Picklist Value Label
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsPicklistsPicklistNamePut(var_picklist_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxActionsBuildproductionPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Build Production Vault
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxActionsBuildproductionPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxActionsPromoteproductionPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Promote to Production
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxActionsPromoteproductionPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxActionsRecheckusagePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Recheck Sandbox Usage Limit
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxActionsRecheckusagePost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxBatchChangesizePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Change Sandbox Size
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxBatchChangesizePost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxEntitlementsSetPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Set Sandbox Entitlements
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxEntitlementsSetPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Sandboxes
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxNameDelete  ####################
#'
#' library(openapi)
#' var_name <- "name_example" # character | The name of the sandbox vault to delete. This is the name which appears on the My Vaults page.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Sandbox
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxNameDelete(var_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create or Refresh Sandbox
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxSnapshotApiNameActionsUpdatePost  ####################
#'
#' library(openapi)
#' var_api_name <- "api_name_example" # character | The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Sandbox Snapshot
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxSnapshotApiNameActionsUpdatePost(var_api_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxSnapshotApiNameActionsUpgradePost  ####################
#'
#' library(openapi)
#' var_api_name <- "api_name_example" # character | The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Upgrade Sandbox Snapshot
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxSnapshotApiNameActionsUpgradePost(var_api_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxSnapshotApiNameDelete  ####################
#'
#' library(openapi)
#' var_api_name <- "api_name_example" # character | The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Sandbox Snapshot
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxSnapshotApiNameDelete(var_api_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxSnapshotGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Sandbox Snapshots
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxSnapshotGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxSnapshotPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Sandbox Snapshot
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxSnapshotPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxVaultIdActionsRefreshPost  ####################
#'
#' library(openapi)
#' var_vault_id <- "vault_id_example" # character | The Vault ID of the sandbox to be refreshed.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Refresh Sandbox from Snapshot
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxVaultIdActionsRefreshPost(var_vault_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSandboxVaultIdGet  ####################
#'
#' library(openapi)
#' var_vault_id <- "vault_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Sandbox Details by ID
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSandboxVaultIdGet(var_vault_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSecuritypoliciesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Security Policies
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSecuritypoliciesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsSecuritypoliciesSecurityPolicyNameGet  ####################
#'
#' library(openapi)
#' var_security_policy_name <- "security_policy_name_example" # character | Security policy name__v field value (retrieved from previous request). This is typically a numeric value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Security Policy
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsSecuritypoliciesSecurityPolicyNameGet(var_security_policy_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersGet  ####################
#'
#' library(openapi)
#' var_vaults <- "all" # character | Retrieve all users assigned to all Vaults in your domain. (Optional)
#' var_exclude_vault_membership <- "exclude_vault_membership_example" # character | Optional: Set to false to include vault_membership fields. If true or omitted, vault_membership fields are not included in the response. (Optional)
#' var_exclude_app_licensing <- "exclude_app_licensing_example" # character | Optional: Set to false to include app_licensing fields. If true or omitted, app_licensing fields are not included in the response. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Users
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersGet(vaults = var_vaults, exclude_vault_membership = var_exclude_vault_membership, exclude_app_licensing = var_exclude_app_licensing, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersIdGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The user id field value. Use the value me to get information for the currently authenticated user.
#' var_exclude_vault_membership <- "exclude_vault_membership_example" # character | Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response. (Optional)
#' var_exclude_app_licensing <- "exclude_app_licensing_example" # character | Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersIdGet(var_id, exclude_vault_membership = var_exclude_vault_membership, exclude_app_licensing = var_exclude_app_licensing, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersIdPermissionsGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of the user. Use the value me to retrieve information for the currently authenticated user.
#' var_filter <- "filter_example" # character | Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve User Permissions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersIdPermissionsGet(var_id, filter = var_filter, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersIdPut  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The user id field value. Use the value me to get information for the currently authenticated user.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Single User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersIdPut(var_id, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersMeGet  ####################
#'
#' library(openapi)
#' var_exclude_vault_membership <- "exclude_vault_membership_example" # character | Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response. (Optional)
#' var_exclude_app_licensing <- "exclude_app_licensing_example" # character | Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Validate Session User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersMeGet(exclude_vault_membership = var_exclude_vault_membership, exclude_app_licensing = var_exclude_app_licensing, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersMePasswordPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Change My Password
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersMePasswordPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersMePermissionsGet  ####################
#'
#' library(openapi)
#' var_filter <- "filter_example" # character | Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve My User Permissions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersMePermissionsGet(filter = var_filter, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersMePut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update My User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersMePut(authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Single User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersPut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Users
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersPut(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersUserIdDelete  ####################
#'
#' library(openapi)
#' var_user_id <- "user_id_example" # character | The user id field value. 
#' var_domain <- "true" # character | When true, this disables the user account in all vaults in the domain. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Disable User
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersUserIdDelete(var_user_id, domain = var_domain, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsUsersUserIdVaultMembershipVaultIdPut  ####################
#'
#' library(openapi)
#' var_user_id <- "user_id_example" # character | The user id field value.
#' var_vault_id <- "vault_id_example" # character | The system-managed id field value assigned to each vault in the domain.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Vault Membership
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsUsersUserIdVaultMembershipVaultIdPut(var_user_id, var_vault_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsVaultActionsComparePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Vault Compare
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsVaultActionsComparePost(authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ObjectsVaultActionsConfigreportPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Vault Configuration Report
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ObjectsVaultActionsConfigreportPost(authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  QueryComponentsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Component Definition Query
#' api_instance <- DefaultApi$new()
#'
#' api_instance$QueryComponentsPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  QueryNextPagePost  ####################
#'
#' library(openapi)
#' var_next_page <- "next_page_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_describe_query <- "true" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Next Page URL
#' api_instance <- DefaultApi$new()
#'
#' api_instance$QueryNextPagePost(var_next_page, authorization = var_authorization, accept = var_accept, x_vault_api_describe_query = var_x_vault_api_describe_query, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  QueryPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_describe_query <- "true" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Submitting a Query
#' api_instance <- DefaultApi$new()
#'
#' api_instance$QueryPost(authorization = var_authorization, accept = var_accept, x_vault_api_describe_query = var_x_vault_api_describe_query, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  QueryPreviousPagePost  ####################
#'
#' library(openapi)
#' var_previous_page <- "previous_page_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_describe_query <- "true" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Previous Page URL
#' api_instance <- DefaultApi$new()
#'
#' api_instance$QueryPreviousPagePost(var_previous_page, authorization = var_authorization, accept = var_accept, x_vault_api_describe_query = var_x_vault_api_describe_query, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2MeGet  ####################
#'
#' library(openapi)
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Current User with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2MeGet(attributes = var_attributes, excluded_attributes = var_excluded_attributes, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2MePut  ####################
#'
#' library(openapi)
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_content_type <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Current User with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2MePut(attributes = var_attributes, excluded_attributes = var_excluded_attributes, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2ResourceTypesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All SCIM Resource Types
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2ResourceTypesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2ResourceTypesTypeGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | A specific resource type. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the id value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single SCIM Resource Type
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2ResourceTypesTypeGet(var_type, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2SchemasGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All SCIM Schema Information
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2SchemasGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2SchemasIdGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The ID of a specific schema. For example, urn:ietf:params:scim:schemas:extension:veevavault:2.0:User.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single SCIM Schema Information
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2SchemasIdGet(var_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2ServiceProviderConfigGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve SCIM Provider
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2ServiceProviderConfigGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2TypeGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
#' var_filter <- "filter_example" # character | Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator. (Optional)
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_sort_by <- "sort_by_example" # character | Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage (Optional)
#' var_sort_order <- "sort_order_example" # character | Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000. (Optional)
#' var_start_index <- "start_index_example" # character | Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve SCIM Resources
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2TypeGet(var_type, filter = var_filter, attributes = var_attributes, excluded_attributes = var_excluded_attributes, sort_by = var_sort_by, sort_order = var_sort_order, start_index = var_start_index, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2TypeIdGet  ####################
#'
#' library(openapi)
#' var_type <- "type_example" # character | The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
#' var_id <- "id_example" # character | The ID of the resource to retrieve. You can retrieve all resource IDs from a particular resource type with the Retrieve SCIM Resources endpoint. For example, business_admin__v.
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single SCIM Resource
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2TypeIdGet(var_type, var_id, attributes = var_attributes, excluded_attributes = var_excluded_attributes, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2UsersGet  ####################
#'
#' library(openapi)
#' var_filter <- "filter_example" # character | Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator. (Optional)
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_sort_by <- "sort_by_example" # character | Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage (Optional)
#' var_sort_order <- "sort_order_example" # character | Optional: Specify the order in which the sortBy parameter is applied. Allowed values are ascending or descending. If omitted, defaults to ascending. (Optional)
#' var_count <- "count_example" # character | Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000. (Optional)
#' var_start_index <- "start_index_example" # character | Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Users with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2UsersGet(filter = var_filter, attributes = var_attributes, excluded_attributes = var_excluded_attributes, sort_by = var_sort_by, sort_order = var_sort_order, count = var_count, start_index = var_start_index, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2UsersIdGet  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The id of the user you wish to update.
#' var_filter <- "filter_example" # character | Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator. (Optional)
#' var_attributes <- "attributes_example" # character | Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned. (Optional)
#' var_excluded_attributes <- "excluded_attributes_example" # character | Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single User with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2UsersIdGet(var_id, filter = var_filter, attributes = var_attributes, excluded_attributes = var_excluded_attributes, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2UsersIdPut  ####################
#'
#' library(openapi)
#' var_id <- "id_example" # character | The id of the user you wish to update.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_content_type <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update User with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2UsersIdPut(var_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ScimV2UsersPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/scim+json" # character |  (Optional)
#' var_content_type <- "application/scim+json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create User with SCIM
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ScimV2UsersPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesCertificateCertIdGet  ####################
#'
#' library(openapi)
#' var_cert_id <- "cert_id_example" # character | The cert_id is provided in each Spark message in the X-VaultAPISignature-CertificateId header.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Signing Certificate
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesCertificateCertIdGet(var_cert_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesConfigurationModeActionsDisablePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "content_type_example" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Disable Configuration Mode
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesConfigurationModeActionsDisablePost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesConfigurationModeActionsEnablePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "content_type_example" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Enable Configuration Mode
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesConfigurationModeActionsEnablePost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesDirectdataFilesGet  ####################
#'
#' library(openapi)
#' var_extract_type <- "incremental_directdata" # character | The Direct Data file type: incremental_directdata, full_directdata, or log_directdata. If omitted, returns all files. (Optional)
#' var_start_time <- 2023-12-07T00:00Z # integer | Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 7AM on January 15, 2024 would use 2024-01-15T07:00:00Z. If omitted, defaults to the Vault’s creation date and time. (Optional)
#' var_stop_time <- 2023-12-08T00:00Z # integer | Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 9AM on January 15, 2024 would use 2024-01-15T09:00:00Z. If omitted, defaults to today’s date and current time. (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Available Direct Data Files
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesDirectdataFilesGet(extract_type = var_extract_type, start_time = var_start_time, stop_time = var_stop_time, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesDirectdataFilesNameGet  ####################
#'
#' library(openapi)
#' var_name <- "name_example" # character | The name of the Direct Data file part. Obtain this from the Retrieve Available Direct Data Files request. For example, 146478-20240213-0000-F.001.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Direct Data File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesDirectdataFilesNameGet(var_name, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingItemsContentItemGet  ####################
#'
#' library(openapi)
#' var_item <- "item_example" # character | The absolute path to a file or folder. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_range <- "range_example" # character | Optional: Specifies a partial range of bytes to include in the upload. Maximum 50 MB. Must be in the format `bytes={min}-{max}`. For example, `bytes=0-1000`. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Item Content
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingItemsContentItemGet(var_item, authorization = var_authorization, accept = var_accept, range = var_range, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingItemsItemDelete  ####################
#'
#' library(openapi)
#' var_item <- "item_example" # character | The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
#' var_recursive <- "recursive_example" # character | Applicable to deleting folders only. If true, the request will delete the contents of a folder and all subfolders. The default is false. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete File or Folder
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingItemsItemDelete(var_item, recursive = var_recursive, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingItemsItemGet  ####################
#'
#' library(openapi)
#' var_item <- "item_example" # character | The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
#' var_recursive <- "recursive_example" # character | If true, the response will contain the contents of all subfolders. If not specified, the default value is false. (Optional)
#' var_limit <- "limit_example" # character | Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000. (Optional)
#' var_format_result <- "format_result_example" # character | If set to csv, the response includes a job_id. Use the Job ID value to retrieve the status and results of the request. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #List Items at a Path
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingItemsItemGet(var_item, recursive = var_recursive, limit = var_limit, format_result = var_format_result, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingItemsItemPut  ####################
#'
#' library(openapi)
#' var_item <- "item_example" # character | The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Folder or File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingItemsItemPut(var_item, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingItemsPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_md5 <- "content_md5_example" # character | Optional: The MD5 checksum of the file being uploaded. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Folder or File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingItemsPost(authorization = var_authorization, accept = var_accept, content_md5 = var_content_md5, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #List Upload Sessions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Resumable Upload Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadPost(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadUploadSessionIdDelete  ####################
#'
#' library(openapi)
#' var_upload_session_id <- "upload_session_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Abort Upload Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadUploadSessionIdDelete(var_upload_session_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadUploadSessionIdGet  ####################
#'
#' library(openapi)
#' var_upload_session_id <- "upload_session_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Get Upload Session Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadUploadSessionIdGet(var_upload_session_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadUploadSessionIdPartsGet  ####################
#'
#' library(openapi)
#' var_upload_session_id <- "upload_session_id_example" # character | 
#' var_limit <- "limit_example" # character | Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #List File Parts Uploaded to Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadUploadSessionIdPartsGet(var_upload_session_id, limit = var_limit, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadUploadSessionIdPost  ####################
#'
#' library(openapi)
#' var_upload_session_id <- "upload_session_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Commit Upload Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadUploadSessionIdPost(var_upload_session_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesFileStagingUploadUploadSessionIdPut  ####################
#'
#' library(openapi)
#' var_upload_session_id <- "upload_session_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/octet-stream" # character |  (Optional)
#' var_x_vault_api_file_part_number <- "x_vault_api_file_part_number_example" # character | The part number, which uniquely identifies a file part and defines its position within the file as a whole. If a part is uploaded using a part number that has already been used, Vault overwrites the previously uploaded file part. You must upload parts in numerical order. For example, you cannot upload part 3 without first uploading parts 1 and 2. (Optional)
#' var_content_md5 <- "content_md5_example" # character | Optional: The MD5 checksum of the file part being uploaded. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Upload to a Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesFileStagingUploadUploadSessionIdPut(var_upload_session_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_file_part_number = var_x_vault_api_file_part_number, content_md5 = var_content_md5, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsHistoriesGet  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Sets the date to start retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job. (Optional)
#' var_end_date <- "end_date_example" # character | Sets the date to end retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time. (Optional)
#' var_status <- "status_example" # character | Filter to only retrieve jobs in a certain status. Allowed values are success, errors_encountered, failed_to_run, missed_schedule, cancelled. If omitted, retrieves all statuses. (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the first job history returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Job Histories
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsHistoriesGet(start_date = var_start_date, end_date = var_end_date, status = var_status, limit = var_limit, offset = var_offset, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsJobIdErrorsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested import job. This was returned from the Import Bulk Translation File request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Import Bulk Translation File Job Errors
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsJobIdErrorsGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsJobIdGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The ID of the job, returned from the original job request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Job Status
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsJobIdGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsJobIdSummaryGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested import job. This was returned from the Import Bulk Translation File request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Import Bulk Translation File Job Summary
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsJobIdSummaryGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsJobIdTasksGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The ID of the SDK job, returned from the original job request.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve SDK Job Tasks
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsJobIdTasksGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsMonitorsGet  ####################
#'
#' library(openapi)
#' var_start_date <- "start_date_example" # character | Sets the date to start retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job. (Optional)
#' var_end_date <- "end_date_example" # character | Sets the date to end retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time. (Optional)
#' var_status <- "status_example" # character | Filter to only retrieve jobs in a certain status. Allowed values are scheduled, queued, running. If omitted, retrieves all statuses. (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of jobs per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the first job instance returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Job Monitors
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsMonitorsGet(start_date = var_start_date, end_date = var_end_date, status = var_status, limit = var_limit, offset = var_offset, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesJobsStartNowJobIdPost  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The ID of the scheduled job instance to start.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Start Job
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesJobsStartNowJobIdPost(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderExtractPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Extract Data Files
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderExtractPost(authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderJobIdTasksTaskIdFailurelogGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested extract job.
#' var_task_id <- "task_id_example" # character | The id value of the requested extract task.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Load Failure Log Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderJobIdTasksTaskIdFailurelogGet(var_job_id, var_task_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderJobIdTasksTaskIdResultsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested extract job.
#' var_task_id <- "task_id_example" # character | The id value of the requested extract task.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Loader Extract Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderJobIdTasksTaskIdResultsGet(var_job_id, var_task_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderJobIdTasksTaskIdResultsRenditionsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested extract job.
#' var_task_id <- "task_id_example" # character | The id value of the requested extract task.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Loader Extract Renditions Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderJobIdTasksTaskIdResultsRenditionsGet(var_job_id, var_task_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderJobIdTasksTaskIdSuccesslogGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The id value of the requested extract job.
#' var_task_id <- "task_id_example" # character | The id value of the requested extract task.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Load Success Log Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderJobIdTasksTaskIdSuccesslogGet(var_job_id, var_task_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesLoaderLoadPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Load Data Objects
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesLoaderLoadPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesPackageActionsValidatePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Validate Package
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesPackageActionsValidatePost(authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesPackagePost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Export Package
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesPackagePost(authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesPackagePut  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Import Package
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesPackagePut(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesQueuesGet  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Queues
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesQueuesGet(authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesQueuesQueueNameActionsDisableDeliveryPut  ####################
#'
#' library(openapi)
#' var_queue_name <- "queue_name_example" # character | The name of a specific Queue.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Disable Delivery
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesQueuesQueueNameActionsDisableDeliveryPut(var_queue_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesQueuesQueueNameActionsEnableDeliveryPut  ####################
#'
#' library(openapi)
#' var_queue_name <- "queue_name_example" # character | The name of a specific Queue.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Enable Delivery
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesQueuesQueueNameActionsEnableDeliveryPut(var_queue_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesQueuesQueueNameActionsResetPut  ####################
#'
#' library(openapi)
#' var_queue_name <- "queue_name_example" # character | The name of a specific Queue.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Reset Queue
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesQueuesQueueNameActionsResetPut(var_queue_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesQueuesQueueNameGet  ####################
#'
#' library(openapi)
#' var_queue_name <- "queue_name_example" # character | The name of a specific queue. For example, queue__c.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Queue Status
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesQueuesQueueNameGet(var_queue_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  ServicesVobjectVaultPackageVPackageIdActionsValidatePost  ####################
#'
#' library(openapi)
#' var_package_id <- "package_id_example" # character | The ID of the package to validate. You can find this in the API response of a package import, or in the URL of package in the Vault UI.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Validate Imported Package
#' api_instance <- DefaultApi$new()
#'
#' api_instance$ServicesVobjectVaultPackageVPackageIdActionsValidatePost(var_package_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  SessionDelete  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character | The Vault sessionId to end. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #End Session
#' api_instance <- DefaultApi$new()
#'
#' api_instance$SessionDelete(accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  UicodeDistributionsDistributionNameCodeGet  ####################
#'
#' library(openapi)
#' var_distribution_name <- "distribution_name_example" # character | The name attribute of the client code distribution to download.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Single Client Code Distribution
#' api_instance <- DefaultApi$new()
#'
#' api_instance$UicodeDistributionsDistributionNameCodeGet(var_distribution_name, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  UicodeDistributionsDistributionNameDelete  ####################
#'
#' library(openapi)
#' var_distribution_name <- "distribution_name_example" # character | The name attribute of the client code distribution to delete.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Single Client Code Distribution
#' api_instance <- DefaultApi$new()
#'
#' api_instance$UicodeDistributionsDistributionNameDelete(var_distribution_name, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  UicodeDistributionsDistributionNameGet  ####################
#'
#' library(openapi)
#' var_distribution_name <- "distribution_name_example" # character | The name attribute of the client code distribution to delete.
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Single Client Code Distribution Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$UicodeDistributionsDistributionNameGet(var_distribution_name, accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  UicodeDistributionsGet  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve All Client Code Distribution Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$UicodeDistributionsGet(accept = var_accept, authorization = var_authorization, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  UicodeDistributionsPost  ####################
#'
#' library(openapi)
#' var_accept <- "application/json" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "multipart/form-data" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Add or Replace Single Client Code Distribution
#' api_instance <- DefaultApi$new()
#'
#' api_instance$UicodeDistributionsPost(accept = var_accept, authorization = var_authorization, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectVaultPackageVPackageIdActionsDeployPost  ####################
#'
#' library(openapi)
#' var_package_id <- "package_id_example" # character | The id field value of the vault_package__v object record used for deployment. See Import Package.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Deploy Package
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectVaultPackageVPackageIdActionsDeployPost(var_package_id, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectVaultPackageVPackageIdActionsDeployResultsGet  ####################
#'
#' library(openapi)
#' var_package_id <- "package_id_example" # character | The id field value of the vault_package__v object record used for deployment. See Deploy Package.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Package Deploy Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectVaultPackageVPackageIdActionsDeployResultsGet(var_package_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsCascadedeleteResultsObjectNameJobStatusJobIdGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_job_status <- "job_status_example" # character | 
#' var_job_id <- "job_id_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Results of Cascade Delete Job
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsCascadedeleteResultsObjectNameJobStatusJobIdGet(var_object_name, var_job_status, var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsDeepcopyResultsObjectNameJobStatusJobIdGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_job_status <- "job_status_example" # character | The ID of the job, retrieved from the response of the job request.
#' var_job_id <- "job_id_example" # character | Possible values are success or failure. Find if your job succeeded or failed by retrieving the job status.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Results of Deep Copy Job
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsDeepcopyResultsObjectNameJobStatusJobIdGet(var_object_name, var_job_status, var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsEdlItemVActionsCreateplaceholderPost  ####################
#'
#' library(openapi)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create a Placeholder from an EDL Item
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsEdlItemVActionsCreateplaceholderPost(authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsMergesJobIdLogGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Merge Records Job Log
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsMergesJobIdLogGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsMergesJobIdResultsGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Record Merge Results
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsMergesJobIdResultsGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsMergesJobIdStatusGet  ####################
#'
#' library(openapi)
#' var_job_id <- "job_id_example" # character | The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Record Merge Status
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsMergesJobIdStatusGet(var_job_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameActionsActionNamePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value.
#' var_action_name <- "action_name_example" # character | Either the name of the Objectaction or Objectlifecyclestateuseraction to initiate. This is obtained from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Object Action on Multiple Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsActionNamePost(var_object_name, var_action_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameActionsChangetypePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Change Object Type
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsChangetypePost(var_object_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameActionsMergePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value. For example, account__v. This object must have Enable Merges configured.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#' var__vobjects__object_name__actions_merge_post_request_inner <- c(_vobjects__object_name__actions_merge_post_request_inner$new("duplicate_record_id_example", "main_record_id_example")) # array[VobjectsObjectNameActionsMergePostRequestInner] |  (Optional)
#'
#' #Initiate Record Merge
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsMergePost(var_object_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id, _vobjects__object_name__actions_merge_post_request_inner = var__vobjects__object_name__actions_merge_post_request_inner)
#'
#'
#' ####################  VobjectsObjectNameActionsRecalculaterollupsGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object for which to check the status of a Roll-up field recalculation.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Roll-up Field Recalculation Status
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsRecalculaterollupsGet(var_object_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameActionsRecalculaterollupsPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object for which to check the status of a Roll-up field recalculation.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Recalculate Roll-up Fields
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsRecalculaterollupsPost(var_object_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameActionsUpdatecorporatecurrencyPut  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Corporate Currency Fields
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameActionsUpdatecorporatecurrencyPut(var_object_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameAttachmentsBatchDelete  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_id_param <- "If you’re identifying attachments in your input by external id, add idParam=external_id__v to the request endpoint." # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Multiple Object Record Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameAttachmentsBatchDelete(var_object_name, id_param = var_id_param, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameAttachmentsBatchPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Multiple Object Record Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameAttachmentsBatchPost(var_object_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameAttachmentsBatchPut  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Multiple Object Record Attachment Descriptions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameAttachmentsBatchPut(var_object_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameDelete  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character | Can be text/csv or application/json (Optional)
#' var_accept <- "application/json" # character | Can be text/csv or application/json (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Object Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameDelete(var_object_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameIdRolesRoleNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name.
#' var_id <- "id_example" # character | The id of the document, binder, or object record.
#' var_role_name <- "role_name_example" # character | Optional: Include a role name to filter for a specific role. For example, owner__v.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record Roles
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameIdRolesRoleNameGet(var_object_name, var_id, var_role_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdActionsActionNameGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value from which to retrieve user actions.
#' var_action_name <- "action_name_example" # character | The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object User Actions Details
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdActionsActionNameGet(var_object_name, var_object_record_id, var_action_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdActionsActionNamePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value from which to retrieve user actions.
#' var_action_name <- "action_name_example" # character | The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "application/x-www-form-urlencoded" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Initiate Object Action on a Single Record
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdActionsActionNamePost(var_object_name, var_object_record_id, var_action_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdActionsCascadedeletePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Cascade Delete Object Record
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdActionsCascadedeletePost(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdActionsDeepcopyPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Deep Copy Object Record
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdActionsDeepcopyPost(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdActionsGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_loc <- "true" # character | Optional: When true, retrieves localized (translated) strings for the label.  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record User Actions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdActionsGet(var_object_name, var_object_record_id, loc = var_loc, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFileGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value. For example, product__v.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_field_name <- "attachment_field_name_example" # character | The name of the Attachment field to update.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Attachment Field File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFileGet(var_object_name, var_object_record_id, var_attachment_field_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFilePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value. For example, product__v.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_field_name <- "attachment_field_name_example" # character | The name of the Attachment field to update.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Attachment Field File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFilePost(var_object_name, var_object_record_id, var_attachment_field_name, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentFieldsFileGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value. For example, product__v.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download All Attachment Field Files
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentFieldsFileGet(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdDelete  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Object Record Attachment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdDelete(var_object_name, var_object_record_id, var_attachment_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record Attachment Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdGet(var_object_name, var_object_record_id, var_attachment_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdPut  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Object Record Attachment Description
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdPut(var_object_name, var_object_record_id, var_attachment_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Delete Object Record Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete(var_object_name, var_object_record_id, var_attachment_id, var_attachment_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | 
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download Object Record Attachment File
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet(var_object_name, var_object_record_id, var_attachment_id, var_attachment_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record Attachment Version Metadata
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionGet(var_object_name, var_object_record_id, var_attachment_id, var_attachment_version, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_attachment_version <- "attachment_version_example" # character | The attachment version__v field value.
#' var_restore <- "true" # character |  (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Restore Object Record Attachment Version
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionPost(var_object_name, var_object_record_id, var_attachment_id, var_attachment_version, restore = var_restore, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_attachment_id <- "attachment_id_example" # character | The attachment id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record Attachment Versions
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsGet(var_object_name, var_object_record_id, var_attachment_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsFileGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Download All Object Record Attachment Files
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsFileGet(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record Attachments
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsGet(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAttachmentsPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create Object Record Attachment
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAttachmentsPost(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdAudittrailGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value.
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value from which to retrieve user actions.
#' var_start_date <- "start_date_example" # character | Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vVault’s creation date.  (Optional)
#' var_end_date <- "end_date_example" # character | Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date.  (Optional)
#' var_format_result <- "format_result_example" # character | To request a CSV file of your audit history, use csv.  (Optional)
#' var_limit <- "limit_example" # character | Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200. (Optional)
#' var_offset <- "offset_example" # character | Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0. (Optional)
#' var_events <- "events_example" # character | Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of object audit events. The values passed to this parameter are case sensitive. For example, events=Copy,Edit,Delete. If omitted, defaults to all audit events. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Complete Audit History for a Single Object Record
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdAudittrailGet(var_object_name, var_object_record_id, start_date = var_start_date, end_date = var_end_date, format_result = var_format_result, limit = var_limit, offset = var_offset, events = var_events, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameObjectRecordIdGet  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_object_record_id <- "object_record_id_example" # character | The object record id field value.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Object Record
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameObjectRecordIdGet(var_object_name, var_object_record_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNamePost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_id_param <- "id_param_example" # character | To upsert object records, add `idParam={field_name}` to the request endpoint. You can use any object field which has `unique` set to `true` in the object metadata. For example, `idParam=external_id__v`. (Optional)
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character | Can be text/csv or application/json (Optional)
#' var_accept <- "application/json" # character | Can be text/csv or application/json (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | If set to `true`, Vault allows you to create or update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header. (Optional)
#' var_x_vault_api_no_triggers <- "true" # character | If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Create & Upsert Object Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNamePost(var_object_name, id_param = var_id_param, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_no_triggers = var_x_vault_api_no_triggers, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNamePut  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The object name__v field value (product__v, country__v, custom_object__c, etc.).
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_content_type <- "text/csv" # character | Can be text/csv or application/json (Optional)
#' var_accept <- "application/json" # character | Can be text/csv or application/json (Optional)
#' var_x_vault_api_migration_mode <- "true" # character | If set to `true`, Vault allows you to update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header. (Optional)
#' var_x_vault_api_no_triggers <- "true" # character | If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers. (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Update Object Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNamePut(var_object_name, authorization = var_authorization, content_type = var_content_type, accept = var_accept, x_vault_api_migration_mode = var_x_vault_api_migration_mode, x_vault_api_no_triggers = var_x_vault_api_no_triggers, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameRolesDelete  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object where you want to remove roles.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Remove Users & Groups from Roles on Object Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameRolesDelete(var_object_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsObjectNameRolesPost  ####################
#'
#' library(openapi)
#' var_object_name <- "object_name_example" # character | The name of the object where you want to remove roles.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_content_type <- "text/csv" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Assign Users & Groups to Roles on Object Records
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsObjectNameRolesPost(var_object_name, authorization = var_authorization, accept = var_accept, content_type = var_content_type, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' ####################  VobjectsOutboundPackageVPackageIdDependenciesGet  ####################
#'
#' library(openapi)
#' var_package_id <- "package_id_example" # character | The ID of the outbound_package__v record from which to retrieve dependencies.
#' var_authorization <- "{{sessionId}}" # character |  (Optional)
#' var_accept <- "application/json" # character |  (Optional)
#' var_x_vault_api_client_id <- "{{clientId}}" # character | Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log. (Optional)
#'
#' #Retrieve Outbound Package Dependencies
#' api_instance <- DefaultApi$new()
#'
#' api_instance$VobjectsOutboundPackageVPackageIdDependenciesGet(var_package_id, authorization = var_authorization, accept = var_accept, x_vault_api_client_id = var_x_vault_api_client_id)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DefaultApi <- R6::R6Class(
  "DefaultApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new DefaultApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Retrieve API Versions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve API Versions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Content File
    #'
    #' @param component_type_and_record_name The component type of the record followed by the name of the record from which to retrieve the content file. The format is `{Componenttype}.{record_name}`. For example, `Formattedoutput.my_formatted_output__c`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlComponentsComponentTypeAndRecordNameFilesGet = function(component_type_and_record_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlComponentsComponentTypeAndRecordNameFilesGetWithHttpInfo(component_type_and_record_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Content File
    #'
    #' @param component_type_and_record_name The component type of the record followed by the name of the record from which to retrieve the content file. The format is `{Componenttype}.{record_name}`. For example, `Formattedoutput.my_formatted_output__c`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlComponentsComponentTypeAndRecordNameFilesGetWithHttpInfo = function(component_type_and_record_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_type_and_record_name`)) {
        stop("Missing required parameter `component_type_and_record_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/components/{component_type_and_record_name}/files"
      if (!missing(`component_type_and_record_name`)) {
        local_var_url_path <- gsub("\\{component_type_and_record_name\\}", URLencode(as.character(`component_type_and_record_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Component Record (MDL)
    #'
    #' @param component_type_and_record_name The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlComponentsComponentTypeAndRecordNameGet = function(component_type_and_record_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlComponentsComponentTypeAndRecordNameGetWithHttpInfo(component_type_and_record_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Component Record (MDL)
    #'
    #' @param component_type_and_record_name The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlComponentsComponentTypeAndRecordNameGetWithHttpInfo = function(component_type_and_record_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_type_and_record_name`)) {
        stop("Missing required parameter `component_type_and_record_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/components/{component_type_and_record_name}"
      if (!missing(`component_type_and_record_name`)) {
        local_var_url_path <- gsub("\\{component_type_and_record_name\\}", URLencode(as.character(`component_type_and_record_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Asynchronous MDL Script Results
    #'
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlExecuteAsyncJobIdResultsGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlExecuteAsyncJobIdResultsGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Asynchronous MDL Script Results
    #'
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlExecuteAsyncJobIdResultsGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/execute_async/{job_id}/results"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Execute MDL Script Asynchronously
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlExecuteAsyncPost = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlExecuteAsyncPostWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Execute MDL Script Asynchronously
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlExecuteAsyncPostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/execute_async"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Execute MDL Script
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlExecutePost = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlExecutePostWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Execute MDL Script
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlExecutePostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/execute"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Upload Content File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ApiMdlFilesPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ApiMdlFilesPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Upload Content File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ApiMdlFilesPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/api/mdl/files"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Audit Details
    #'
    #' @param audit_trail_type The name of the specified audit type (document_audit_trail, object_audit_trail, etc). Use the Retrieve Audit Types API to retrieve types available in your Vault. Requests for login_audit_trail only accept one request per user at a time.
    #' @param start_date (optional) Specify a start date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days.
    #' @param end_date (optional) Specify an end date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days.
    #' @param all_dates (optional) Set to true to request audit information for all dates. You must leave start_date and end_date blank when requesting an export of a full audit trail.
    #' @param format_result (optional) To request a downloadable CSV file of your audit details, use csv. The response contains a jobId to retrieve the job status, which contains a link to download the CSV file. If omitted, the API returns a JSON response and does not start a job. If all_dates is true, this parameter is required.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param objects (optional) This is an optional parameter when specifying object_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more object names to retrieve their audit details. For example, objects=product__v,country__v. If omitted, defaults to all objects.
    #' @param events (optional) This is an optional parameter when specifying object_audit_trail or document_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more audit events to retrieve their audit details. For example, events=Edit,Delete,TaskAssignment. If omitted, defaults to all audit events. See Vault Help for full lists of object audit events and document audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    AudittrailAuditTrailTypeGet = function(audit_trail_type, start_date = NULL, end_date = NULL, all_dates = NULL, format_result = NULL, limit = NULL, offset = NULL, objects = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$AudittrailAuditTrailTypeGetWithHttpInfo(audit_trail_type, start_date, end_date, all_dates, format_result, limit, offset, objects, events, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Audit Details
    #'
    #' @param audit_trail_type The name of the specified audit type (document_audit_trail, object_audit_trail, etc). Use the Retrieve Audit Types API to retrieve types available in your Vault. Requests for login_audit_trail only accept one request per user at a time.
    #' @param start_date (optional) Specify a start date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days.
    #' @param end_date (optional) Specify an end date to retrieve audit information. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z. If omitted, defaults to the last 30 days.
    #' @param all_dates (optional) Set to true to request audit information for all dates. You must leave start_date and end_date blank when requesting an export of a full audit trail.
    #' @param format_result (optional) To request a downloadable CSV file of your audit details, use csv. The response contains a jobId to retrieve the job status, which contains a link to download the CSV file. If omitted, the API returns a JSON response and does not start a job. If all_dates is true, this parameter is required.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param objects (optional) This is an optional parameter when specifying object_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more object names to retrieve their audit details. For example, objects=product__v,country__v. If omitted, defaults to all objects.
    #' @param events (optional) This is an optional parameter when specifying object_audit_trail or document_audit_trail as the {audit_trail_type}. Provide a comma-separated list of one or more audit events to retrieve their audit details. For example, events=Edit,Delete,TaskAssignment. If omitted, defaults to all audit events. See Vault Help for full lists of object audit events and document audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    AudittrailAuditTrailTypeGetWithHttpInfo = function(audit_trail_type, start_date = NULL, end_date = NULL, all_dates = NULL, format_result = NULL, limit = NULL, offset = NULL, objects = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`audit_trail_type`)) {
        stop("Missing required parameter `audit_trail_type`.")
      }













      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["all_dates"]] <- `all_dates`

      query_params[["format_result"]] <- `format_result`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["objects"]] <- `objects`

      query_params[["events"]] <- `events`

      local_var_url_path <- "/audittrail/{audit_trail_type}"
      if (!missing(`audit_trail_type`)) {
        local_var_url_path <- gsub("\\{audit_trail_type\\}", URLencode(as.character(`audit_trail_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Authentication Type Discovery
    #'
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    AuthDiscoveryPost = function(accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$AuthDiscoveryPostWithHttpInfo(accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Authentication Type Discovery
    #'
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    AuthDiscoveryPostWithHttpInfo = function(accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE



      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/auth/discovery"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' OAuth 2.0 / OpenID Connect
    #'
    #' @param oath_oidc_profile_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    AuthOauthSessionOathOidcProfileIdPost = function(oath_oidc_profile_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$AuthOauthSessionOathOidcProfileIdPostWithHttpInfo(oath_oidc_profile_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' OAuth 2.0 / OpenID Connect
    #'
    #' @param oath_oidc_profile_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    AuthOauthSessionOathOidcProfileIdPostWithHttpInfo = function(oath_oidc_profile_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`oath_oidc_profile_id`)) {
        stop("Missing required parameter `oath_oidc_profile_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/auth/oauth/session/{oath_oidc_profile_id}"
      if (!missing(`oath_oidc_profile_id`)) {
        local_var_url_path <- gsub("\\{oath_oidc_profile_id\\}", URLencode(as.character(`oath_oidc_profile_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' User Name and Password
    #'
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    AuthPost = function(content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$AuthPostWithHttpInfo(content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' User Name and Password
    #'
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    AuthPostWithHttpInfo = function(content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/auth"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Source Code File
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeClassNameDelete = function(class_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeClassNameDeleteWithHttpInfo(class_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Source Code File
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeClassNameDeleteWithHttpInfo = function(class_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class_name`)) {
        stop("Missing required parameter `class_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/{class_name}"
      if (!missing(`class_name`)) {
        local_var_url_path <- gsub("\\{class_name\\}", URLencode(as.character(`class_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Disable Vault Extension
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeClassNameDisablePut = function(class_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeClassNameDisablePutWithHttpInfo(class_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Disable Vault Extension
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeClassNameDisablePutWithHttpInfo = function(class_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class_name`)) {
        stop("Missing required parameter `class_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/{class_name}/disable"
      if (!missing(`class_name`)) {
        local_var_url_path <- gsub("\\{class_name\\}", URLencode(as.character(`class_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Enable Vault Extension
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeClassNameEnablePut = function(class_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeClassNameEnablePutWithHttpInfo(class_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Enable Vault Extension
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeClassNameEnablePutWithHttpInfo = function(class_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class_name`)) {
        stop("Missing required parameter `class_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/{class_name}/enable"
      if (!missing(`class_name`)) {
        local_var_url_path <- gsub("\\{class_name\\}", URLencode(as.character(`class_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single Source Code File
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeClassNameGet = function(class_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeClassNameGetWithHttpInfo(class_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single Source Code File
    #'
    #' @param class_name The fully qualified class name of your file.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeClassNameGetWithHttpInfo = function(class_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`class_name`)) {
        stop("Missing required parameter `class_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/{class_name}"
      if (!missing(`class_name`)) {
        local_var_url_path <- gsub("\\{class_name\\}", URLencode(as.character(`class_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Profiling Sessions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Profiling Sessions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Profiling Session
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Profiling Session
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' End Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerSessionNameActionsEndPost = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerSessionNameActionsEndPostWithHttpInfo(session_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' End Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerSessionNameActionsEndPostWithHttpInfo = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`session_name`)) {
        stop("Missing required parameter `session_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler/{session_name}/actions/end"
      if (!missing(`session_name`)) {
        local_var_url_path <- gsub("\\{session_name\\}", URLencode(as.character(`session_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerSessionNameDelete = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerSessionNameDeleteWithHttpInfo(session_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerSessionNameDeleteWithHttpInfo = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`session_name`)) {
        stop("Missing required parameter `session_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler/{session_name}"
      if (!missing(`session_name`)) {
        local_var_url_path <- gsub("\\{session_name\\}", URLencode(as.character(`session_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerSessionNameGet = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerSessionNameGetWithHttpInfo(session_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Profiling Session
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerSessionNameGetWithHttpInfo = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`session_name`)) {
        stop("Missing required parameter `session_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler/{session_name}"
      if (!missing(`session_name`)) {
        local_var_url_path <- gsub("\\{session_name\\}", URLencode(as.character(`session_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Profiling Session Results
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodeProfilerSessionNameResultsGet = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodeProfilerSessionNameResultsGetWithHttpInfo(session_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Profiling Session Results
    #'
    #' @param session_name The name of the session, for example,  baseline__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodeProfilerSessionNameResultsGetWithHttpInfo = function(session_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`session_name`)) {
        stop("Missing required parameter `session_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code/profiler/{session_name}/results"
      if (!missing(`session_name`)) {
        local_var_url_path <- gsub("\\{session_name\\}", URLencode(as.character(`session_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add or Replace Single Source Code File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CodePut = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CodePutWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add or Replace Single Source Code File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CodePutWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/code"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Specific Root Nodes
    #'
    #' @param edl_hierarchy_or_template Choose to retrieve either edl_hierarchy__v or edl_template__v
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CompositesTreesEdlHierarchyOrTemplateActionsListnodesPost = function(edl_hierarchy_or_template, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CompositesTreesEdlHierarchyOrTemplateActionsListnodesPostWithHttpInfo(edl_hierarchy_or_template, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Specific Root Nodes
    #'
    #' @param edl_hierarchy_or_template Choose to retrieve either edl_hierarchy__v or edl_template__v
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CompositesTreesEdlHierarchyOrTemplateActionsListnodesPostWithHttpInfo = function(edl_hierarchy_or_template, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`edl_hierarchy_or_template`)) {
        stop("Missing required parameter `edl_hierarchy_or_template`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/composites/trees/{edl_hierarchy_or_template}/actions/listnodes"
      if (!missing(`edl_hierarchy_or_template`)) {
        local_var_url_path <- gsub("\\{edl_hierarchy_or_template\\}", URLencode(as.character(`edl_hierarchy_or_template`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Root Nodes
    #'
    #' @param edl_hierarchy_or_template 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CompositesTreesEdlHierarchyOrTemplateGet = function(edl_hierarchy_or_template, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CompositesTreesEdlHierarchyOrTemplateGetWithHttpInfo(edl_hierarchy_or_template, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Root Nodes
    #'
    #' @param edl_hierarchy_or_template 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CompositesTreesEdlHierarchyOrTemplateGetWithHttpInfo = function(edl_hierarchy_or_template, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`edl_hierarchy_or_template`)) {
        stop("Missing required parameter `edl_hierarchy_or_template`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/composites/trees/{edl_hierarchy_or_template}"
      if (!missing(`edl_hierarchy_or_template`)) {
        local_var_url_path <- gsub("\\{edl_hierarchy_or_template\\}", URLencode(as.character(`edl_hierarchy_or_template`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve a Node's Children
    #'
    #' @param parent_node_id The ID of a parent node in the hierarchy.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CompositesTreesEdlHierarchyVParentNodeIdChildrenGet = function(parent_node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CompositesTreesEdlHierarchyVParentNodeIdChildrenGetWithHttpInfo(parent_node_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve a Node's Children
    #'
    #' @param parent_node_id The ID of a parent node in the hierarchy.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CompositesTreesEdlHierarchyVParentNodeIdChildrenGetWithHttpInfo = function(parent_node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`parent_node_id`)) {
        stop("Missing required parameter `parent_node_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/composites/trees/edl_hierarchy__v/{parent_node_id}/children"
      if (!missing(`parent_node_id`)) {
        local_var_url_path <- gsub("\\{parent_node_id\\}", URLencode(as.character(`parent_node_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Node Order
    #'
    #' @param parent_node_id The ID of a parent node in the hierarchy.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    CompositesTreesEdlHierarchyVParentNodeIdChildrenPut = function(parent_node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$CompositesTreesEdlHierarchyVParentNodeIdChildrenPutWithHttpInfo(parent_node_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Node Order
    #'
    #' @param parent_node_id The ID of a parent node in the hierarchy.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    CompositesTreesEdlHierarchyVParentNodeIdChildrenPutWithHttpInfo = function(parent_node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`parent_node_id`)) {
        stop("Missing required parameter `parent_node_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/composites/trees/edl_hierarchy__v/{parent_node_id}/children"
      if (!missing(`parent_node_id`)) {
        local_var_url_path <- gsub("\\{parent_node_id\\}", URLencode(as.character(`parent_node_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Component Record (XML/JSON)
    #'
    #' @param component_type_and_record_name The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationComponentTypeAndRecordNameGet = function(component_type_and_record_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationComponentTypeAndRecordNameGetWithHttpInfo(component_type_and_record_name, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Component Record (XML/JSON)
    #'
    #' @param component_type_and_record_name The component type name (`Picklist`, `Docfield`, `Doctype`, etc.) followed by the name of the record from which to retrieve metadata. The format is `{Componenttype}.{record_name}`. For example, `Picklist.color__c`. Find this with the [Retrieve Component Record Collection](https://developer.veevavault.com/api/24.3#Retrieve_Component_Record_Collection) endpoint.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationComponentTypeAndRecordNameGetWithHttpInfo = function(component_type_and_record_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_type_and_record_name`)) {
        stop("Missing required parameter `component_type_and_record_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/configuration/{component_type_and_record_name}"
      if (!missing(`component_type_and_record_name`)) {
        local_var_url_path <- gsub("\\{component_type_and_record_name\\}", URLencode(as.character(`component_type_and_record_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Component Record Collection
    #'
    #' @param component_type 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationComponentTypeGet = function(component_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationComponentTypeGetWithHttpInfo(component_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Component Record Collection
    #'
    #' @param component_type 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationComponentTypeGetWithHttpInfo = function(component_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_type`)) {
        stop("Missing required parameter `component_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/configuration/{component_type}"
      if (!missing(`component_type`)) {
        local_var_url_path <- gsub("\\{component_type\\}", URLencode(as.character(`component_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Details from a Specific Object
    #'
    #' @param object_name_and_object_type The object name followed by the object type in the format `Objecttype.{object_name}.{object_type}`. For example, `Objecttype.product__v.base__v`.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationObjectNameAndObjectTypeGet = function(object_name_and_object_type, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationObjectNameAndObjectTypeGetWithHttpInfo(object_name_and_object_type, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Details from a Specific Object
    #'
    #' @param object_name_and_object_type The object name followed by the object type in the format `Objecttype.{object_name}.{object_type}`. For example, `Objecttype.product__v.base__v`.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationObjectNameAndObjectTypeGetWithHttpInfo = function(object_name_and_object_type, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name_and_object_type`)) {
        stop("Missing required parameter `object_name_and_object_type`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/configuration/{object_name_and_object_type}"
      if (!missing(`object_name_and_object_type`)) {
        local_var_url_path <- gsub("\\{object_name_and_object_type\\}", URLencode(as.character(`object_name_and_object_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Details from All Object Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationObjecttypeGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationObjecttypeGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Details from All Object Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationObjecttypeGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/configuration/Objecttype"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Lifecycle Role Assignment Override Rules
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationRoleAssignmentRuleDelete = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationRoleAssignmentRuleDeleteWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Lifecycle Role Assignment Override Rules
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationRoleAssignmentRuleDeleteWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/configuration/role_assignment_rule"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Lifecycle Role Assignment Rules (Default & Override)
    #'
    #' @param lifecycle__v (optional) Include the name of the lifecycle from which to retrieve information. For example: lifecycle_v=general_lifecycle__c
    #' @param role__v (optional) Include the name of the role from which to retrieve information. For example: role__v=editor__c
    #' @param product__v (optional) Include the ID/name of a specific product to see product-based override rules to default users/allowed users for the lifecycle role. For example: product__v=0PR0011001 or product__v.name__v=CholeCap
    #' @param country__v (optional) Include the ID/name of a specific country to see country-based override rules to default users/allowed users for the lifecycle role. For example: country__v=0CR0022002 or country__v.name__v=United States
    #' @param study__v (optional) In eTMF Vaults only. Include the ID/name of a specific study to see study-based override rules to default users/allowed users for the lifecycle role. For example: study__v=0ST0021J01 or study__v.name__v=CholeCap Study
    #' @param study_country__v (optional) In eTMF Vaults only. Include the ID/name of a specific study country to see study country-based override rules to default users/allowed users for the lifecycle role. For example: study_country__v=0SC0001001 or study_country__v.name__v=Germany
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationRoleAssignmentRuleGet = function(lifecycle__v = NULL, role__v = NULL, product__v = NULL, country__v = NULL, study__v = NULL, study_country__v = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationRoleAssignmentRuleGetWithHttpInfo(lifecycle__v, role__v, product__v, country__v, study__v, study_country__v, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Lifecycle Role Assignment Rules (Default & Override)
    #'
    #' @param lifecycle__v (optional) Include the name of the lifecycle from which to retrieve information. For example: lifecycle_v=general_lifecycle__c
    #' @param role__v (optional) Include the name of the role from which to retrieve information. For example: role__v=editor__c
    #' @param product__v (optional) Include the ID/name of a specific product to see product-based override rules to default users/allowed users for the lifecycle role. For example: product__v=0PR0011001 or product__v.name__v=CholeCap
    #' @param country__v (optional) Include the ID/name of a specific country to see country-based override rules to default users/allowed users for the lifecycle role. For example: country__v=0CR0022002 or country__v.name__v=United States
    #' @param study__v (optional) In eTMF Vaults only. Include the ID/name of a specific study to see study-based override rules to default users/allowed users for the lifecycle role. For example: study__v=0ST0021J01 or study__v.name__v=CholeCap Study
    #' @param study_country__v (optional) In eTMF Vaults only. Include the ID/name of a specific study country to see study country-based override rules to default users/allowed users for the lifecycle role. For example: study_country__v=0SC0001001 or study_country__v.name__v=Germany
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationRoleAssignmentRuleGetWithHttpInfo = function(lifecycle__v = NULL, role__v = NULL, product__v = NULL, country__v = NULL, study__v = NULL, study_country__v = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE










      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["lifecycle__v"]] <- `lifecycle__v`

      query_params[["role__v"]] <- `role__v`

      query_params[["product__v"]] <- `product__v`

      query_params[["country__v"]] <- `country__v`

      query_params[["study__v"]] <- `study__v`

      query_params[["study_country__v"]] <- `study_country__v`

      local_var_url_path <- "/configuration/role_assignment_rule"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Lifecycle Role Assignment Override Rules
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationRoleAssignmentRulePost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationRoleAssignmentRulePostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Lifecycle Role Assignment Override Rules
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationRoleAssignmentRulePostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/configuration/role_assignment_rule"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Lifecycle Role Assignment Rules (Default & Override)
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ConfigurationRoleAssignmentRulePut = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ConfigurationRoleAssignmentRulePutWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Lifecycle Role Assignment Rules (Default & Override)
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ConfigurationRoleAssignmentRulePutWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/configuration/role_assignment_rule"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Delegated Session
    #'
    #' @param authorization (optional) The sessionId of the currently authenticated user who will initiate the delegated session. Cannot be a delegated_sessionid.
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    DelegationLoginPost = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$DelegationLoginPostWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Delegated Session
    #'
    #' @param authorization (optional) The sessionId of the currently authenticated user who will initiate the delegated session. Cannot be a delegated_sessionid.
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DelegationLoginPostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/delegation/login"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Delegations
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    DelegationVaultsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$DelegationVaultsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Delegations
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DelegationVaultsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/delegation/vaults"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Session Keep Alive
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    KeepAlivePost = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$KeepAlivePostWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Session Keep Alive
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    KeepAlivePostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/keep-alive"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Limits on Objects
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LimitsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LimitsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Limits on Objects
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LimitsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/limits"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Daily API Usage
    #'
    #' @param date (optional) The day to retrieve the API Usage log. Date is in UTC and follows the format YYYY-MM-DD. Date cannot be more than 30 days in the past.
    #' @param log_format (optional) Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsApiUsageGet = function(date = NULL, log_format = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsApiUsageGetWithHttpInfo(date, log_format, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Daily API Usage
    #'
    #' @param date (optional) The day to retrieve the API Usage log. Date is in UTC and follows the format YYYY-MM-DD. Date cannot be more than 30 days in the past.
    #' @param log_format (optional) Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsApiUsageGetWithHttpInfo = function(date = NULL, log_format = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["date"]] <- `date`

      query_params[["log_format"]] <- `log_format`

      local_var_url_path <- "/logs/api_usage"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Debug Logs
    #'
    #' @param user_id (optional) Filter results to retrieve the debug log for this user ID only. If omitted, this request retrieves debug logs for all users in the Vault.
    #' @param include_inactive (optional) Set to `true` to include debug log sessions with a status of `inactive__sys` in the response. If omitted, defaults to `false` and inactive sessions are not included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugGet = function(user_id = NULL, include_inactive = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugGetWithHttpInfo(user_id, include_inactive, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Debug Logs
    #'
    #' @param user_id (optional) Filter results to retrieve the debug log for this user ID only. If omitted, this request retrieves debug logs for all users in the Vault.
    #' @param include_inactive (optional) Set to `true` to include debug log sessions with a status of `inactive__sys` in the response. If omitted, defaults to `false` and inactive sessions are not included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugGetWithHttpInfo = function(user_id = NULL, include_inactive = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["user_id"]] <- `user_id`

      query_params[["include_inactive"]] <- `include_inactive`

      local_var_url_path <- "/logs/code/debug"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Debug Log
    #'
    #' @param id The ID of the debug log to delete.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugIdActionsResetDelete = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugIdActionsResetDeleteWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Debug Log
    #'
    #' @param id The ID of the debug log to delete.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugIdActionsResetDeleteWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/logs/code/debug/{id}/actions/reset"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reset Debug Log
    #'
    #' @param id The ID of the debug log to delete.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugIdActionsResetPost = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugIdActionsResetPostWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reset Debug Log
    #'
    #' @param id The ID of the debug log to delete.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugIdActionsResetPostWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/logs/code/debug/{id}/actions/reset"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Debug Log Files
    #'
    #' @param id The ID of the debug log to download.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugIdFilesGet = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugIdFilesGetWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Debug Log Files
    #'
    #' @param id The ID of the debug log to download.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugIdFilesGetWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/logs/code/debug/{id}/files"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single Debug Log
    #'
    #' @param id The ID of the debug log to retrieve.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugIdGet = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugIdGetWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single Debug Log
    #'
    #' @param id The ID of the debug log to retrieve.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugIdGetWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/logs/code/debug/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Debug Log
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeDebugPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeDebugPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Debug Log
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeDebugPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/logs/code/debug"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download SDK Runtime Log
    #'
    #' @param date (optional) No description
    #' @param log_format (optional) Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    LogsCodeRuntimeGet = function(date = NULL, log_format = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$LogsCodeRuntimeGetWithHttpInfo(date, log_format, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download SDK Runtime Log
    #'
    #' @param date (optional) No description
    #' @param log_format (optional) Optional: Specify the format to download. Possible values are csv or logfile. If omitted, defaults to csv. Note that this call always downloads a ZIP file. This parameter only changes the format of the file contained within the ZIP. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    LogsCodeRuntimeGetWithHttpInfo = function(date = NULL, log_format = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["date"]] <- `date`

      query_params[["log_format"]] <- `log_format`

      local_var_url_path <- "/logs/code/runtime"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Import Bulk Translation File
    #'
    #' @param message_type The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MessagesMessageTypeActionsImportPost = function(message_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MessagesMessageTypeActionsImportPostWithHttpInfo(message_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Import Bulk Translation File
    #'
    #' @param message_type The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MessagesMessageTypeActionsImportPostWithHttpInfo = function(message_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_type`)) {
        stop("Missing required parameter `message_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/messages/{message_type}/actions/import"
      if (!missing(`message_type`)) {
        local_var_url_path <- gsub("\\{message_type\\}", URLencode(as.character(`message_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Bulk Translation File
    #'
    #' @param message_type The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
    #' @param lang A valid language code value, for example, en. Retrieve available values from the Admin Key (admin_key__sys) field on the Language (language__sys) object. Active and Inactive languages are both valid. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MessagesMessageTypeLanguageLangActionsExportPost = function(message_type, lang, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MessagesMessageTypeLanguageLangActionsExportPostWithHttpInfo(message_type, lang, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Bulk Translation File
    #'
    #' @param message_type The message type name: field_labels__sys, system_messages__sys, notification_template_messages__sys, or user_account_messages__sys.
    #' @param lang A valid language code value, for example, en. Retrieve available values from the Admin Key (admin_key__sys) field on the Language (language__sys) object. Active and Inactive languages are both valid. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MessagesMessageTypeLanguageLangActionsExportPostWithHttpInfo = function(message_type, lang, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`message_type`)) {
        stop("Missing required parameter `message_type`.")
      }

      if (missing(`lang`)) {
        stop("Missing required parameter `lang`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/messages/{message_type}/language/{lang}/actions/export"
      if (!missing(`message_type`)) {
        local_var_url_path <- gsub("\\{message_type\\}", URLencode(as.character(`message_type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`lang`)) {
        local_var_url_path <- gsub("\\{lang\\}", URLencode(as.character(`lang`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Audit Metadata
    #'
    #' @param audit_trail_type The name of the specified audit type (document_audit_trail, object_audit_trail, etc).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataAudittrailAuditTrailTypeGet = function(audit_trail_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataAudittrailAuditTrailTypeGetWithHttpInfo(audit_trail_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Audit Metadata
    #'
    #' @param audit_trail_type The name of the specified audit type (document_audit_trail, object_audit_trail, etc).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataAudittrailAuditTrailTypeGetWithHttpInfo = function(audit_trail_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`audit_trail_type`)) {
        stop("Missing required parameter `audit_trail_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/audittrail/{audit_trail_type}"
      if (!missing(`audit_trail_type`)) {
        local_var_url_path <- gsub("\\{audit_trail_type\\}", URLencode(as.character(`audit_trail_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Audit Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataAudittrailGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataAudittrailGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Audit Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataAudittrailGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/audittrail"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Component Type Metadata
    #'
    #' @param component_type The component type name (Picklist, Docfield, Doctype, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataComponentsComponentTypeGet = function(component_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataComponentsComponentTypeGetWithHttpInfo(component_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Component Type Metadata
    #'
    #' @param component_type The component type name (Picklist, Docfield, Doctype, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataComponentsComponentTypeGetWithHttpInfo = function(component_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`component_type`)) {
        stop("Missing required parameter `component_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/components/{component_type}"
      if (!missing(`component_type`)) {
        local_var_url_path <- gsub("\\{component_type\\}", URLencode(as.character(`component_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Component Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataComponentsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataComponentsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Component Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataComponentsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/components"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Template Node Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsBindersTemplatesBindernodesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsBindersTemplatesBindernodesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Template Node Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsBindersTemplatesBindernodesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/binders/templates/bindernodes"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Template Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsBindersTemplatesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsBindersTemplatesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Template Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsBindersTemplatesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/binders/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Annotation Placemark Type Metadata
    #'
    #' @param placemark_type The name of the placemark type. For example, sticky__sys.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsAnnotationsPlacemarksTypesPlacemarkTypeGet = function(placemark_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsAnnotationsPlacemarksTypesPlacemarkTypeGetWithHttpInfo(placemark_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Annotation Placemark Type Metadata
    #'
    #' @param placemark_type The name of the placemark type. For example, sticky__sys.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsAnnotationsPlacemarksTypesPlacemarkTypeGetWithHttpInfo = function(placemark_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`placemark_type`)) {
        stop("Missing required parameter `placemark_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/annotations/placemarks/types/{placemark_type}"
      if (!missing(`placemark_type`)) {
        local_var_url_path <- gsub("\\{placemark_type\\}", URLencode(as.character(`placemark_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Annotation Reference Type Metadata
    #'
    #' @param reference_type 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsAnnotationsReferencesTypesReferenceTypeGet = function(reference_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsAnnotationsReferencesTypesReferenceTypeGetWithHttpInfo(reference_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Annotation Reference Type Metadata
    #'
    #' @param reference_type 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsAnnotationsReferencesTypesReferenceTypeGetWithHttpInfo = function(reference_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`reference_type`)) {
        stop("Missing required parameter `reference_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/annotations/references/types/{reference_type}"
      if (!missing(`reference_type`)) {
        local_var_url_path <- gsub("\\{reference_type\\}", URLencode(as.character(`reference_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Annotation Type Metadata
    #'
    #' @param annotation_type The name of the annotation type. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in Medical and PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys`
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsAnnotationsTypesAnnotationTypeGet = function(annotation_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsAnnotationsTypesAnnotationTypeGetWithHttpInfo(annotation_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Annotation Type Metadata
    #'
    #' @param annotation_type The name of the annotation type. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in Medical and PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys`
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsAnnotationsTypesAnnotationTypeGetWithHttpInfo = function(annotation_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`annotation_type`)) {
        stop("Missing required parameter `annotation_type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/annotations/types/{annotation_type}"
      if (!missing(`annotation_type`)) {
        local_var_url_path <- gsub("\\{annotation_type\\}", URLencode(as.character(`annotation_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Event SubType Metadata
    #'
    #' @param event_type The event type. For example, distribution__v.
    #' @param event_subtype The event subtype. For example, approved_email__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsEventsEventTypeTypesEventSubtypeGet = function(event_type, event_subtype, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsEventsEventTypeTypesEventSubtypeGetWithHttpInfo(event_type, event_subtype, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Event SubType Metadata
    #'
    #' @param event_type The event type. For example, distribution__v.
    #' @param event_subtype The event subtype. For example, approved_email__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsEventsEventTypeTypesEventSubtypeGetWithHttpInfo = function(event_type, event_subtype, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`event_type`)) {
        stop("Missing required parameter `event_type`.")
      }

      if (missing(`event_subtype`)) {
        stop("Missing required parameter `event_subtype`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/events/{event_type}/types/{event_subtype}"
      if (!missing(`event_type`)) {
        local_var_url_path <- gsub("\\{event_type\\}", URLencode(as.character(`event_type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`event_subtype`)) {
        local_var_url_path <- gsub("\\{event_subtype\\}", URLencode(as.character(`event_subtype`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Event Types and Subtypes
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsEventsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsEventsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Event Types and Subtypes
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsEventsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/events"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Lock Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsLockGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsLockGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Lock Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsLockGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/lock"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Common Document Fields
    #'
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsPropertiesFindCommonPost = function(content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsPropertiesFindCommonPostWithHttpInfo(content_type, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Common Document Fields
    #'
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsPropertiesFindCommonPostWithHttpInfo = function(content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/properties/find_common"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Document Fields
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsPropertiesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsPropertiesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Document Fields
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsPropertiesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/properties"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Template Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTemplatesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTemplatesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Template Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTemplatesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Document Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTypesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTypesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Document Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTypesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/types"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Type
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTypesTypeGet = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTypesTypeGetWithHttpInfo(type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Type
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTypesTypeGetWithHttpInfo = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/types/{type}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Type Relationships
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTypesTypeRelationshipsGet = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTypesTypeRelationshipsGetWithHttpInfo(type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Type Relationships
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTypesTypeRelationshipsGetWithHttpInfo = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/types/{type}/relationships"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Classification
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param subtype The document subtype. See Retrieve Document Types.
    #' @param classification The document classification. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTypesTypeSubtypesSubtypeClassificationsClassificationGet = function(type, subtype, classification, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTypesTypeSubtypesSubtypeClassificationsClassificationGetWithHttpInfo(type, subtype, classification, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Classification
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param subtype The document subtype. See Retrieve Document Types.
    #' @param classification The document classification. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTypesTypeSubtypesSubtypeClassificationsClassificationGetWithHttpInfo = function(type, subtype, classification, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      if (missing(`subtype`)) {
        stop("Missing required parameter `subtype`.")
      }

      if (missing(`classification`)) {
        stop("Missing required parameter `classification`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/types/{type}/subtypes/{subtype}/classifications/{classification}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`subtype`)) {
        local_var_url_path <- gsub("\\{subtype\\}", URLencode(as.character(`subtype`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`classification`)) {
        local_var_url_path <- gsub("\\{classification\\}", URLencode(as.character(`classification`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Subtype
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param subtype The document subtype. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsDocumentsTypesTypeSubtypesSubtypeGet = function(type, subtype, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsDocumentsTypesTypeSubtypesSubtypeGetWithHttpInfo(type, subtype, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Subtype
    #'
    #' @param type The document type. See Retrieve Document Types.
    #' @param subtype The document subtype. See Retrieve Document Types.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsDocumentsTypesTypeSubtypesSubtypeGetWithHttpInfo = function(type, subtype, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      if (missing(`subtype`)) {
        stop("Missing required parameter `subtype`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/documents/types/{type}/subtypes/{subtype}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`subtype`)) {
        local_var_url_path <- gsub("\\{subtype\\}", URLencode(as.character(`subtype`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Group Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsGroupsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsGroupsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Group Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsGroupsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/groups"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Security Policy Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsSecuritypoliciesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsSecuritypoliciesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Security Policy Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsSecuritypoliciesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/securitypolicies"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve User Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataObjectsUsersGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataObjectsUsersGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve User Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataObjectsUsersGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/objects/users"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Archived Document Signature Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataQueryArchivedDocumentsRelationshipsDocumentSignatureSysrGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataQueryArchivedDocumentsRelationshipsDocumentSignatureSysrGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Archived Document Signature Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataQueryArchivedDocumentsRelationshipsDocumentSignatureSysrGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/query/archived_documents/relationships/document_signature__sysr"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Signature Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataQueryDocumentsRelationshipsDocumentSignatureSysrGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataQueryDocumentsRelationshipsDocumentSignatureSysrGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Signature Metadata
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataQueryDocumentsRelationshipsDocumentSignatureSysrGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/query/documents/relationships/document_signature__sysr"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Collection
    #'
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsGet = function(loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsGetWithHttpInfo(loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Collection
    #'
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsGetWithHttpInfo = function(loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/metadata/vobjects"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Cancel Raw Object Deployment
    #'
    #' @param object_name 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsObjectNameActionsCanceldeploymentPost = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsObjectNameActionsCanceldeploymentPostWithHttpInfo(object_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancel Raw Object Deployment
    #'
    #' @param object_name 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsObjectNameActionsCanceldeploymentPostWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/vobjects/{object_name}/actions/canceldeployment"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Field Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_field_name The object field name value (id, name__v, external_id__v, etc.).
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsObjectNameFieldsObjectFieldNameGet = function(object_name, object_field_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsObjectNameFieldsObjectFieldNameGetWithHttpInfo(object_name, object_field_name, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Field Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_field_name The object field name value (id, name__v, external_id__v, etc.).
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsObjectNameFieldsObjectFieldNameGetWithHttpInfo = function(object_name, object_field_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_field_name`)) {
        stop("Missing required parameter `object_field_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/metadata/vobjects/{object_name}/fields/{object_field_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_field_name`)) {
        local_var_url_path <- gsub("\\{object_field_name\\}", URLencode(as.character(`object_field_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsObjectNameGet = function(object_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsObjectNameGetWithHttpInfo(object_name, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param loc (optional) To retrieve localized (translated) strings, include the parameter loc=true. See the next request below for details.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsObjectNameGetWithHttpInfo = function(object_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/metadata/vobjects/{object_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Page Layouts
    #'
    #' @param object_name 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsObjectNamePageLayoutsGet = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsObjectNamePageLayoutsGetWithHttpInfo(object_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Page Layouts
    #'
    #' @param object_name 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsObjectNamePageLayoutsGetWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/vobjects/{object_name}/page_layouts"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Page Layout Metadata
    #'
    #' @param object_name The name of the object from which to retrieve page layout metadata.
    #' @param layout_name The name of the page layout from which to retrieve metadata.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    MetadataVobjectsObjectNamePageLayoutsLayoutNameGet = function(object_name, layout_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$MetadataVobjectsObjectNamePageLayoutsLayoutNameGetWithHttpInfo(object_name, layout_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Page Layout Metadata
    #'
    #' @param object_name The name of the object from which to retrieve page layout metadata.
    #' @param layout_name The name of the page layout from which to retrieve metadata.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    MetadataVobjectsObjectNamePageLayoutsLayoutNameGetWithHttpInfo = function(object_name, layout_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`layout_name`)) {
        stop("Missing required parameter `layout_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/metadata/vobjects/{object_name}/page_layouts/{layout_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`layout_name`)) {
        local_var_url_path <- gsub("\\{layout_name\\}", URLencode(as.character(`layout_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Email Notification Histories
    #'
    #' @param start_date (optional) Specify a start date to retrieve notification history. This date cannot be more than 2 years ago. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the start of the day. If start_date is omitted entirely, defaults to the start of the previous day. If you’ve specified a start_date, you must also specify an end_date.
    #' @param end_date (optional) Specify an end date to retrieve notification history. This date cannot be more than 30 days away from the specified start_date. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the time of the API request. If you’ve specified an end_date, you must also specify a start_date.
    #' @param all_dates (optional) Set to true to request notification history for all dates. This is the same as requesting a full CSV export from the Vault UI. When requesting a full notification history, you must leave start_date and end_date blank and set format_result to csv. You can request an export of notification history for all_dates once every 24 hours.
    #' @param format_result (optional) To request a downloadable CSV file of your notification history, set this parameter to csv. The response contains a jobId to retrieve the job status, which provides a link to download the CSV file. If omitted, the API returns a JSON response with notification history and does not start a job. If all_dates is true, this parameter must be csv.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. If omitted, defaults to 0.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    NotificationsHistoriesGet = function(start_date = NULL, end_date = NULL, all_dates = NULL, format_result = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$NotificationsHistoriesGetWithHttpInfo(start_date, end_date, all_dates, format_result, limit, offset, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Email Notification Histories
    #'
    #' @param start_date (optional) Specify a start date to retrieve notification history. This date cannot be more than 2 years ago. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the start of the day. If start_date is omitted entirely, defaults to the start of the previous day. If you’ve specified a start_date, you must also specify an end_date.
    #' @param end_date (optional) Specify an end date to retrieve notification history. This date cannot be more than 30 days away from the specified start_date. Dates must be in YYYY-MM-DD or YYYY-MM-DDTHH:mm:ssZ format. If time is omitted (THH:mm:ssZ), defaults to the time of the API request. If you’ve specified an end_date, you must also specify a start_date.
    #' @param all_dates (optional) Set to true to request notification history for all dates. This is the same as requesting a full CSV export from the Vault UI. When requesting a full notification history, you must leave start_date and end_date blank and set format_result to csv. You can request an export of notification history for all_dates once every 24 hours.
    #' @param format_result (optional) To request a downloadable CSV file of your notification history, set this parameter to csv. The response contains a jobId to retrieve the job status, which provides a link to download the CSV file. If omitted, the API returns a JSON response with notification history and does not start a job. If all_dates is true, this parameter must be csv.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. If omitted, defaults to 0.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    NotificationsHistoriesGetWithHttpInfo = function(start_date = NULL, end_date = NULL, all_dates = NULL, format_result = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE










      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["all_dates"]] <- `all_dates`

      query_params[["format_result"]] <- `format_result`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/notifications/histories"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Bulk Workflow Action Details
    #'
    #' @param action 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsActionGet = function(action, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsActionGetWithHttpInfo(action, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Bulk Workflow Action Details
    #'
    #' @param action 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsActionGetWithHttpInfo = function(action, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`action`)) {
        stop("Missing required parameter `action`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/{action}"
      if (!missing(`action`)) {
        local_var_url_path <- gsub("\\{action\\}", URLencode(as.character(`action`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Workflow Actions on Multiple Workflows
    #'
    #' @param action 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsActionPost = function(action, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsActionPostWithHttpInfo(action, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Workflow Actions on Multiple Workflows
    #'
    #' @param action 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsActionPostWithHttpInfo = function(action, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`action`)) {
        stop("Missing required parameter `action`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/{action}"
      if (!missing(`action`)) {
        local_var_url_path <- gsub("\\{action\\}", URLencode(as.character(`action`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Cancel Workflow Tasks
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsCanceltasksPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsCanceltasksPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancel Workflow Tasks
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsCanceltasksPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/canceltasks"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Cancel Workflows
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsCancelworkflowsPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsCancelworkflowsPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancel Workflows
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsCancelworkflowsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/cancelworkflows"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Bulk Workflow Actions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Bulk Workflow Actions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reassign Workflow Tasks
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsReassigntasksPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsReassigntasksPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reassign Workflow Tasks
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsReassigntasksPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/reassigntasks"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Replace Workflow Owner
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectWorkflowActionsReplaceworkflowownerPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectWorkflowActionsReplaceworkflowownerPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Replace Workflow Owner
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectWorkflowActionsReplaceworkflowownerPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/object/workflow/actions/replaceworkflowowner"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Export Results
    #'
    #' @param job_id The id value of the requested export job. This is returned with the export binder requests above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersActionsExportJobIdResultsGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersActionsExportJobIdResultsGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Export Results
    #'
    #' @param job_id The id value of the requested export job. This is returned with the export binder requests above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersActionsExportJobIdResultsGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/actions/export/{job_id}/results"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Binder (Latest Version)
    #'
    #' @param binder_id The binder id field value.
    #' @param source (optional) to include source content or not
    #' @param renditiontype (optional) to include viewable renditions
    #' @param docversion (optional) to include all major versions
    #' @param attachments (optional) to include all versions of attachments
    #' @param export (optional) configurable filename metadata
    #' @param docfield (optional) to exclude document metadata csv
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdActionsExportPost = function(binder_id, source = NULL, renditiontype = NULL, docversion = NULL, attachments = NULL, export = NULL, docfield = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdActionsExportPostWithHttpInfo(binder_id, source, renditiontype, docversion, attachments, export, docfield, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Binder (Latest Version)
    #'
    #' @param binder_id The binder id field value.
    #' @param source (optional) to include source content or not
    #' @param renditiontype (optional) to include viewable renditions
    #' @param docversion (optional) to include all major versions
    #' @param attachments (optional) to include all versions of attachments
    #' @param export (optional) configurable filename metadata
    #' @param docfield (optional) to exclude document metadata csv
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdActionsExportPostWithHttpInfo = function(binder_id, source = NULL, renditiontype = NULL, docversion = NULL, attachments = NULL, export = NULL, docfield = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["source"]] <- `source`

      query_params[["renditiontype"]] <- `renditiontype`

      query_params[["docversion"]] <- `docversion`

      query_params[["attachments"]] <- `attachments`

      query_params[["export"]] <- `export`

      query_params[["docfield"]] <- `docfield`

      local_var_url_path <- "/objects/binders/{binder_id}/actions/export"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Refresh Binder Auto-Filing
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdActionsPost = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdActionsPostWithHttpInfo(binder_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Refresh Binder Auto-Filing
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdActionsPostWithHttpInfo = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/actions"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdBindingRulePut = function(binder_id, content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdBindingRulePutWithHttpInfo(binder_id, content_type, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdBindingRulePutWithHttpInfo = function(binder_id, content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/binding_rule"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdDelete = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdDeleteWithHttpInfo(binder_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdDeleteWithHttpInfo = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder Document Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdDocumentsNodeIdBindingRulePut = function(binder_id, node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdDocumentsNodeIdBindingRulePutWithHttpInfo(binder_id, node_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder Document Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdDocumentsNodeIdBindingRulePutWithHttpInfo = function(binder_id, node_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`node_id`)) {
        stop("Missing required parameter `node_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/documents/{node_id}/binding_rule"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node_id`)) {
        local_var_url_path <- gsub("\\{node_id\\}", URLencode(as.character(`node_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add Document to Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdDocumentsPost = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdDocumentsPostWithHttpInfo(binder_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add Document to Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdDocumentsPostWithHttpInfo = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/documents"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Document from Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdDocumentsSectionIdDelete = function(binder_id, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdDocumentsSectionIdDeleteWithHttpInfo(binder_id, section_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Document from Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdDocumentsSectionIdDeleteWithHttpInfo = function(binder_id, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/documents/{section_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Move Document in Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdDocumentsSectionIdPut = function(binder_id, section_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdDocumentsSectionIdPutWithHttpInfo(binder_id, section_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Move Document in Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdDocumentsSectionIdPutWithHttpInfo = function(binder_id, section_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/documents/{section_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param depth (optional) To retrieve all information in all levels of the binder, set this to all. By default, only one level is returned.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdGet = function(binder_id, depth = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdGetWithHttpInfo(binder_id, depth, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param depth (optional) To retrieve all information in all levels of the binder, set this to all. By default, only one level is returned.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdGetWithHttpInfo = function(binder_id, depth = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["depth"]] <- `depth`

      local_var_url_path <- "/objects/binders/{binder_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdPost = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdPostWithHttpInfo(binder_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdPostWithHttpInfo = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdPut = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdPutWithHttpInfo(binder_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdPutWithHttpInfo = function(binder_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on a Single Binder
    #'
    #' @param binder_id The id value of the binder from which to remove roles.
    #' @param role_name_and_user_or_group The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
    #' @param id The id value of the user or group to remove from the role.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdRolesRoleNameAndUserOrGroupIdDelete = function(binder_id, role_name_and_user_or_group, id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdRolesRoleNameAndUserOrGroupIdDeleteWithHttpInfo(binder_id, role_name_and_user_or_group, id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on a Single Binder
    #'
    #' @param binder_id The id value of the binder from which to remove roles.
    #' @param role_name_and_user_or_group The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
    #' @param id The id value of the user or group to remove from the role.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdRolesRoleNameAndUserOrGroupIdDeleteWithHttpInfo = function(binder_id, role_name_and_user_or_group, id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`role_name_and_user_or_group`)) {
        stop("Missing required parameter `role_name_and_user_or_group`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/roles/{role_name_and_user_or_group}/{id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`role_name_and_user_or_group`)) {
        local_var_url_path <- gsub("\\{role_name_and_user_or_group\\}", URLencode(as.character(`role_name_and_user_or_group`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder Section Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id field value.
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdSectionsNodeIdBindingRulePut = function(binder_id, node_id, content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdSectionsNodeIdBindingRulePutWithHttpInfo(binder_id, node_id, content_type, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder Section Binding Rule
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id field value.
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdSectionsNodeIdBindingRulePutWithHttpInfo = function(binder_id, node_id, content_type = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`node_id`)) {
        stop("Missing required parameter `node_id`.")
      }







      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/sections/{node_id}/binding_rule"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node_id`)) {
        local_var_url_path <- gsub("\\{node_id\\}", URLencode(as.character(`node_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id of the section.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdSectionsNodeIdPut = function(binder_id, node_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdSectionsNodeIdPutWithHttpInfo(binder_id, node_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param node_id The binder node id of the section.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdSectionsNodeIdPutWithHttpInfo = function(binder_id, node_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`node_id`)) {
        stop("Missing required parameter `node_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/sections/{node_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`node_id`)) {
        local_var_url_path <- gsub("\\{node_id\\}", URLencode(as.character(`node_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdSectionsPost = function(binder_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdSectionsPostWithHttpInfo(binder_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdSectionsPostWithHttpInfo = function(binder_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/sections"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdSectionsSectionIdDelete = function(binder_id, section_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdSectionsSectionIdDeleteWithHttpInfo(binder_id, section_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Binder Section
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdSectionsSectionIdDeleteWithHttpInfo = function(binder_id, section_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/sections/{section_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Sections
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdSectionsSectionIdGet = function(binder_id, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdSectionsSectionIdGetWithHttpInfo(binder_id, section_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Sections
    #'
    #' @param binder_id The binder id field value.
    #' @param section_id The binder node id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdSectionsSectionIdGetWithHttpInfo = function(binder_id, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/sections/{section_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Binder Versions
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsGet = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsGetWithHttpInfo(binder_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Binder Versions
    #'
    #' @param binder_id The binder id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsGetWithHttpInfo = function(binder_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Binder (Specific Version)
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param source (optional) to include source content or not
    #' @param renditiontype (optional) to include viewable renditions
    #' @param docversion (optional) to include all major versions
    #' @param attachments (optional) to include all versions of attachments
    #' @param export (optional) configurable filename metadata
    #' @param docfield (optional) to exclude document metadata csv
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionActionsExportPost = function(binder_id, major_version, minor_version, source = NULL, renditiontype = NULL, docversion = NULL, attachments = NULL, export = NULL, docfield = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionActionsExportPostWithHttpInfo(binder_id, major_version, minor_version, source, renditiontype, docversion, attachments, export, docfield, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Binder (Specific Version)
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param source (optional) to include source content or not
    #' @param renditiontype (optional) to include viewable renditions
    #' @param docversion (optional) to include all major versions
    #' @param attachments (optional) to include all versions of attachments
    #' @param export (optional) configurable filename metadata
    #' @param docfield (optional) to exclude document metadata csv
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionActionsExportPostWithHttpInfo = function(binder_id, major_version, minor_version, source = NULL, renditiontype = NULL, docversion = NULL, attachments = NULL, export = NULL, docfield = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }













      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["source"]] <- `source`

      query_params[["renditiontype"]] <- `renditiontype`

      query_params[["docversion"]] <- `docversion`

      query_params[["attachments"]] <- `attachments`

      query_params[["export"]] <- `export`

      query_params[["docfield"]] <- `docfield`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}/actions/export"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionDelete = function(binder_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionDeleteWithHttpInfo(binder_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionDeleteWithHttpInfo = function(binder_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionGet = function(binder_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionGetWithHttpInfo(binder_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionGetWithHttpInfo = function(binder_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionPut = function(binder_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionPutWithHttpInfo(binder_id, major_version, minor_version, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder Version
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionPutWithHttpInfo = function(binder_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsPost = function(binder_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsPostWithHttpInfo(binder_id, major_version, minor_version, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsPostWithHttpInfo = function(binder_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}/relationships"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param relationship_id The binder relationship id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete = function(binder_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDeleteWithHttpInfo(binder_id, major_version, minor_version, relationship_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param relationship_id The binder relationship id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDeleteWithHttpInfo = function(binder_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`relationship_id`)) {
        stop("Missing required parameter `relationship_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}/relationships/{relationship_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`relationship_id`)) {
        local_var_url_path <- gsub("\\{relationship_id\\}", URLencode(as.character(`relationship_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param relationship_id The binder relationship id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet = function(binder_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGetWithHttpInfo(binder_id, major_version, minor_version, relationship_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Relationship
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param relationship_id The binder relationship id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGetWithHttpInfo = function(binder_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`relationship_id`)) {
        stop("Missing required parameter `relationship_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}/relationships/{relationship_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`relationship_id`)) {
        local_var_url_path <- gsub("\\{relationship_id\\}", URLencode(as.character(`relationship_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Version Section
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param section_id Retrieve all sections (documents and subsections) in a binder’s sub-level node. If not included, all sections from the binder’s top-level root node will be returned.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionSectionsSectionIdGet = function(binder_id, major_version, minor_version, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersBinderIdVersionsMajorVersionMinorVersionSectionsSectionIdGetWithHttpInfo(binder_id, major_version, minor_version, section_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Version Section
    #'
    #' @param binder_id The binder id field value.
    #' @param major_version The binder major_version_number__v field value.
    #' @param minor_version The binder minor_version_number__v field value.
    #' @param section_id Retrieve all sections (documents and subsections) in a binder’s sub-level node. If not included, all sections from the binder’s top-level root node will be returned.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersBinderIdVersionsMajorVersionMinorVersionSectionsSectionIdGetWithHttpInfo = function(binder_id, major_version, minor_version, section_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`binder_id`)) {
        stop("Missing required parameter `binder_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`section_id`)) {
        stop("Missing required parameter `section_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{binder_id}/versions/{major_version}/{minor_version}/sections/{section_id}"
      if (!missing(`binder_id`)) {
        local_var_url_path <- gsub("\\{binder_id\\}", URLencode(as.character(`binder_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`section_id`)) {
        local_var_url_path <- gsub("\\{section_id\\}", URLencode(as.character(`section_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Binder Roles
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdRolesGet = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdRolesGetWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Binder Roles
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdRolesGetWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/roles"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on a Single Binder
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdRolesPost = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdRolesPostWithHttpInfo(id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on a Single Binder
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdRolesPostWithHttpInfo = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/roles"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Role
    #'
    #' @param id The binder `id`.
    #' @param role_name The name of the role to retrieve. For example, `owner__v`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdRolesRoleNameGet = function(id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdRolesRoleNameGetWithHttpInfo(id, role_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Role
    #'
    #' @param id The binder `id`.
    #' @param role_name The name of the role to retrieve. For example, `owner__v`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdRolesRoleNameGetWithHttpInfo = function(id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`role_name`)) {
        stop("Missing required parameter `role_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/roles/{role_name}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`role_name`)) {
        local_var_url_path <- gsub("\\{role_name\\}", URLencode(as.character(`role_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder User Actions
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsGet = function(id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsGetWithHttpInfo(id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder User Actions
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsGetWithHttpInfo = function(id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/versions/{major_version}/{minor_version}/lifecycle_actions"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Entry Criteria
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param name__v The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet = function(id, major_version, minor_version, name__v, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGetWithHttpInfo(id, major_version, minor_version, name__v, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Entry Criteria
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param name__v The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGetWithHttpInfo = function(id, major_version, minor_version, name__v, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`name__v`)) {
        stop("Missing required parameter `name__v`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/versions/{major_version}/{minor_version}/lifecycle_actions/{name__v}/entry_requirements"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`name__v`)) {
        local_var_url_path <- gsub("\\{name__v\\}", URLencode(as.character(`name__v`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Binder User Action
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param name__v The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut = function(id, major_version, minor_version, name__v, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPutWithHttpInfo(id, major_version, minor_version, name__v, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Binder User Action
    #'
    #' @param id The binder id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the binder.
    #' @param minor_version The minor version number of the binder.
    #' @param name__v The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPutWithHttpInfo = function(id, major_version, minor_version, name__v, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`name__v`)) {
        stop("Missing required parameter `name__v`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/{id}/versions/{major_version}/{minor_version}/lifecycle_actions/{name__v}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`name__v`)) {
        local_var_url_path <- gsub("\\{name__v\\}", URLencode(as.character(`name__v`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve User Actions on Multiple Binders
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersLifecycleActionsPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersLifecycleActionsPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve User Actions on Multiple Binders
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersLifecycleActionsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/lifecycle_actions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Bulk Binder User Actions
    #'
    #' @param user_action_name The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Binders endpoint.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersLifecycleActionsUserActionNamePut = function(user_action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersLifecycleActionsUserActionNamePutWithHttpInfo(user_action_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Bulk Binder User Actions
    #'
    #' @param user_action_name The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Binders endpoint.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersLifecycleActionsUserActionNamePutWithHttpInfo = function(user_action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_action_name`)) {
        stop("Missing required parameter `user_action_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/lifecycle_actions/{user_action_name}"
      if (!missing(`user_action_name`)) {
        local_var_url_path <- gsub("\\{user_action_name\\}", URLencode(as.character(`user_action_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder
    #'
    #' @param async (optional) When creating a binder, the binder metadata is indexed synchronously by default. To process the indexing asynchronously, include a query parameter async set to true (objects/binders?async=true). This helps speed up the response time from Vault when processing large amounts of data. 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersPost = function(async = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersPostWithHttpInfo(async, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder
    #'
    #' @param async (optional) When creating a binder, the binder metadata is indexed synchronously by default. To process the indexing asynchronously, include a query parameter async set to true (objects/binders?async=true). This helps speed up the response time from Vault when processing large amounts of data. 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersPostWithHttpInfo = function(async = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["async"]] <- `async`

      local_var_url_path <- "/objects/binders"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Template Collection
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Template Collection
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder Template
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder Template
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Binder Template
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesPut = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesPutWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Binder Template
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesPutWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Template Node Attributes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesTemplateNameBindernodesGet = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesTemplateNameBindernodesGetWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Template Node Attributes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesTemplateNameBindernodesGetWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates/{template_name}/bindernodes"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Binder Template Node
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesTemplateNameBindernodesPost = function(template_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesTemplateNameBindernodesPostWithHttpInfo(template_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Binder Template Node
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesTemplateNameBindernodesPostWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates/{template_name}/bindernodes"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Replace Binder Template Nodes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesTemplateNameBindernodesPut = function(template_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesTemplateNameBindernodesPutWithHttpInfo(template_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Replace Binder Template Nodes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesTemplateNameBindernodesPutWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates/{template_name}/bindernodes"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Binder Template
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesTemplateNameDelete = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesTemplateNameDeleteWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Binder Template
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesTemplateNameDeleteWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates/{template_name}"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Binder Template Attributes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsBindersTemplatesTemplateNameGet = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsBindersTemplatesTemplateNameGetWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Binder Template Attributes
    #'
    #' @param template_name The binder template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsBindersTemplatesTemplateNameGetWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/binders/templates/{template_name}"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Deleted Document IDs
    #'
    #' @param start_date (optional) Specify a date (no more than 30 days past) after which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z
    #' @param end_date (optional) Specify a date (no more than 30 days past) before which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDeletionsDocumentsGet = function(start_date = NULL, end_date = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDeletionsDocumentsGetWithHttpInfo(start_date, end_date, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Deleted Document IDs
    #'
    #' @param start_date (optional) Specify a date (no more than 30 days past) after which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z
    #' @param end_date (optional) Specify a date (no more than 30 days past) before which Vault will look for deleted documents. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2016 would use 2016-01-15T07:00:00Z 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDeletionsDocumentsGetWithHttpInfo = function(start_date = NULL, end_date = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      local_var_url_path <- "/objects/deletions/documents"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Deleted Object Record ID
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDeletionsVobjectsObjectNameGet = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDeletionsVobjectsObjectNameGetWithHttpInfo(object_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Deleted Object Record ID
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDeletionsVobjectsObjectNameGetWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/deletions/vobjects/{object_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Document Workflows
    #'
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsActionsGet = function(loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsActionsGetWithHttpInfo(loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Document Workflows
    #'
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsActionsGetWithHttpInfo = function(loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/documents/actions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Controlled Copy Job Results
    #'
    #' @param lifecycle_and_state_and_action The `name__v` values for the lifecycle, state, and action in the format `{lifecycle_name}.{state_name}.{action_name}`. To get this value, [Retrieve the Job Status](https://developer.veevavault.com/api/24.3#RetrieveJobStatus) and find the `href` under the `artifacts` link.
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsActionsLifecycleAndStateAndActionJobIdResultsGet = function(lifecycle_and_state_and_action, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsActionsLifecycleAndStateAndActionJobIdResultsGetWithHttpInfo(lifecycle_and_state_and_action, job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Controlled Copy Job Results
    #'
    #' @param lifecycle_and_state_and_action The `name__v` values for the lifecycle, state, and action in the format `{lifecycle_name}.{state_name}.{action_name}`. To get this value, [Retrieve the Job Status](https://developer.veevavault.com/api/24.3#RetrieveJobStatus) and find the `href` under the `artifacts` link.
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsActionsLifecycleAndStateAndActionJobIdResultsGetWithHttpInfo = function(lifecycle_and_state_and_action, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`lifecycle_and_state_and_action`)) {
        stop("Missing required parameter `lifecycle_and_state_and_action`.")
      }

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/actions/{lifecycle_and_state_and_action}/{job_id}/results"
      if (!missing(`lifecycle_and_state_and_action`)) {
        local_var_url_path <- gsub("\\{lifecycle_and_state_and_action\\}", URLencode(as.character(`lifecycle_and_state_and_action`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Workflow Details
    #'
    #' @param workflow_name The document workflow name value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsActionsWorkflowNameGet = function(workflow_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsActionsWorkflowNameGetWithHttpInfo(workflow_name, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Workflow Details
    #'
    #' @param workflow_name The document workflow name value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by setting loc to true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsActionsWorkflowNameGetWithHttpInfo = function(workflow_name, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_name`)) {
        stop("Missing required parameter `workflow_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/documents/actions/{workflow_name}"
      if (!missing(`workflow_name`)) {
        local_var_url_path <- gsub("\\{workflow_name\\}", URLencode(as.character(`workflow_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Document Workflow
    #'
    #' @param workflow_name The document workflow name value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsActionsWorkflowNamePost = function(workflow_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsActionsWorkflowNamePostWithHttpInfo(workflow_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Document Workflow
    #'
    #' @param workflow_name The document workflow name value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsActionsWorkflowNamePostWithHttpInfo = function(workflow_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_name`)) {
        stop("Missing required parameter `workflow_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/actions/{workflow_name}"
      if (!missing(`workflow_name`)) {
        local_var_url_path <- gsub("\\{workflow_name\\}", URLencode(as.character(`workflow_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Annotations
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAnnotationsBatchDelete = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAnnotationsBatchDeleteWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Annotations
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAnnotationsBatchDeleteWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/annotations/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Annotations
    #'
    #' @param authorization 
    #' @param content_type 
    #' @param accept 
    #' @param x_vault_api_client_id Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAnnotationsBatchPost = function(authorization, content_type, accept, x_vault_api_client_id, ...) {
      local_var_response <- self$ObjectsDocumentsAnnotationsBatchPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Annotations
    #'
    #' @param authorization 
    #' @param content_type 
    #' @param accept 
    #' @param x_vault_api_client_id Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAnnotationsBatchPostWithHttpInfo = function(authorization, content_type, accept, x_vault_api_client_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`authorization`)) {
        stop("Missing required parameter `authorization`.")
      }

      if (missing(`content_type`)) {
        stop("Missing required parameter `content_type`.")
      }

      if (missing(`accept`)) {
        stop("Missing required parameter `accept`.")
      }

      if (missing(`x_vault_api_client_id`)) {
        stop("Missing required parameter `x_vault_api_client_id`.")
      }

      if (nchar(`authorization`) > 0) {
        stop("Invalid length for `authorization` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be smaller than or equal to 0.")
      }
      if (nchar(`authorization`) < 0) {
        stop("Invalid length for `authorization` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be bigger than or equal to 0.")
      }

      if (nchar(`content_type`) > 0) {
        stop("Invalid length for `content_type` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be smaller than or equal to 0.")
      }
      if (nchar(`content_type`) < 0) {
        stop("Invalid length for `content_type` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be bigger than or equal to 0.")
      }

      if (nchar(`accept`) > 0) {
        stop("Invalid length for `accept` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be smaller than or equal to 0.")
      }
      if (nchar(`accept`) < 0) {
        stop("Invalid length for `accept` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be bigger than or equal to 0.")
      }

      if (nchar(`x_vault_api_client_id`) > 0) {
        stop("Invalid length for `x_vault_api_client_id` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be smaller than or equal to 0.")
      }
      if (nchar(`x_vault_api_client_id`) < 0) {
        stop("Invalid length for `x_vault_api_client_id` when calling DefaultApi$ObjectsDocumentsAnnotationsBatchPost, must be bigger than or equal to 0.")
      }

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/annotations/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Annotations
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAnnotationsBatchPut = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAnnotationsBatchPutWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Annotations
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAnnotationsBatchPutWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/annotations/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add Annotation Replies
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAnnotationsRepliesBatchPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAnnotationsRepliesBatchPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add Annotation Replies
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAnnotationsRepliesBatchPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/annotations/replies/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Document Attachments
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAttachmentsBatchDelete = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAttachmentsBatchDeleteWithHttpInfo(accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Document Attachments
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAttachmentsBatchDeleteWithHttpInfo = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/attachments/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Document Attachments
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAttachmentsBatchPost = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAttachmentsBatchPostWithHttpInfo(accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Document Attachments
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAttachmentsBatchPostWithHttpInfo = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/attachments/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Document Attachment Descriptions
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsAttachmentsBatchPut = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsAttachmentsBatchPutWithHttpInfo(accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Document Attachment Descriptions
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsAttachmentsBatchPutWithHttpInfo = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/attachments/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Export Results
    #'
    #' @param job_id The id value of the requested export job. This is returned with the export document requests.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchActionsFileextractJobIdResultsGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchActionsFileextractJobIdResultsGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Export Results
    #'
    #' @param job_id The id value of the requested export job. This is returned with the export document requests.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchActionsFileextractJobIdResultsGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch/actions/fileextract/{job_id}/results"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Documents
    #'
    #' @param source (optional) Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true.
    #' @param renditions (optional) Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false.
    #' @param allversions (optional) Optional: To include all versions or latest version, include a query parameter allversions=true. If omitted, defaults to false.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchActionsFileextractPost = function(source = NULL, renditions = NULL, allversions = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchActionsFileextractPostWithHttpInfo(source, renditions, allversions, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Documents
    #'
    #' @param source (optional) Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true.
    #' @param renditions (optional) Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false.
    #' @param allversions (optional) Optional: To include all versions or latest version, include a query parameter allversions=true. If omitted, defaults to false.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchActionsFileextractPostWithHttpInfo = function(source = NULL, renditions = NULL, allversions = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["source"]] <- `source`

      query_params[["renditions"]] <- `renditions`

      query_params[["allversions"]] <- `allversions`

      local_var_url_path <- "/objects/documents/batch/actions/fileextract"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reclassify Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to manually set the document number and to update documents to any lifecycle state using the status__v field. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message.  You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchActionsReclassifyPut = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchActionsReclassifyPutWithHttpInfo(authorization, content_type, accept, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reclassify Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to manually set the document number and to update documents to any lifecycle state using the status__v field. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message.  You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchActionsReclassifyPutWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch/actions/reclassify"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Document Renditions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchActionsRerenderPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchActionsRerenderPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Document Renditions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchActionsRerenderPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch/actions/rerender"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Documents
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchDelete = function(id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchDeleteWithHttpInfo(id_param, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Documents
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchDeleteWithHttpInfo = function(id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      local_var_url_path <- "/objects/documents/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Undo Collaborative Authoring Checkout
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchLockDelete = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchLockDeleteWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Undo Collaborative Authoring Checkout
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchLockDeleteWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch/lock"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to create documents in any lifecycle state using the status__v field, and to manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchPostWithHttpInfo(authorization, accept, content_type, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to create documents in any lifecycle state using the status__v field, and to manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header.Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsBatchPut = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsBatchPutWithHttpInfo(authorization, accept, content_type, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header.Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsBatchPutWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Anchor IDs
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAnchorsGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAnchorsGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Anchor IDs
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAnchorsGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/anchors"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Document Annotations to PDF
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAnnotationsFileGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAnnotationsFileGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Document Annotations to PDF
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAnnotationsFileGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/annotations/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Import Document Annotations from PDF
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAnnotationsFilePost = function(doc_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAnnotationsFilePostWithHttpInfo(doc_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Import Document Annotations from PDF
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAnnotationsFilePostWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/annotations/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdDelete = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdDeleteWithHttpInfo(doc_id, attachment_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdDeleteWithHttpInfo = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdFileGet = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdFileGetWithHttpInfo(doc_id, attachment_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdFileGetWithHttpInfo = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Attachment Metadata
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdGet = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdGetWithHttpInfo(doc_id, attachment_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Attachment Metadata
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdGetWithHttpInfo = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Document Attachment Description
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdPut = function(doc_id, attachment_id, accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdPutWithHttpInfo(doc_id, attachment_id, accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Document Attachment Description
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdPutWithHttpInfo = function(doc_id, attachment_id, accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionDeleteWithHttpInfo(doc_id, attachment_id, attachment_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionDeleteWithHttpInfo = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo(doc_id, attachment_id, attachment_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/versions/{attachment_version}/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Attachment Version Metadata
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionGet = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo(doc_id, attachment_id, attachment_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Attachment Version Metadata
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo = function(doc_id, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Restore Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param restore (optional) The parameter restore must be set to true. 
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionPost = function(doc_id, attachment_id, attachment_version, restore = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionPostWithHttpInfo(doc_id, attachment_id, attachment_version, restore, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Restore Document Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param restore (optional) The parameter restore must be set to true. 
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsAttachmentVersionPostWithHttpInfo = function(doc_id, attachment_id, attachment_version, restore = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }








      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["restore"]] <- `restore`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Attachment Versions
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsGet = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsGetWithHttpInfo(doc_id, attachment_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Attachment Versions
    #'
    #' @param doc_id The document id field value.
    #' @param attachment_id The attachment id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsAttachmentIdVersionsGetWithHttpInfo = function(doc_id, attachment_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/{attachment_id}/versions"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download All Document Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsFileGet = function(doc_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsFileGetWithHttpInfo(doc_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download All Document Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsFileGetWithHttpInfo = function(doc_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsGet = function(doc_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsGetWithHttpInfo(doc_id, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsGetWithHttpInfo = function(doc_id, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAttachmentsPost = function(doc_id, accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAttachmentsPostWithHttpInfo(doc_id, accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Document Attachment
    #'
    #' @param doc_id The document id field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAttachmentsPostWithHttpInfo = function(doc_id, accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/attachments"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Complete Audit History for a Single Document
    #'
    #' @param doc_id The document ID for which to retrieve audit history.
    #' @param start_date (optional) Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vault’s creation date. 
    #' @param end_date (optional) Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date. 
    #' @param format_result (optional) To request a CSV file of your audit history, use csv. 
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param events (optional) Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of document audit events. The values passed to this parameter are case sensitive. For example, events=WorkflowCompletion,TaskAssignment. If omitted, defaults to all audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdAudittrailGet = function(doc_id, start_date = NULL, end_date = NULL, format_result = NULL, limit = NULL, offset = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdAudittrailGetWithHttpInfo(doc_id, start_date, end_date, format_result, limit, offset, events, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Complete Audit History for a Single Document
    #'
    #' @param doc_id The document ID for which to retrieve audit history.
    #' @param start_date (optional) Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vault’s creation date. 
    #' @param end_date (optional) Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date. 
    #' @param format_result (optional) To request a CSV file of your audit history, use csv. 
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param events (optional) Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of document audit events. The values passed to this parameter are case sensitive. For example, events=WorkflowCompletion,TaskAssignment. If omitted, defaults to all audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdAudittrailGetWithHttpInfo = function(doc_id, start_date = NULL, end_date = NULL, format_result = NULL, limit = NULL, offset = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["format_result"]] <- `format_result`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["events"]] <- `events`

      local_var_url_path <- "/objects/documents/{doc_id}/audittrail"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdDelete = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdDeleteWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdDeleteWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Events
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdEventsGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdEventsGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Events
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdEventsGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/events"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document File
    #'
    #' @param doc_id The document id field value.
    #' @param lock_document (optional) Set to true to Check Out this document before retrieval. If omitted, defaults to false.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdFileGet = function(doc_id, lock_document = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdFileGetWithHttpInfo(doc_id, lock_document, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document File
    #'
    #' @param doc_id The document id field value.
    #' @param lock_document (optional) Set to true to Check Out this document before retrieval. If omitted, defaults to false.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdFileGetWithHttpInfo = function(doc_id, lock_document = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }






      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["lockDocument"]] <- `lock_document`

      local_var_url_path <- "/objects/documents/{doc_id}/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdLockDelete = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdLockDeleteWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdLockDeleteWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/lock"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdLockGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdLockGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdLockGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/lock"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdLockPost = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdLockPostWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Document Lock
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdLockPostWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/lock"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download All Document Version Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdMajorVersionMinorVersionAttachmentsFileGet = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdMajorVersionMinorVersionAttachmentsFileGetWithHttpInfo(doc_id, major_version, minor_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download All Document Version Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdMajorVersionMinorVersionAttachmentsFileGetWithHttpInfo = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/{major_version}/{minor_version}/attachments/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Single Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param suppress_rendition (optional) Set to true to suppress automatic generation of the viewable rendition. If omitted, defaults to false
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdPost = function(doc_id, suppress_rendition = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdPostWithHttpInfo(doc_id, suppress_rendition, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Single Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param suppress_rendition (optional) Set to true to suppress automatic generation of the viewable rendition. If omitted, defaults to false
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdPostWithHttpInfo = function(doc_id, suppress_rendition = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["suppressRendition"]] <- `suppress_rendition`

      local_var_url_path <- "/objects/documents/{doc_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Single Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdPut = function(doc_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdPutWithHttpInfo(doc_id, authorization, content_type, accept, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Single Document
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to change the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdPutWithHttpInfo = function(doc_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Renditions
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRenditionsGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRenditionsGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Renditions
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRenditionsGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/renditions"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRenditionsRenditionTypeDelete = function(doc_id, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRenditionsRenditionTypeDeleteWithHttpInfo(doc_id, rendition_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRenditionsRenditionTypeDeleteWithHttpInfo = function(doc_id, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Rendition File
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param steady_state (optional) Set to true to download a rendition (file) from the latest steady state version (1.0, 2.0, etc.) of a document. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRenditionsRenditionTypeGet = function(doc_id, rendition_type, steady_state = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRenditionsRenditionTypeGetWithHttpInfo(doc_id, rendition_type, steady_state, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Rendition File
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param steady_state (optional) Set to true to download a rendition (file) from the latest steady state version (1.0, 2.0, etc.) of a document. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRenditionsRenditionTypeGetWithHttpInfo = function(doc_id, rendition_type, steady_state = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["steadyState"]] <- `steady_state`

      local_var_url_path <- "/objects/documents/{doc_id}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add Single Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRenditionsRenditionTypePost = function(doc_id, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRenditionsRenditionTypePostWithHttpInfo(doc_id, rendition_type, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add Single Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRenditionsRenditionTypePostWithHttpInfo = function(doc_id, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Replace Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRenditionsRenditionTypePut = function(doc_id, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRenditionsRenditionTypePutWithHttpInfo(doc_id, rendition_type, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Replace Document Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRenditionsRenditionTypePutWithHttpInfo = function(doc_id, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on a Single Document
    #'
    #' @param doc_id The id value of the document from which to remove roles.
    #' @param role_name_and_user_or_group The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
    #' @param id The id value of the user or group to remove from the role.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdRolesRoleNameAndUserOrGroupIdDelete = function(doc_id, role_name_and_user_or_group, id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdRolesRoleNameAndUserOrGroupIdDeleteWithHttpInfo(doc_id, role_name_and_user_or_group, id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on a Single Document
    #'
    #' @param doc_id The id value of the document from which to remove roles.
    #' @param role_name_and_user_or_group The name of the role from which to remove the user or group followed by either `user` or `group`. The format is `{role_name}.{user_or_group}`. For example, `consumer__v.user`.
    #' @param id The id value of the user or group to remove from the role.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdRolesRoleNameAndUserOrGroupIdDeleteWithHttpInfo = function(doc_id, role_name_and_user_or_group, id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`role_name_and_user_or_group`)) {
        stop("Missing required parameter `role_name_and_user_or_group`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/roles/{role_name_and_user_or_group}/{id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`role_name_and_user_or_group`)) {
        local_var_url_path <- gsub("\\{role_name_and_user_or_group\\}", URLencode(as.character(`role_name_and_user_or_group`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Version Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionMajorVersionMinorVersionAttachmentsGet = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionMajorVersionMinorVersionAttachmentsGetWithHttpInfo(doc_id, major_version, minor_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Version Attachments
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionMajorVersionMinorVersionAttachmentsGetWithHttpInfo = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/version/{major_version}/{minor_version}/attachments"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Versions
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsGet = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsGetWithHttpInfo(doc_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Versions
    #'
    #' @param doc_id The document id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsGetWithHttpInfo = function(doc_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Read Annotations by ID
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param annotation_id The annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdGet = function(doc_id, major_version, minor_version, annotation_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdGetWithHttpInfo(doc_id, major_version, minor_version, annotation_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Read Annotations by ID
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param annotation_id The annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdGetWithHttpInfo = function(doc_id, major_version, minor_version, annotation_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`annotation_id`)) {
        stop("Missing required parameter `annotation_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/annotations/{annotation_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`annotation_id`)) {
        local_var_url_path <- gsub("\\{annotation_id\\}", URLencode(as.character(`annotation_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Read Replies of Parent Annotation
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param annotation_id The parent annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdRepliesGet = function(doc_id, major_version, minor_version, annotation_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdRepliesGetWithHttpInfo(doc_id, major_version, minor_version, annotation_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Read Replies of Parent Annotation
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param annotation_id The parent annotation ID, which can be retrieved with Read Annotations by Document Version and Type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsAnnotationIdRepliesGetWithHttpInfo = function(doc_id, major_version, minor_version, annotation_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`annotation_id`)) {
        stop("Missing required parameter `annotation_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/annotations/{annotation_id}/replies"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`annotation_id`)) {
        local_var_url_path <- gsub("\\{annotation_id\\}", URLencode(as.character(`annotation_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Document Version Annotations to PDF
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFileGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFileGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Document Version Annotations to PDF
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFileGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/annotations/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Import Document Version Annotations from PDF
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFilePost = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFilePostWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Import Document Version Annotations from PDF
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsFilePostWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/annotations/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Read Annotations by Document Version and Type
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param limit (optional) Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 500. If omitted, defaults to 500. Values greater than 500 are ignored.
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to offset=201.
    #' @param annotation_types (optional) The type(s) of annotations to retrieve. For example, note__sys,anchor__sys. If omitted, Vault returns all annotations. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys` 
    #' @param pagination_id (optional) A unique identifier used to load requests with paginated results.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsGet = function(doc_id, major_version, minor_version, limit = NULL, offset = NULL, annotation_types = NULL, pagination_id = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsGetWithHttpInfo(doc_id, major_version, minor_version, limit, offset, annotation_types, pagination_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Read Annotations by Document Version and Type
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param limit (optional) Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 500. If omitted, defaults to 500. Values greater than 500 are ignored.
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to offset=201.
    #' @param annotation_types (optional) The type(s) of annotations to retrieve. For example, note__sys,anchor__sys. If omitted, Vault returns all annotations. Valid annotation types include: `note__sys` `line__sys` `document_link__sys` `permalink_link__sys` `anchor__sys` `reply__sys` `external_link__sys`  The following annotation types are only valid in PromoMats Vaults:  `suggested_link__sys` `approved_link__sys` `auto_link__sys` `keyword_link__sys` 
    #' @param pagination_id (optional) A unique identifier used to load requests with paginated results.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAnnotationsGetWithHttpInfo = function(doc_id, major_version, minor_version, limit = NULL, offset = NULL, annotation_types = NULL, pagination_id = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["annotation_types"]] <- `annotation_types`

      query_params[["pagination_id"]] <- `pagination_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/annotations"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Version Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param attachment_id The id field value of the attachment.
    #' @param attachment_version The version of the attachment.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionFileGet = function(doc_id, major_version, minor_version, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo(doc_id, major_version, minor_version, attachment_id, attachment_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Version Attachment Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param attachment_id The id field value of the attachment.
    #' @param attachment_version The version of the attachment.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo = function(doc_id, major_version, minor_version, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }









      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/attachments/{attachment_id}/versions/{attachment_version}/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Version Attachment Versions
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param attachment_id The id of the document attachment to retrieve.
    #' @param attachment_version Optional: The version of the attachment to retrieve. If omitted, the endpoint retrieves all versions of the specified attachment.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionGet = function(doc_id, major_version, minor_version, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo(doc_id, major_version, minor_version, attachment_id, attachment_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Version Attachment Versions
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param attachment_id The id of the document attachment to retrieve.
    #' @param attachment_version Optional: The version of the attachment to retrieve. If omitted, the endpoint retrieves all versions of the specified attachment.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo = function(doc_id, major_version, minor_version, attachment_id, attachment_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }









      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDelete = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDeleteWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDeleteWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Version Notes as CSV
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDocExportAnnotationsToCsvGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDocExportAnnotationsToCsvGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Version Notes as CSV
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionDocExportAnnotationsToCsvGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/doc-export-annotations-to-csv"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Document Event
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionEventsPost = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionEventsPostWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Document Event
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionEventsPostWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/events"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Video Annotations
    #'
    #' @param doc_id The video document id field value.
    #' @param major_version The video document major_version_number__v field value.
    #' @param minor_version The video document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) This `Accept` header only changes the format of the response in the case of an error. On `SUCCESS`, the HTTP Response Header `Content-Type` is set to `text/plain;charset=UnicodeLittle`.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionExportVideoAnnotationsGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionExportVideoAnnotationsGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Video Annotations
    #'
    #' @param doc_id The video document id field value.
    #' @param major_version The video document major_version_number__v field value.
    #' @param minor_version The video document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) This `Accept` header only changes the format of the response in the case of an error. On `SUCCESS`, the HTTP Response Header `Content-Type` is set to `text/plain;charset=UnicodeLittle`.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionExportVideoAnnotationsGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/export-video-annotations"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Version File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionFileGet = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionFileGetWithHttpInfo(doc_id, major_version, minor_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Version File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionFileGetWithHttpInfo = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/file"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to manually set the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionPut = function(doc_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionPutWithHttpInfo(doc_id, major_version, minor_version, authorization, content_type, accept, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Document Version
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true, Vault allows you to manually set the document number. All other Document Migration Mode overrides available at document creation are ignored, but do not generate an error message. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionPutWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Relationships
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Relationships
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/relationships"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Single Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsPost = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsPostWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Single Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsPostWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/relationships"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param relationship_id The relationship id field value. See Retrieve Document Relationships.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDelete = function(doc_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDeleteWithHttpInfo(doc_id, major_version, minor_version, relationship_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param relationship_id The relationship id field value. See Retrieve Document Relationships.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdDeleteWithHttpInfo = function(doc_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`relationship_id`)) {
        stop("Missing required parameter `relationship_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/relationships/{relationship_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`relationship_id`)) {
        local_var_url_path <- gsub("\\{relationship_id\\}", URLencode(as.character(`relationship_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param relationship_id The relationship id field value. See Retrieve Document Relationships.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGet = function(doc_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGetWithHttpInfo(doc_id, major_version, minor_version, relationship_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Relationship
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param relationship_id The relationship id field value. See Retrieve Document Relationships.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRelationshipsRelationshipIdGetWithHttpInfo = function(doc_id, major_version, minor_version, relationship_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`relationship_id`)) {
        stop("Missing required parameter `relationship_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/relationships/{relationship_id}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`relationship_id`)) {
        local_var_url_path <- gsub("\\{relationship_id\\}", URLencode(as.character(`relationship_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Version Renditions
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsGet = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsGetWithHttpInfo(doc_id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Version Renditions
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsGetWithHttpInfo = function(doc_id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/renditions"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeDelete = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeDeleteWithHttpInfo(doc_id, major_version, minor_version, rendition_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeDeleteWithHttpInfo = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Version Rendition File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeGet = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeGetWithHttpInfo(doc_id, major_version, minor_version, rendition_type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Version Rendition File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypeGetWithHttpInfo = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Upload Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePost = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePostWithHttpInfo(doc_id, major_version, minor_version, rendition_type, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Upload Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePostWithHttpInfo = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Replace Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePut = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePutWithHttpInfo(doc_id, major_version, minor_version, rendition_type, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Replace Document Version Rendition
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param rendition_type The document rendition type.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionRenditionsRenditionTypePutWithHttpInfo = function(doc_id, major_version, minor_version, rendition_type, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`rendition_type`)) {
        stop("Missing required parameter `rendition_type`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/renditions/{rendition_type}"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`rendition_type`)) {
        local_var_url_path <- gsub("\\{rendition_type\\}", URLencode(as.character(`rendition_type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Version Thumbnail File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionThumbnailGet = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionThumbnailGetWithHttpInfo(doc_id, major_version, minor_version, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Version Thumbnail File
    #'
    #' @param doc_id The document id field value.
    #' @param major_version The document major_version_number__v field value.
    #' @param minor_version The document minor_version_number__v field value.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsDocIdVersionsMajorVersionMinorVersionThumbnailGetWithHttpInfo = function(doc_id, major_version, minor_version, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`doc_id`)) {
        stop("Missing required parameter `doc_id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{doc_id}/versions/{major_version}/{minor_version}/thumbnail"
      if (!missing(`doc_id`)) {
        local_var_url_path <- gsub("\\{doc_id\\}", URLencode(as.character(`doc_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Documents
    #'
    #' @param named_filter (optional) Retrieves only documents which you have created.
    #' @param scope (optional) Searches only within the document content.
    #' @param versionscope (optional) Retrieves all document versions, rather than only the latest version.
    #' @param search (optional) Search for documents based on a {keyword} in searchable document fields.
    #' @param limit (optional) See VQL documentation for more information.
    #' @param sort (optional) See VQL documentation for more information.
    #' @param start (optional) See VQL documentation for more information.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsGet = function(named_filter = NULL, scope = NULL, versionscope = NULL, search = NULL, limit = NULL, sort = NULL, start = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsGetWithHttpInfo(named_filter, scope, versionscope, search, limit, sort, start, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Documents
    #'
    #' @param named_filter (optional) Retrieves only documents which you have created.
    #' @param scope (optional) Searches only within the document content.
    #' @param versionscope (optional) Retrieves all document versions, rather than only the latest version.
    #' @param search (optional) Search for documents based on a {keyword} in searchable document fields.
    #' @param limit (optional) See VQL documentation for more information.
    #' @param sort (optional) See VQL documentation for more information.
    #' @param start (optional) See VQL documentation for more information.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsGetWithHttpInfo = function(named_filter = NULL, scope = NULL, versionscope = NULL, search = NULL, limit = NULL, sort = NULL, start = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["named_filter"]] <- `named_filter`

      query_params[["scope"]] <- `scope`

      query_params[["versionscope"]] <- `versionscope`

      query_params[["search"]] <- `search`

      query_params[["limit"]] <- `limit`

      query_params[["sort"]] <- `sort`

      query_params[["start"]] <- `start`

      local_var_url_path <- "/objects/documents"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Document Roles
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdRolesGet = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdRolesGetWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Document Roles
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdRolesGetWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/roles"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on a Single Document
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdRolesPost = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdRolesPostWithHttpInfo(id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on a Single Document
    #'
    #' @param id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdRolesPostWithHttpInfo = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/roles"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Role
    #'
    #' @param id The document `id`.
    #' @param role_name The name of the role to retrieve. For example, `owner__v`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdRolesRoleNameGet = function(id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdRolesRoleNameGetWithHttpInfo(id, role_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Role
    #'
    #' @param id The document `id`.
    #' @param role_name The name of the role to retrieve. For example, `owner__v`.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdRolesRoleNameGetWithHttpInfo = function(id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`role_name`)) {
        stop("Missing required parameter `role_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/roles/{role_name}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`role_name`)) {
        local_var_url_path <- gsub("\\{role_name\\}", URLencode(as.character(`role_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document User Actions
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsGet = function(id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsGetWithHttpInfo(id, major_version, minor_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document User Actions
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsGetWithHttpInfo = function(id, major_version, minor_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/versions/{major_version}/{minor_version}/lifecycle_actions"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Entry Criteria
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param name__v The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGet = function(id, major_version, minor_version, name__v, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGetWithHttpInfo(id, major_version, minor_version, name__v, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Entry Criteria
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param name__v The lifecycle name__v field value from which to retrieve entry criteria. This is retrieved from the Retrieve User Actions request above.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVEntryRequirementsGetWithHttpInfo = function(id, major_version, minor_version, name__v, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`name__v`)) {
        stop("Missing required parameter `name__v`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/versions/{major_version}/{minor_version}/lifecycle_actions/{name__v}/entry_requirements"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`name__v`)) {
        local_var_url_path <- gsub("\\{name__v\\}", URLencode(as.character(`name__v`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Document User Action
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param name__v The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPut = function(id, major_version, minor_version, name__v, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPutWithHttpInfo(id, major_version, minor_version, name__v, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Document User Action
    #'
    #' @param id The document id field value from which to retrieve available user actions.
    #' @param major_version The major version number of the document.
    #' @param minor_version The minor version number of the document.
    #' @param name__v The action name__v field value to initiate. This is retrieved from the Retrieve User Action request.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsIdVersionsMajorVersionMinorVersionLifecycleActionsNameVPutWithHttpInfo = function(id, major_version, minor_version, name__v, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`major_version`)) {
        stop("Missing required parameter `major_version`.")
      }

      if (missing(`minor_version`)) {
        stop("Missing required parameter `minor_version`.")
      }

      if (missing(`name__v`)) {
        stop("Missing required parameter `name__v`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/{id}/versions/{major_version}/{minor_version}/lifecycle_actions/{name__v}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`major_version`)) {
        local_var_url_path <- gsub("\\{major_version\\}", URLencode(as.character(`major_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`minor_version`)) {
        local_var_url_path <- gsub("\\{minor_version\\}", URLencode(as.character(`minor_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`name__v`)) {
        local_var_url_path <- gsub("\\{name__v\\}", URLencode(as.character(`name__v`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve User Actions on Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsLifecycleActionsPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsLifecycleActionsPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve User Actions on Multiple Documents
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsLifecycleActionsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/lifecycle_actions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Bulk Document User Actions
    #'
    #' @param user_action_name The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Documents endpoint.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsLifecycleActionsUserActionNamePut = function(user_action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsLifecycleActionsUserActionNamePutWithHttpInfo(user_action_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Bulk Document User Actions
    #'
    #' @param user_action_name The user action name__v field value. Find this value with the Retrieve User Actions on Multiple Documents endpoint.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsLifecycleActionsUserActionNamePutWithHttpInfo = function(user_action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_action_name`)) {
        stop("Missing required parameter `user_action_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/lifecycle_actions/{user_action_name}"
      if (!missing(`user_action_name`)) {
        local_var_url_path <- gsub("\\{user_action_name\\}", URLencode(as.character(`user_action_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Single Document
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true,  you can use the status__v field to create documents in any lifecycle state. Additionally, you can manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsPost = function(authorization = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsPostWithHttpInfo(authorization, accept, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Single Document
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_migration_mode (optional) When set to true,  you can use the status__v field to create documents in any lifecycle state. Additionally, you can manually set the name, document number, and version number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsPostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Document Relationships
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRelationshipsBatchDelete = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRelationshipsBatchDeleteWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Document Relationships
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRelationshipsBatchDeleteWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/relationships/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Document Relationships
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRelationshipsBatchPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRelationshipsBatchPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Document Relationships
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRelationshipsBatchPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/relationships/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Document Renditions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRenditionsBatchDelete = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRenditionsBatchDeleteWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Document Renditions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRenditionsBatchDeleteWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/renditions/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add Multiple Document Renditions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param large_size_asset (optional) If set to true, indicates that the renditions to add are of the Large Size Asset (large_size_asset__v) rendition type. Vault applies Document Migration Mode limitations to renditions created with the request, but Document Migration permission is not required and your vault need not be in Migration Mode to use the parameter. Note that the request results in an error if the CSV contains any rendition type other than large_size_asset__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) Must be set to true when importing any rendition type other than large_size_asset__v. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRenditionsBatchPost = function(id_param = NULL, large_size_asset = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRenditionsBatchPostWithHttpInfo(id_param, large_size_asset, authorization, accept, content_type, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add Multiple Document Renditions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param large_size_asset (optional) If set to true, indicates that the renditions to add are of the Large Size Asset (large_size_asset__v) rendition type. Vault applies Document Migration Mode limitations to renditions created with the request, but Document Migration permission is not required and your vault need not be in Migration Mode to use the parameter. Note that the request results in an error if the CSV contains any rendition type other than large_size_asset__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) Must be set to true when importing any rendition type other than large_size_asset__v. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRenditionsBatchPostWithHttpInfo = function(id_param = NULL, large_size_asset = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      query_params[["largeSizeAsset"]] <- `large_size_asset`

      local_var_url_path <- "/objects/documents/renditions/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Users and Groups from Roles on Multiple Documents & Binders
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRolesBatchDelete = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRolesBatchDeleteWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Users and Groups from Roles on Multiple Documents & Binders
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRolesBatchDeleteWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/roles/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on Multiple Documents & Binders
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsRolesBatchPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsRolesBatchPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on Multiple Documents & Binders
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsRolesBatchPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/roles/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Template Collection
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Template Collection
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Single Document Template
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Single Document Template
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Document Templates
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesPut = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesPutWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Document Templates
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesPutWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Basic Document Template
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesTemplateNameDelete = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesTemplateNameDeleteWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Basic Document Template
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesTemplateNameDeleteWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates/{template_name}"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Document Template File
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesTemplateNameFileGet = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesTemplateNameFileGetWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Document Template File
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesTemplateNameFileGetWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates/{template_name}/file"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Document Template Attributes
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesTemplateNameGet = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesTemplateNameGetWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Document Template Attributes
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesTemplateNameGetWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates/{template_name}"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Single Document Template
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTemplatesTemplateNamePut = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTemplatesTemplateNamePutWithHttpInfo(template_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Single Document Template
    #'
    #' @param template_name The document template name__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTemplatesTemplateNamePutWithHttpInfo = function(template_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`template_name`)) {
        stop("Missing required parameter `template_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/templates/{template_name}"
      if (!missing(`template_name`)) {
        local_var_url_path <- gsub("\\{template_name\\}", URLencode(as.character(`template_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Document Tokens
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsTokensPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsTokensPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Document Tokens
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsTokensPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/documents/tokens"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Document Versions
    #'
    #' @param source (optional) Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true.
    #' @param renditions (optional) Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsVersionsBatchActionsFileextractPost = function(source = NULL, renditions = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsVersionsBatchActionsFileextractPostWithHttpInfo(source, renditions, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Document Versions
    #'
    #' @param source (optional) Optional: To exclude source files, include a query parameter source=false. If omitted, defaults to true.
    #' @param renditions (optional) Optional: To include renditions, include a query parameter renditions=true. If omitted, defaults to false.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsVersionsBatchActionsFileextractPostWithHttpInfo = function(source = NULL, renditions = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["source"]] <- `source`

      query_params[["renditions"]] <- `renditions`

      local_var_url_path <- "/objects/documents/versions/batch/actions/fileextract"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Document Versions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsVersionsBatchDelete = function(id_param = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsVersionsBatchDeleteWithHttpInfo(id_param, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Document Versions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsVersionsBatchDeleteWithHttpInfo = function(id_param = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      local_var_url_path <- "/objects/documents/versions/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Document Versions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) Must be set to true. Vault allows you to manually set the name and version number and to create documents in any lifecycle state using the `status__v` field, but does not allow you to change the document number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDocumentsVersionsBatchPost = function(id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDocumentsVersionsBatchPostWithHttpInfo(id_param, authorization, accept, content_type, x_vault_api_migration_mode, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Document Versions
    #'
    #' @param id_param (optional) If you’re identifying documents in your input by their external ID
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_migration_mode (optional) Must be set to true. Vault allows you to manually set the name and version number and to create documents in any lifecycle state using the `status__v` field, but does not allow you to change the document number. Vault also bypasses entry criteria, entry actions, and event actions. You must have the Document Migration permission to use this header. Learn more about <a href=\"https://platform.veevavault.help/en/gr/54028\">Document Migration Mode in Vault Help.</a>
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDocumentsVersionsBatchPostWithHttpInfo = function(id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_migration_mode = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      local_var_url_path <- "/objects/documents/versions/batch"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Domain Information
    #'
    #' @param include_application (optional) To include Vault application type information in the response, set include_application to true. If omitted, defaults to false and application information is not included.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDomainGet = function(include_application = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDomainGetWithHttpInfo(include_application, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Domain Information
    #'
    #' @param include_application (optional) To include Vault application type information in the response, set include_application to true. If omitted, defaults to false and application information is not included.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDomainGetWithHttpInfo = function(include_application = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["include_application"]] <- `include_application`

      local_var_url_path <- "/objects/domain"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Domains
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsDomainsGet = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsDomainsGetWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Domains
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsDomainsGetWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/domains"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add EDL Matched Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsEdlMatchedDocumentsBatchActionsAddPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsEdlMatchedDocumentsBatchActionsAddPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add EDL Matched Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsEdlMatchedDocumentsBatchActionsAddPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/edl_matched_documents/batch/actions/add"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove EDL Matched Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsEdlMatchedDocumentsBatchActionsRemovePost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsEdlMatchedDocumentsBatchActionsRemovePostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove EDL Matched Documents
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsEdlMatchedDocumentsBatchActionsRemovePostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/edl_matched_documents/batch/actions/remove"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Auto Managed Groups
    #'
    #' @param limit (optional) Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 1000. 
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsAutoGet = function(limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsAutoGetWithHttpInfo(limit, offset, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Auto Managed Groups
    #'
    #' @param limit (optional) Paginate the results by specifying the maximum number of records per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 1000. 
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsAutoGetWithHttpInfo = function(limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/objects/groups/auto"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Groups
    #'
    #' @param include_implied (optional) When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. If omitted, the response includes only the members__v field. These users are individually added to a group by an Admin.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsGet = function(include_implied = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsGetWithHttpInfo(include_implied, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Groups
    #'
    #' @param include_implied (optional) When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. If omitted, the response includes only the members__v field. These users are individually added to a group by an Admin.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsGetWithHttpInfo = function(include_implied = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["includeImplied"]] <- `include_implied`

      local_var_url_path <- "/objects/groups"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Group
    #'
    #' @param group_id The group id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsGroupIdDelete = function(group_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsGroupIdDeleteWithHttpInfo(group_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Group
    #'
    #' @param group_id The group id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsGroupIdDeleteWithHttpInfo = function(group_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`group_id`)) {
        stop("Missing required parameter `group_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/groups/{group_id}"
      if (!missing(`group_id`)) {
        local_var_url_path <- gsub("\\{group_id\\}", URLencode(as.character(`group_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Group
    #'
    #' @param group_id The group id field value.
    #' @param include_implied (optional) When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. When not used, the response includes only the members__v field. These users are individually added to a group by Admin.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsGroupIdGet = function(group_id, include_implied = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsGroupIdGetWithHttpInfo(group_id, include_implied, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Group
    #'
    #' @param group_id The group id field value.
    #' @param include_implied (optional) When true, the response includes the implied_members__v field. These users are automatically added to the group when their security_profiles__v are added to the group. When not used, the response includes only the members__v field. These users are individually added to a group by Admin.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsGroupIdGetWithHttpInfo = function(group_id, include_implied = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`group_id`)) {
        stop("Missing required parameter `group_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["includeImplied"]] <- `include_implied`

      local_var_url_path <- "/objects/groups/{group_id}"
      if (!missing(`group_id`)) {
        local_var_url_path <- gsub("\\{group_id\\}", URLencode(as.character(`group_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Group
    #'
    #' @param group_id The group id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsGroupIdPut = function(group_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsGroupIdPutWithHttpInfo(group_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Group
    #'
    #' @param group_id The group id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsGroupIdPutWithHttpInfo = function(group_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`group_id`)) {
        stop("Missing required parameter `group_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/groups/{group_id}"
      if (!missing(`group_id`)) {
        local_var_url_path <- gsub("\\{group_id\\}", URLencode(as.character(`group_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Group 
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsGroupsPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsGroupsPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Group 
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsGroupsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/groups"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Application License Usage
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsLicensesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsLicensesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Application License Usage
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsLicensesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/licenses"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Multi-Record Workflows
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsActionsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsActionsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Multi-Record Workflows
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsActionsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/actions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Multi-Record Workflow Details
    #'
    #' @param workflow_name 
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsActionsWorkflowNameGet = function(workflow_name, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsActionsWorkflowNameGetWithHttpInfo(workflow_name, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Multi-Record Workflow Details
    #'
    #' @param workflow_name 
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsActionsWorkflowNameGetWithHttpInfo = function(workflow_name, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_name`)) {
        stop("Missing required parameter `workflow_name`.")
      }




      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/actions/{workflow_name}"
      if (!missing(`workflow_name`)) {
        local_var_url_path <- gsub("\\{workflow_name\\}", URLencode(as.character(`workflow_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Multi-Record Workflow
    #'
    #' @param workflow_name 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsActionsWorkflowNamePost = function(workflow_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsActionsWorkflowNamePostWithHttpInfo(workflow_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Multi-Record Workflow
    #'
    #' @param workflow_name 
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsActionsWorkflowNamePostWithHttpInfo = function(workflow_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_name`)) {
        stop("Missing required parameter `workflow_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/actions/{workflow_name}"
      if (!missing(`workflow_name`)) {
        local_var_url_path <- gsub("\\{workflow_name\\}", URLencode(as.character(`workflow_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflows
    #'
    #' @param object__v (optional) To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used. 
    #' @param record_id__v (optional) To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used. 
    #' @param participant (optional) To retrieve all workflows available to a particular user, include the user id field value as ?participant={id}. To retrieve your own workflows, set this value to ?participant=me. This parameter is required when the object__v and record_id__v parameters are not used. 
    #' @param status__v (optional) To retrieve all workflows with specific statuses, include one or more status name__v field values. For example: status__v=active__v, status__v=active__v,completed__v. Workflows with `status__v=active__v` are in progress for the indicated object record. Valid statuses include: active__v completed__v cancelled__v
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201. 
    #' @param page_size (optional) This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000. 
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including ?loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsGet = function(object__v = NULL, record_id__v = NULL, participant = NULL, status__v = NULL, offset = NULL, page_size = NULL, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsGetWithHttpInfo(object__v, record_id__v, participant, status__v, offset, page_size, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflows
    #'
    #' @param object__v (optional) To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used. 
    #' @param record_id__v (optional) To retrieve all workflows configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the participant parameter is not used. 
    #' @param participant (optional) To retrieve all workflows available to a particular user, include the user id field value as ?participant={id}. To retrieve your own workflows, set this value to ?participant=me. This parameter is required when the object__v and record_id__v parameters are not used. 
    #' @param status__v (optional) To retrieve all workflows with specific statuses, include one or more status name__v field values. For example: status__v=active__v, status__v=active__v,completed__v. Workflows with `status__v=active__v` are in progress for the indicated object record. Valid statuses include: active__v completed__v cancelled__v
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201. 
    #' @param page_size (optional) This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000. 
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including ?loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsGetWithHttpInfo = function(object__v = NULL, record_id__v = NULL, participant = NULL, status__v = NULL, offset = NULL, page_size = NULL, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["object__v"]] <- `object__v`

      query_params[["record_id__v"]] <- `record_id__v`

      query_params[["participant"]] <- `participant`

      query_params[["status__v"]] <- `status__v`

      query_params[["offset"]] <- `offset`

      query_params[["page_size"]] <- `page_size`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Tasks
    #'
    #' @param object__v (optional) To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used.
    #' @param record_id__v (optional) To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used.
    #' @param assignee__v (optional) To retrieve all workflow tasks available to a particular user, include the user id field value as ?assignee__v={id}. To retrieve your own workflow tasks, set this value to ?assignee__v=me. This parameter is required when the object__v and record_id__v parameters are not used. 
    #' @param status__v (optional) To retrieve all workflow tasks with specific statuses, include one or more status name__v field values. For example: ?status__v=available__v or ?status__v=available__v,completed__v. 
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201. 
    #' @param page_size (optional) This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000. 
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including ?loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksGet = function(object__v = NULL, record_id__v = NULL, assignee__v = NULL, status__v = NULL, offset = NULL, page_size = NULL, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksGetWithHttpInfo(object__v, record_id__v, assignee__v, status__v, offset, page_size, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Tasks
    #'
    #' @param object__v (optional) To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used.
    #' @param record_id__v (optional) To retrieve all workflow tasks configured on an object, include the Vault object name__v and object record id field values as ?object__v={name__v}&record_id__v={id}. These two parameters are required when the assignee__v parameter is not used.
    #' @param assignee__v (optional) To retrieve all workflow tasks available to a particular user, include the user id field value as ?assignee__v={id}. To retrieve your own workflow tasks, set this value to ?assignee__v=me. This parameter is required when the object__v and record_id__v parameters are not used. 
    #' @param status__v (optional) To retrieve all workflow tasks with specific statuses, include one or more status name__v field values. For example: ?status__v=available__v or ?status__v=available__v,completed__v. 
    #' @param offset (optional) This parameter is used to paginate the results. It specifies the amount of offset from the first record returned. Vault returns 200 records per page by default. If you are viewing the first 200 results (page 1) and want to see the next page, set this to ?offset=201. 
    #' @param page_size (optional) This parameter is used to paginate the results. It specifies the size number of records to display per page. Vault returns 200 records per page by default. You can set this value lower or as high as 1000 records per page. For example: ?page_size=1000. 
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including ?loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksGetWithHttpInfo = function(object__v = NULL, record_id__v = NULL, assignee__v = NULL, status__v = NULL, offset = NULL, page_size = NULL, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["object__v"]] <- `object__v`

      query_params[["record_id__v"]] <- `record_id__v`

      query_params[["assignee__v"]] <- `assignee__v`

      query_params[["status__v"]] <- `status__v`

      query_params[["offset"]] <- `offset`

      query_params[["page_size"]] <- `page_size`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows/tasks"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Accept Single Record Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsAcceptPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsAcceptPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Accept Single Record Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsAcceptPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/accept"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Cancel Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsCancelPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsCancelPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cancel Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsCancelPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/cancel"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Complete Single Record Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsCompletePost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsCompletePostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Complete Single Record Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsCompletePostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/complete"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Task Actions
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsGet = function(task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsGetWithHttpInfo(task_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Task Actions
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsGetWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Accept Multi-item Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsMdwacceptPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsMdwacceptPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Accept Multi-item Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsMdwacceptPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/mdwaccept"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Complete Multi-item Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsMdwcompletePost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsMdwcompletePostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Complete Multi-item Workflow Task
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsMdwcompletePostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/mdwcomplete"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Manage Multi-Item Workflow Content
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsMdwmanagecontentPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsMdwmanagecontentPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Manage Multi-Item Workflow Content
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsMdwmanagecontentPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/mdwmanagecontent"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reassign Multi-item Workflow Task
    #'
    #' @param task_id The id of the task to reassign.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsMdwreassignPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsMdwreassignPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reassign Multi-item Workflow Task
    #'
    #' @param task_id The id of the task to reassign.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsMdwreassignPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/mdwreassign"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reassign Single Record Workflow Task
    #'
    #' @param task_id The id of the task to reassign.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsReassignPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsReassignPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reassign Single Record Workflow Task
    #'
    #' @param task_id The id of the task to reassign.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsReassignPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/reassign"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Task Action Details
    #'
    #' @param task_id The task id field value.
    #' @param task_action The name of the task action retrieved from Retrieve Workflow Task Actions.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsTaskActionGet = function(task_id, task_action, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsTaskActionGetWithHttpInfo(task_id, task_action, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Task Action Details
    #'
    #' @param task_id The task id field value.
    #' @param task_action The name of the task action retrieved from Retrieve Workflow Task Actions.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsTaskActionGetWithHttpInfo = function(task_id, task_action, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }

      if (missing(`task_action`)) {
        stop("Missing required parameter `task_action`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/{task_action}"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`task_action`)) {
        local_var_url_path <- gsub("\\{task_action\\}", URLencode(as.character(`task_action`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Undo Workflow Task Acceptance
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsUndoacceptPost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsUndoacceptPostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Undo Workflow Task Acceptance
    #'
    #' @param task_id The task id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsUndoacceptPostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/undoaccept"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Workflow Task Due Date
    #'
    #' @param task_id The id of the task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdActionsUpdateduedatePost = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdActionsUpdateduedatePostWithHttpInfo(task_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Workflow Task Due Date
    #'
    #' @param task_id The id of the task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdActionsUpdateduedatePostWithHttpInfo = function(task_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}/actions/updateduedate"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Task Details
    #'
    #' @param task_id The task id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsTasksTaskIdGet = function(task_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsTasksTaskIdGetWithHttpInfo(task_id, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Task Details
    #'
    #' @param task_id The task id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsTasksTaskIdGetWithHttpInfo = function(task_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows/tasks/{task_id}"
      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Actions
    #'
    #' @param workflow_id The workflow id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsWorkflowIdActionsGet = function(workflow_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsWorkflowIdActionsGetWithHttpInfo(workflow_id, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Actions
    #'
    #' @param workflow_id The workflow id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsWorkflowIdActionsGetWithHttpInfo = function(workflow_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_id`)) {
        stop("Missing required parameter `workflow_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows/{workflow_id}/actions"
      if (!missing(`workflow_id`)) {
        local_var_url_path <- gsub("\\{workflow_id\\}", URLencode(as.character(`workflow_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Action Details
    #'
    #' @param workflow_id The workflow id field value.
    #' @param workflow_action The workflow action name retrieved from Retrieve Workflow Actions.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionGet = function(workflow_id, workflow_action, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionGetWithHttpInfo(workflow_id, workflow_action, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Action Details
    #'
    #' @param workflow_id The workflow id field value.
    #' @param workflow_action The workflow action name retrieved from Retrieve Workflow Actions.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionGetWithHttpInfo = function(workflow_id, workflow_action, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_id`)) {
        stop("Missing required parameter `workflow_id`.")
      }

      if (missing(`workflow_action`)) {
        stop("Missing required parameter `workflow_action`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/objectworkflows/{workflow_id}/actions/{workflow_action}"
      if (!missing(`workflow_id`)) {
        local_var_url_path <- gsub("\\{workflow_id\\}", URLencode(as.character(`workflow_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`workflow_action`)) {
        local_var_url_path <- gsub("\\{workflow_action\\}", URLencode(as.character(`workflow_action`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Workflow Action
    #'
    #' @param workflow_id The workflow id field value.
    #' @param workflow_action The workflow action name retrieved from Retrieve Workflow Actions.
    #' @param documents__sys (optional) Include the id or ids as a comma-separated list of the document(s) to be removed from a document workflow when using the removecontent action. If your workflow_action is remove_content, include the id of the document to remove the workflow. To remove multiple documents, use a comma-separated list of ids.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionPost = function(workflow_id, workflow_action, documents__sys = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionPostWithHttpInfo(workflow_id, workflow_action, documents__sys, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Workflow Action
    #'
    #' @param workflow_id The workflow id field value.
    #' @param workflow_action The workflow action name retrieved from Retrieve Workflow Actions.
    #' @param documents__sys (optional) Include the id or ids as a comma-separated list of the document(s) to be removed from a document workflow when using the removecontent action. If your workflow_action is remove_content, include the id of the document to remove the workflow. To remove multiple documents, use a comma-separated list of ids.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsWorkflowIdActionsWorkflowActionPostWithHttpInfo = function(workflow_id, workflow_action, documents__sys = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_id`)) {
        stop("Missing required parameter `workflow_id`.")
      }

      if (missing(`workflow_action`)) {
        stop("Missing required parameter `workflow_action`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["documents__sys"]] <- `documents__sys`

      local_var_url_path <- "/objects/objectworkflows/{workflow_id}/actions/{workflow_action}"
      if (!missing(`workflow_id`)) {
        local_var_url_path <- gsub("\\{workflow_id\\}", URLencode(as.character(`workflow_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`workflow_action`)) {
        local_var_url_path <- gsub("\\{workflow_action\\}", URLencode(as.character(`workflow_action`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Workflow Details
    #'
    #' @param workflow_id The workflow id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsObjectworkflowsWorkflowIdGet = function(workflow_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsObjectworkflowsWorkflowIdGetWithHttpInfo(workflow_id, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Workflow Details
    #'
    #' @param workflow_id The workflow id field value.
    #' @param loc (optional) When localized (translated) strings are available, retrieve them by including loc=true.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsObjectworkflowsWorkflowIdGetWithHttpInfo = function(workflow_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`workflow_id`)) {
        stop("Missing required parameter `workflow_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/objects/objectworkflows/{workflow_id}"
      if (!missing(`workflow_id`)) {
        local_var_url_path <- gsub("\\{workflow_id\\}", URLencode(as.character(`workflow_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Picklists
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Picklists
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Picklist Values
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsPicklistNameGet = function(picklist_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsPicklistNameGetWithHttpInfo(picklist_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Picklist Values
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsPicklistNameGetWithHttpInfo = function(picklist_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`picklist_name`)) {
        stop("Missing required parameter `picklist_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists/{picklist_name}"
      if (!missing(`picklist_name`)) {
        local_var_url_path <- gsub("\\{picklist_name\\}", URLencode(as.character(`picklist_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Inactivate Picklist Value
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param picklist_value_name The picklist value name field value (north_america__c, south_america__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsPicklistNamePicklistValueNameDelete = function(picklist_name, picklist_value_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsPicklistNamePicklistValueNameDeleteWithHttpInfo(picklist_name, picklist_value_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Inactivate Picklist Value
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param picklist_value_name The picklist value name field value (north_america__c, south_america__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsPicklistNamePicklistValueNameDeleteWithHttpInfo = function(picklist_name, picklist_value_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`picklist_name`)) {
        stop("Missing required parameter `picklist_name`.")
      }

      if (missing(`picklist_value_name`)) {
        stop("Missing required parameter `picklist_value_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists/{picklist_name}/{picklist_value_name}"
      if (!missing(`picklist_name`)) {
        local_var_url_path <- gsub("\\{picklist_name\\}", URLencode(as.character(`picklist_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`picklist_value_name`)) {
        local_var_url_path <- gsub("\\{picklist_value_name\\}", URLencode(as.character(`picklist_value_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Picklist Value
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param picklist_value_name The picklist value name field value (north_america__c, south_america__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsPicklistNamePicklistValueNamePut = function(picklist_name, picklist_value_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsPicklistNamePicklistValueNamePutWithHttpInfo(picklist_name, picklist_value_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Picklist Value
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param picklist_value_name The picklist value name field value (north_america__c, south_america__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsPicklistNamePicklistValueNamePutWithHttpInfo = function(picklist_name, picklist_value_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`picklist_name`)) {
        stop("Missing required parameter `picklist_name`.")
      }

      if (missing(`picklist_value_name`)) {
        stop("Missing required parameter `picklist_value_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists/{picklist_name}/{picklist_value_name}"
      if (!missing(`picklist_name`)) {
        local_var_url_path <- gsub("\\{picklist_name\\}", URLencode(as.character(`picklist_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`picklist_value_name`)) {
        local_var_url_path <- gsub("\\{picklist_value_name\\}", URLencode(as.character(`picklist_value_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Picklist Values
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsPicklistNamePost = function(picklist_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsPicklistNamePostWithHttpInfo(picklist_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Picklist Values
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsPicklistNamePostWithHttpInfo = function(picklist_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`picklist_name`)) {
        stop("Missing required parameter `picklist_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists/{picklist_name}"
      if (!missing(`picklist_name`)) {
        local_var_url_path <- gsub("\\{picklist_name\\}", URLencode(as.character(`picklist_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Picklist Value Label
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsPicklistsPicklistNamePut = function(picklist_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsPicklistsPicklistNamePutWithHttpInfo(picklist_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Picklist Value Label
    #'
    #' @param picklist_name The picklist name field value (license_type__v, product_family__c, region__c, etc.)
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsPicklistsPicklistNamePutWithHttpInfo = function(picklist_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`picklist_name`)) {
        stop("Missing required parameter `picklist_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/picklists/{picklist_name}"
      if (!missing(`picklist_name`)) {
        local_var_url_path <- gsub("\\{picklist_name\\}", URLencode(as.character(`picklist_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Build Production Vault
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxActionsBuildproductionPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxActionsBuildproductionPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Build Production Vault
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxActionsBuildproductionPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/actions/buildproduction"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Promote to Production
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxActionsPromoteproductionPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxActionsPromoteproductionPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Promote to Production
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxActionsPromoteproductionPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/actions/promoteproduction"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Recheck Sandbox Usage Limit
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxActionsRecheckusagePost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxActionsRecheckusagePostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Recheck Sandbox Usage Limit
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxActionsRecheckusagePostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/actions/recheckusage"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Change Sandbox Size
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxBatchChangesizePost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxBatchChangesizePostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Change Sandbox Size
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxBatchChangesizePostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/batch/changesize"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Set Sandbox Entitlements
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxEntitlementsSetPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxEntitlementsSetPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Set Sandbox Entitlements
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxEntitlementsSetPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/entitlements/set"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Sandboxes
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Sandboxes
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Sandbox
    #'
    #' @param name The name of the sandbox vault to delete. This is the name which appears on the My Vaults page.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxNameDelete = function(name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxNameDeleteWithHttpInfo(name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Sandbox
    #'
    #' @param name The name of the sandbox vault to delete. This is the name which appears on the My Vaults page.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxNameDeleteWithHttpInfo = function(name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create or Refresh Sandbox
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create or Refresh Sandbox
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxSnapshotApiNameActionsUpdatePost = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxSnapshotApiNameActionsUpdatePostWithHttpInfo(api_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxSnapshotApiNameActionsUpdatePostWithHttpInfo = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`api_name`)) {
        stop("Missing required parameter `api_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/snapshot/{api_name}/actions/update"
      if (!missing(`api_name`)) {
        local_var_url_path <- gsub("\\{api_name\\}", URLencode(as.character(`api_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Upgrade Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxSnapshotApiNameActionsUpgradePost = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxSnapshotApiNameActionsUpgradePostWithHttpInfo(api_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Upgrade Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxSnapshotApiNameActionsUpgradePostWithHttpInfo = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`api_name`)) {
        stop("Missing required parameter `api_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/snapshot/{api_name}/actions/upgrade"
      if (!missing(`api_name`)) {
        local_var_url_path <- gsub("\\{api_name\\}", URLencode(as.character(`api_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxSnapshotApiNameDelete = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxSnapshotApiNameDeleteWithHttpInfo(api_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Sandbox Snapshot
    #'
    #' @param api_name The Vault ID of the sandbox. Obtain this from the Retrieve Sandbox Snapshots request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxSnapshotApiNameDeleteWithHttpInfo = function(api_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`api_name`)) {
        stop("Missing required parameter `api_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/snapshot/{api_name}"
      if (!missing(`api_name`)) {
        local_var_url_path <- gsub("\\{api_name\\}", URLencode(as.character(`api_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Sandbox Snapshots
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxSnapshotGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxSnapshotGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Sandbox Snapshots
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxSnapshotGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/snapshot"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Sandbox Snapshot
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxSnapshotPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxSnapshotPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Sandbox Snapshot
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxSnapshotPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/snapshot"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Refresh Sandbox from Snapshot
    #'
    #' @param vault_id The Vault ID of the sandbox to be refreshed.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxVaultIdActionsRefreshPost = function(vault_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxVaultIdActionsRefreshPostWithHttpInfo(vault_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Refresh Sandbox from Snapshot
    #'
    #' @param vault_id The Vault ID of the sandbox to be refreshed.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxVaultIdActionsRefreshPostWithHttpInfo = function(vault_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vault_id`)) {
        stop("Missing required parameter `vault_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/{vault_id}/actions/refresh"
      if (!missing(`vault_id`)) {
        local_var_url_path <- gsub("\\{vault_id\\}", URLencode(as.character(`vault_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Sandbox Details by ID
    #'
    #' @param vault_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSandboxVaultIdGet = function(vault_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSandboxVaultIdGetWithHttpInfo(vault_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Sandbox Details by ID
    #'
    #' @param vault_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSandboxVaultIdGetWithHttpInfo = function(vault_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`vault_id`)) {
        stop("Missing required parameter `vault_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/sandbox/{vault_id}"
      if (!missing(`vault_id`)) {
        local_var_url_path <- gsub("\\{vault_id\\}", URLencode(as.character(`vault_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Security Policies
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSecuritypoliciesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSecuritypoliciesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Security Policies
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSecuritypoliciesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/securitypolicies"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Security Policy
    #'
    #' @param security_policy_name Security policy name__v field value (retrieved from previous request). This is typically a numeric value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsSecuritypoliciesSecurityPolicyNameGet = function(security_policy_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsSecuritypoliciesSecurityPolicyNameGetWithHttpInfo(security_policy_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Security Policy
    #'
    #' @param security_policy_name Security policy name__v field value (retrieved from previous request). This is typically a numeric value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsSecuritypoliciesSecurityPolicyNameGetWithHttpInfo = function(security_policy_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`security_policy_name`)) {
        stop("Missing required parameter `security_policy_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/securitypolicies/{security_policy_name}"
      if (!missing(`security_policy_name`)) {
        local_var_url_path <- gsub("\\{security_policy_name\\}", URLencode(as.character(`security_policy_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Users
    #'
    #' @param vaults (optional) Retrieve all users assigned to all Vaults in your domain.
    #' @param exclude_vault_membership (optional) Optional: Set to false to include vault_membership fields. If true or omitted, vault_membership fields are not included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to false to include app_licensing fields. If true or omitted, app_licensing fields are not included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersGet = function(vaults = NULL, exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersGetWithHttpInfo(vaults, exclude_vault_membership, exclude_app_licensing, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Users
    #'
    #' @param vaults (optional) Retrieve all users assigned to all Vaults in your domain.
    #' @param exclude_vault_membership (optional) Optional: Set to false to include vault_membership fields. If true or omitted, vault_membership fields are not included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to false to include app_licensing fields. If true or omitted, app_licensing fields are not included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersGetWithHttpInfo = function(vaults = NULL, exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["vaults"]] <- `vaults`

      query_params[["exclude_vault_membership"]] <- `exclude_vault_membership`

      query_params[["exclude_app_licensing"]] <- `exclude_app_licensing`

      local_var_url_path <- "/objects/users/"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve User
    #'
    #' @param id The user id field value. Use the value me to get information for the currently authenticated user.
    #' @param exclude_vault_membership (optional) Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersIdGet = function(id, exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersIdGetWithHttpInfo(id, exclude_vault_membership, exclude_app_licensing, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve User
    #'
    #' @param id The user id field value. Use the value me to get information for the currently authenticated user.
    #' @param exclude_vault_membership (optional) Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersIdGetWithHttpInfo = function(id, exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["exclude_vault_membership"]] <- `exclude_vault_membership`

      query_params[["exclude_app_licensing"]] <- `exclude_app_licensing`

      local_var_url_path <- "/objects/users/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve User Permissions
    #'
    #' @param id The ID of the user. Use the value me to retrieve information for the currently authenticated user.
    #' @param filter (optional) Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersIdPermissionsGet = function(id, filter = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersIdPermissionsGetWithHttpInfo(id, filter, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve User Permissions
    #'
    #' @param id The ID of the user. Use the value me to retrieve information for the currently authenticated user.
    #' @param filter (optional) Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersIdPermissionsGetWithHttpInfo = function(id, filter = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/objects/users/{id}/permissions"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Single User
    #'
    #' @param id The user id field value. Use the value me to get information for the currently authenticated user.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersIdPut = function(id, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersIdPutWithHttpInfo(id, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Single User
    #'
    #' @param id The user id field value. Use the value me to get information for the currently authenticated user.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersIdPutWithHttpInfo = function(id, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Validate Session User
    #'
    #' @param exclude_vault_membership (optional) Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersMeGet = function(exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersMeGetWithHttpInfo(exclude_vault_membership, exclude_app_licensing, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Validate Session User
    #'
    #' @param exclude_vault_membership (optional) Optional: Set to true to omit vault_membership fields. If you don’t need these fields, this may increase performance. If omitted, vault_membership fields are included in the response.
    #' @param exclude_app_licensing (optional) Optional: Set to true to omit app_licensing fields. If you don’t need these fields, this may increase performance. If omitted, app_licensing fields are included in the response.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersMeGetWithHttpInfo = function(exclude_vault_membership = NULL, exclude_app_licensing = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["exclude_vault_membership"]] <- `exclude_vault_membership`

      query_params[["exclude_app_licensing"]] <- `exclude_app_licensing`

      local_var_url_path <- "/objects/users/me"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Change My Password
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersMePasswordPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersMePasswordPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Change My Password
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersMePasswordPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users/me/password"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve My User Permissions
    #'
    #' @param filter (optional) Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersMePermissionsGet = function(filter = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersMePermissionsGetWithHttpInfo(filter, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve My User Permissions
    #'
    #' @param filter (optional) Filter the results to show only one specific name__v, which is in the format object.{object name}.{object or field}_actions. Wildcards are not supported. See example below.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersMePermissionsGetWithHttpInfo = function(filter = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["filter"]] <- `filter`

      local_var_url_path <- "/objects/users/me/permissions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update My User
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersMePut = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersMePutWithHttpInfo(authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update My User
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersMePutWithHttpInfo = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users/me"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Single User
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Single User
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Users
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersPut = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersPutWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Users
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersPutWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Disable User
    #'
    #' @param user_id The user id field value. 
    #' @param domain (optional) When true, this disables the user account in all vaults in the domain.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersUserIdDelete = function(user_id, domain = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersUserIdDeleteWithHttpInfo(user_id, domain, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Disable User
    #'
    #' @param user_id The user id field value. 
    #' @param domain (optional) When true, this disables the user account in all vaults in the domain.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersUserIdDeleteWithHttpInfo = function(user_id, domain = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_id`)) {
        stop("Missing required parameter `user_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["domain"]] <- `domain`

      local_var_url_path <- "/objects/users/{user_id}"
      if (!missing(`user_id`)) {
        local_var_url_path <- gsub("\\{user_id\\}", URLencode(as.character(`user_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Vault Membership
    #'
    #' @param user_id The user id field value.
    #' @param vault_id The system-managed id field value assigned to each vault in the domain.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsUsersUserIdVaultMembershipVaultIdPut = function(user_id, vault_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsUsersUserIdVaultMembershipVaultIdPutWithHttpInfo(user_id, vault_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Vault Membership
    #'
    #' @param user_id The user id field value.
    #' @param vault_id The system-managed id field value assigned to each vault in the domain.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsUsersUserIdVaultMembershipVaultIdPutWithHttpInfo = function(user_id, vault_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_id`)) {
        stop("Missing required parameter `user_id`.")
      }

      if (missing(`vault_id`)) {
        stop("Missing required parameter `vault_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/users/{user_id}/vault_membership/{vault_id}"
      if (!missing(`user_id`)) {
        local_var_url_path <- gsub("\\{user_id\\}", URLencode(as.character(`user_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`vault_id`)) {
        local_var_url_path <- gsub("\\{vault_id\\}", URLencode(as.character(`vault_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Vault Compare
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsVaultActionsComparePost = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsVaultActionsComparePostWithHttpInfo(authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Vault Compare
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsVaultActionsComparePostWithHttpInfo = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/vault/actions/compare"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Vault Configuration Report
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ObjectsVaultActionsConfigreportPost = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ObjectsVaultActionsConfigreportPostWithHttpInfo(authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Vault Configuration Report
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ObjectsVaultActionsConfigreportPostWithHttpInfo = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/objects/vault/actions/configreport"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Component Definition Query
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    QueryComponentsPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$QueryComponentsPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Component Definition Query
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    QueryComponentsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/query/components"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Next Page URL
    #'
    #' @param next_page 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    QueryNextPagePost = function(next_page, authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$QueryNextPagePostWithHttpInfo(next_page, authorization, accept, x_vault_api_describe_query, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Next Page URL
    #'
    #' @param next_page 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    QueryNextPagePostWithHttpInfo = function(next_page, authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`next_page`)) {
        stop("Missing required parameter `next_page`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-DescribeQuery"] <- `x_vault_api_describe_query`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/query/{next_page}"
      if (!missing(`next_page`)) {
        local_var_url_path <- gsub("\\{next_page\\}", URLencode(as.character(`next_page`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Submitting a Query
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    QueryPost = function(authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$QueryPostWithHttpInfo(authorization, accept, x_vault_api_describe_query, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Submitting a Query
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    QueryPostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-DescribeQuery"] <- `x_vault_api_describe_query`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/query"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Previous Page URL
    #'
    #' @param previous_page 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    QueryPreviousPagePost = function(previous_page, authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$QueryPreviousPagePostWithHttpInfo(previous_page, authorization, accept, x_vault_api_describe_query, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Previous Page URL
    #'
    #' @param previous_page 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_describe_query (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    QueryPreviousPagePostWithHttpInfo = function(previous_page, authorization = NULL, accept = NULL, x_vault_api_describe_query = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`previous_page`)) {
        stop("Missing required parameter `previous_page`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-DescribeQuery"] <- `x_vault_api_describe_query`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/query/{previous_page}"
      if (!missing(`previous_page`)) {
        local_var_url_path <- gsub("\\{previous_page\\}", URLencode(as.character(`previous_page`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Current User with SCIM
    #'
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2MeGet = function(attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2MeGetWithHttpInfo(attributes, excluded_attributes, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Current User with SCIM
    #'
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2MeGetWithHttpInfo = function(attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      local_var_url_path <- "/scim/v2/Me"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Current User with SCIM
    #'
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2MePut = function(attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2MePutWithHttpInfo(attributes, excluded_attributes, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Current User with SCIM
    #'
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2MePutWithHttpInfo = function(attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      local_var_url_path <- "/scim/v2/Me"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All SCIM Resource Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2ResourceTypesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2ResourceTypesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All SCIM Resource Types
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2ResourceTypesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/ResourceTypes"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single SCIM Resource Type
    #'
    #' @param type A specific resource type. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the id value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2ResourceTypesTypeGet = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2ResourceTypesTypeGetWithHttpInfo(type, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single SCIM Resource Type
    #'
    #' @param type A specific resource type. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the id value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2ResourceTypesTypeGetWithHttpInfo = function(type, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/ResourceTypes/{type}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All SCIM Schema Information
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2SchemasGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2SchemasGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All SCIM Schema Information
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2SchemasGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/Schemas"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single SCIM Schema Information
    #'
    #' @param id The ID of a specific schema. For example, urn:ietf:params:scim:schemas:extension:veevavault:2.0:User.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2SchemasIdGet = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2SchemasIdGetWithHttpInfo(id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single SCIM Schema Information
    #'
    #' @param id The ID of a specific schema. For example, urn:ietf:params:scim:schemas:extension:veevavault:2.0:User.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2SchemasIdGetWithHttpInfo = function(id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/Schemas/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve SCIM Provider
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2ServiceProviderConfigGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2ServiceProviderConfigGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve SCIM Provider
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2ServiceProviderConfigGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/ServiceProviderConfig"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve SCIM Resources
    #'
    #' @param type The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param sort_by (optional) Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage
    #' @param sort_order (optional) Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000.
    #' @param start_index (optional) Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2TypeGet = function(type, filter = NULL, attributes = NULL, excluded_attributes = NULL, sort_by = NULL, sort_order = NULL, start_index = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2TypeGetWithHttpInfo(type, filter, attributes, excluded_attributes, sort_by, sort_order, start_index, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve SCIM Resources
    #'
    #' @param type The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param sort_by (optional) Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage
    #' @param sort_order (optional) Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000.
    #' @param start_index (optional) Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2TypeGetWithHttpInfo = function(type, filter = NULL, attributes = NULL, excluded_attributes = NULL, sort_by = NULL, sort_order = NULL, start_index = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["filter"]] <- `filter`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      query_params[["sortBy"]] <- `sort_by`

      query_params[["sortOrder"]] <- `sort_order`

      query_params[["startIndex"]] <- `start_index`

      local_var_url_path <- "/scim/v2/{type}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single SCIM Resource
    #'
    #' @param type The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
    #' @param id The ID of the resource to retrieve. You can retrieve all resource IDs from a particular resource type with the Retrieve SCIM Resources endpoint. For example, business_admin__v.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2TypeIdGet = function(type, id, attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2TypeIdGetWithHttpInfo(type, id, attributes, excluded_attributes, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single SCIM Resource
    #'
    #' @param type The resource type to retrieve. You can retrieve all available types from the Retrieve All SCIM Resource Types endpoint, where the value for this parameter is the endpoint value.
    #' @param id The ID of the resource to retrieve. You can retrieve all resource IDs from a particular resource type with the Retrieve SCIM Resources endpoint. For example, business_admin__v.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2TypeIdGetWithHttpInfo = function(type, id, attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      local_var_url_path <- "/scim/v2/{type}/{id}"
      if (!missing(`type`)) {
        local_var_url_path <- gsub("\\{type\\}", URLencode(as.character(`type`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Users with SCIM
    #'
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param sort_by (optional) Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage
    #' @param sort_order (optional) Optional: Specify the order in which the sortBy parameter is applied. Allowed values are ascending or descending. If omitted, defaults to ascending.
    #' @param count (optional) Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000.
    #' @param start_index (optional) Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2UsersGet = function(filter = NULL, attributes = NULL, excluded_attributes = NULL, sort_by = NULL, sort_order = NULL, count = NULL, start_index = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2UsersGetWithHttpInfo(filter, attributes, excluded_attributes, sort_by, sort_order, count, start_index, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Users with SCIM
    #'
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param sort_by (optional) Optional: Specify an attribute or sub-attribute to order the response. For example, you can sort by the displayName attribute, or the name.familyName sub-attribute. If omitted, the response is sorted by id. Note that the following attributes are not supported: securityPolicy securityProfile locale preferredLanguage
    #' @param sort_order (optional) Optional: Specify the order in which the sortBy parameter is applied. Allowed values are ascending or descending. If omitted, defaults to ascending.
    #' @param count (optional) Optional: Specify the number of query results per page, for example, 10. Negative values are treated as 0, and 0 returns no results except for totalResults. If omitted, defaults to 1000.
    #' @param start_index (optional) Optional: Specify the index of the first result. For example, 10 would omit the first 9 results and begin on result 10. Omission, negative values, and 0 is treated as 1.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2UsersGetWithHttpInfo = function(filter = NULL, attributes = NULL, excluded_attributes = NULL, sort_by = NULL, sort_order = NULL, count = NULL, start_index = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE











      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["filter"]] <- `filter`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      query_params[["sortBy"]] <- `sort_by`

      query_params[["sortOrder"]] <- `sort_order`

      query_params[["count"]] <- `count`

      query_params[["startIndex"]] <- `start_index`

      local_var_url_path <- "/scim/v2/Users"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single User with SCIM
    #'
    #' @param id The id of the user you wish to update.
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2UsersIdGet = function(id, filter = NULL, attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2UsersIdGetWithHttpInfo(id, filter, attributes, excluded_attributes, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single User with SCIM
    #'
    #' @param id The id of the user you wish to update.
    #' @param filter (optional) Optional: Filter for a specific attribute value. Must be in the format {attribute} eq \"{value}\". For example, to filter for a particular user name, userName eq \"john\". Complex expressions are not supported, and eq is the only supported operator.
    #' @param attributes (optional) Optional: Include specified attributes only. Enter multiple values in a comma separated list. For example, to include only user name and email in the response, attributes=userName,emails. Note that the schemas and id attributes are always returned.
    #' @param excluded_attributes (optional) Optional: Exclude specific attributes from the response. Enter multiple values in a comma separated list. For example, to exclude user name and email from the response, excludedAttributes=userName,emails. Note that the schemas and id attributes are always returned and cannot be excluded.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2UsersIdGetWithHttpInfo = function(id, filter = NULL, attributes = NULL, excluded_attributes = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["filter"]] <- `filter`

      query_params[["attributes"]] <- `attributes`

      query_params[["excludedAttributes"]] <- `excluded_attributes`

      local_var_url_path <- "/scim/v2/Users/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update User with SCIM
    #'
    #' @param id The id of the user you wish to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2UsersIdPut = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2UsersIdPutWithHttpInfo(id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update User with SCIM
    #'
    #' @param id The id of the user you wish to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2UsersIdPutWithHttpInfo = function(id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/Users/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create User with SCIM
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ScimV2UsersPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ScimV2UsersPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create User with SCIM
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ScimV2UsersPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/scim/v2/Users"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Signing Certificate
    #'
    #' @param cert_id The cert_id is provided in each Spark message in the X-VaultAPISignature-CertificateId header.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesCertificateCertIdGet = function(cert_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesCertificateCertIdGetWithHttpInfo(cert_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Signing Certificate
    #'
    #' @param cert_id The cert_id is provided in each Spark message in the X-VaultAPISignature-CertificateId header.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesCertificateCertIdGetWithHttpInfo = function(cert_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`cert_id`)) {
        stop("Missing required parameter `cert_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/certificate/{cert_id}"
      if (!missing(`cert_id`)) {
        local_var_url_path <- gsub("\\{cert_id\\}", URLencode(as.character(`cert_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Disable Configuration Mode
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesConfigurationModeActionsDisablePost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesConfigurationModeActionsDisablePostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Disable Configuration Mode
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesConfigurationModeActionsDisablePostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/configuration_mode/actions/disable"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Enable Configuration Mode
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesConfigurationModeActionsEnablePost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesConfigurationModeActionsEnablePostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Enable Configuration Mode
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesConfigurationModeActionsEnablePostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/configuration_mode/actions/enable"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Available Direct Data Files
    #'
    #' @param extract_type (optional) The Direct Data file type: incremental_directdata, full_directdata, or log_directdata. If omitted, returns all files.
    #' @param start_time (optional) Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 7AM on January 15, 2024 would use 2024-01-15T07:00:00Z. If omitted, defaults to the Vault’s creation date and time.
    #' @param stop_time (optional) Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 9AM on January 15, 2024 would use 2024-01-15T09:00:00Z. If omitted, defaults to today’s date and current time.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesDirectdataFilesGet = function(extract_type = NULL, start_time = NULL, stop_time = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesDirectdataFilesGetWithHttpInfo(extract_type, start_time, stop_time, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Available Direct Data Files
    #'
    #' @param extract_type (optional) The Direct Data file type: incremental_directdata, full_directdata, or log_directdata. If omitted, returns all files.
    #' @param start_time (optional) Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 7AM on January 15, 2024 would use 2024-01-15T07:00:00Z. If omitted, defaults to the Vault’s creation date and time.
    #' @param stop_time (optional) Specify a time in YYYY-MM-DDTHH:MM:SSZ format. For example, 9AM on January 15, 2024 would use 2024-01-15T09:00:00Z. If omitted, defaults to today’s date and current time.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesDirectdataFilesGetWithHttpInfo = function(extract_type = NULL, start_time = NULL, stop_time = NULL, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE







      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["extract_type"]] <- `extract_type`

      query_params[["start_time"]] <- `start_time`

      query_params[["stop_time"]] <- `stop_time`

      local_var_url_path <- "/services/directdata/files"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Direct Data File
    #'
    #' @param name The name of the Direct Data file part. Obtain this from the Retrieve Available Direct Data Files request. For example, 146478-20240213-0000-F.001.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesDirectdataFilesNameGet = function(name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesDirectdataFilesNameGetWithHttpInfo(name, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Direct Data File
    #'
    #' @param name The name of the Direct Data file part. Obtain this from the Retrieve Available Direct Data Files request. For example, 146478-20240213-0000-F.001.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesDirectdataFilesNameGetWithHttpInfo = function(name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/directdata/files/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Item Content
    #'
    #' @param item The absolute path to a file or folder. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param range (optional) Optional: Specifies a partial range of bytes to include in the upload. Maximum 50 MB. Must be in the format `bytes={min}-{max}`. For example, `bytes=0-1000`.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingItemsContentItemGet = function(item, authorization = NULL, accept = NULL, range = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingItemsContentItemGetWithHttpInfo(item, authorization, accept, range, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Item Content
    #'
    #' @param item The absolute path to a file or folder. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param range (optional) Optional: Specifies a partial range of bytes to include in the upload. Maximum 50 MB. Must be in the format `bytes={min}-{max}`. For example, `bytes=0-1000`.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingItemsContentItemGetWithHttpInfo = function(item, authorization = NULL, accept = NULL, range = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`item`)) {
        stop("Missing required parameter `item`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Range"] <- `range`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/items/content/{item}"
      if (!missing(`item`)) {
        local_var_url_path <- gsub("\\{item\\}", URLencode(as.character(`item`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete File or Folder
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param recursive (optional) Applicable to deleting folders only. If true, the request will delete the contents of a folder and all subfolders. The default is false.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingItemsItemDelete = function(item, recursive = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingItemsItemDeleteWithHttpInfo(item, recursive, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete File or Folder
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param recursive (optional) Applicable to deleting folders only. If true, the request will delete the contents of a folder and all subfolders. The default is false.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingItemsItemDeleteWithHttpInfo = function(item, recursive = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`item`)) {
        stop("Missing required parameter `item`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["recursive"]] <- `recursive`

      local_var_url_path <- "/services/file_staging/items/{item}"
      if (!missing(`item`)) {
        local_var_url_path <- gsub("\\{item\\}", URLencode(as.character(`item`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List Items at a Path
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param recursive (optional) If true, the response will contain the contents of all subfolders. If not specified, the default value is false.
    #' @param limit (optional) Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000.
    #' @param format_result (optional) If set to csv, the response includes a job_id. Use the Job ID value to retrieve the status and results of the request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingItemsItemGet = function(item, recursive = NULL, limit = NULL, format_result = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingItemsItemGetWithHttpInfo(item, recursive, limit, format_result, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List Items at a Path
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param recursive (optional) If true, the response will contain the contents of all subfolders. If not specified, the default value is false.
    #' @param limit (optional) Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000.
    #' @param format_result (optional) If set to csv, the response includes a job_id. Use the Job ID value to retrieve the status and results of the request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingItemsItemGetWithHttpInfo = function(item, recursive = NULL, limit = NULL, format_result = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`item`)) {
        stop("Missing required parameter `item`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["recursive"]] <- `recursive`

      query_params[["limit"]] <- `limit`

      query_params[["format_result"]] <- `format_result`

      local_var_url_path <- "/services/file_staging/items/{item}"
      if (!missing(`item`)) {
        local_var_url_path <- gsub("\\{item\\}", URLencode(as.character(`item`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Folder or File
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingItemsItemPut = function(item, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingItemsItemPutWithHttpInfo(item, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Folder or File
    #'
    #' @param item The absolute path to the file or folder to delete. This path is specific to the authenticated user. Admin users can access the root directory. All other users can only access their own user directory.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingItemsItemPutWithHttpInfo = function(item, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`item`)) {
        stop("Missing required parameter `item`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/items/{item}"
      if (!missing(`item`)) {
        local_var_url_path <- gsub("\\{item\\}", URLencode(as.character(`item`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Folder or File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_md5 (optional) Optional: The MD5 checksum of the file being uploaded.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingItemsPost = function(authorization = NULL, accept = NULL, content_md5 = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingItemsPostWithHttpInfo(authorization, accept, content_md5, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Folder or File
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_md5 (optional) Optional: The MD5 checksum of the file being uploaded.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingItemsPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_md5 = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-MD5"] <- `content_md5`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/items"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List Upload Sessions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List Upload Sessions
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload/"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Resumable Upload Session
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadPost = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadPostWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Resumable Upload Session
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadPostWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Abort Upload Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadUploadSessionIdDelete = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadUploadSessionIdDeleteWithHttpInfo(upload_session_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Abort Upload Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadUploadSessionIdDeleteWithHttpInfo = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`upload_session_id`)) {
        stop("Missing required parameter `upload_session_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload/{upload_session_id}"
      if (!missing(`upload_session_id`)) {
        local_var_url_path <- gsub("\\{upload_session_id\\}", URLencode(as.character(`upload_session_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Get Upload Session Details
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadUploadSessionIdGet = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadUploadSessionIdGetWithHttpInfo(upload_session_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Get Upload Session Details
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadUploadSessionIdGetWithHttpInfo = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`upload_session_id`)) {
        stop("Missing required parameter `upload_session_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload/{upload_session_id}"
      if (!missing(`upload_session_id`)) {
        local_var_url_path <- gsub("\\{upload_session_id\\}", URLencode(as.character(`upload_session_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' List File Parts Uploaded to Session
    #'
    #' @param upload_session_id 
    #' @param limit (optional) Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadUploadSessionIdPartsGet = function(upload_session_id, limit = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadUploadSessionIdPartsGetWithHttpInfo(upload_session_id, limit, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' List File Parts Uploaded to Session
    #'
    #' @param upload_session_id 
    #' @param limit (optional) Optional: The maximum number of items per page in the response. This can be any value between 1 and 1000. If omitted, the default value is 1000.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadUploadSessionIdPartsGetWithHttpInfo = function(upload_session_id, limit = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`upload_session_id`)) {
        stop("Missing required parameter `upload_session_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/services/file_staging/upload/{upload_session_id}/parts"
      if (!missing(`upload_session_id`)) {
        local_var_url_path <- gsub("\\{upload_session_id\\}", URLencode(as.character(`upload_session_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Commit Upload Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadUploadSessionIdPost = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadUploadSessionIdPostWithHttpInfo(upload_session_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Commit Upload Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadUploadSessionIdPostWithHttpInfo = function(upload_session_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`upload_session_id`)) {
        stop("Missing required parameter `upload_session_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload/{upload_session_id}"
      if (!missing(`upload_session_id`)) {
        local_var_url_path <- gsub("\\{upload_session_id\\}", URLencode(as.character(`upload_session_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Upload to a Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_file_part_number (optional) The part number, which uniquely identifies a file part and defines its position within the file as a whole. If a part is uploaded using a part number that has already been used, Vault overwrites the previously uploaded file part. You must upload parts in numerical order. For example, you cannot upload part 3 without first uploading parts 1 and 2.
    #' @param content_md5 (optional) Optional: The MD5 checksum of the file part being uploaded.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesFileStagingUploadUploadSessionIdPut = function(upload_session_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_file_part_number = NULL, content_md5 = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesFileStagingUploadUploadSessionIdPutWithHttpInfo(upload_session_id, authorization, accept, content_type, x_vault_api_file_part_number, content_md5, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Upload to a Session
    #'
    #' @param upload_session_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_file_part_number (optional) The part number, which uniquely identifies a file part and defines its position within the file as a whole. If a part is uploaded using a part number that has already been used, Vault overwrites the previously uploaded file part. You must upload parts in numerical order. For example, you cannot upload part 3 without first uploading parts 1 and 2.
    #' @param content_md5 (optional) Optional: The MD5 checksum of the file part being uploaded.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesFileStagingUploadUploadSessionIdPutWithHttpInfo = function(upload_session_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_file_part_number = NULL, content_md5 = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`upload_session_id`)) {
        stop("Missing required parameter `upload_session_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-FilePartNumber"] <- `x_vault_api_file_part_number`

      header_params["Content-MD5"] <- `content_md5`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/file_staging/upload/{upload_session_id}"
      if (!missing(`upload_session_id`)) {
        local_var_url_path <- gsub("\\{upload_session_id\\}", URLencode(as.character(`upload_session_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Job Histories
    #'
    #' @param start_date (optional) Sets the date to start retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job.
    #' @param end_date (optional) Sets the date to end retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time.
    #' @param status (optional) Filter to only retrieve jobs in a certain status. Allowed values are success, errors_encountered, failed_to_run, missed_schedule, cancelled. If omitted, retrieves all statuses.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the first job history returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsHistoriesGet = function(start_date = NULL, end_date = NULL, status = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsHistoriesGetWithHttpInfo(start_date, end_date, status, limit, offset, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Job Histories
    #'
    #' @param start_date (optional) Sets the date to start retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job.
    #' @param end_date (optional) Sets the date to end retrieving completed jobs, in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time.
    #' @param status (optional) Filter to only retrieve jobs in a certain status. Allowed values are success, errors_encountered, failed_to_run, missed_schedule, cancelled. If omitted, retrieves all statuses.
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the first job history returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsHistoriesGetWithHttpInfo = function(start_date = NULL, end_date = NULL, status = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["status"]] <- `status`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/services/jobs/histories"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Import Bulk Translation File Job Errors
    #'
    #' @param job_id The id value of the requested import job. This was returned from the Import Bulk Translation File request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsJobIdErrorsGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsJobIdErrorsGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Import Bulk Translation File Job Errors
    #'
    #' @param job_id The id value of the requested import job. This was returned from the Import Bulk Translation File request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsJobIdErrorsGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/jobs/{job_id}/errors"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Job Status
    #'
    #' @param job_id The ID of the job, returned from the original job request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsJobIdGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsJobIdGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Job Status
    #'
    #' @param job_id The ID of the job, returned from the original job request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsJobIdGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/jobs/{job_id}"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Import Bulk Translation File Job Summary
    #'
    #' @param job_id The id value of the requested import job. This was returned from the Import Bulk Translation File request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsJobIdSummaryGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsJobIdSummaryGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Import Bulk Translation File Job Summary
    #'
    #' @param job_id The id value of the requested import job. This was returned from the Import Bulk Translation File request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsJobIdSummaryGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/jobs/{job_id}/summary"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve SDK Job Tasks
    #'
    #' @param job_id The ID of the SDK job, returned from the original job request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsJobIdTasksGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsJobIdTasksGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve SDK Job Tasks
    #'
    #' @param job_id The ID of the SDK job, returned from the original job request.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsJobIdTasksGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/jobs/{job_id}/tasks"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Job Monitors
    #'
    #' @param start_date (optional) Sets the date to start retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job.
    #' @param end_date (optional) Sets the date to end retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time.
    #' @param status (optional) Filter to only retrieve jobs in a certain status. Allowed values are scheduled, queued, running. If omitted, retrieves all statuses.
    #' @param limit (optional) Paginate the results by specifying the maximum number of jobs per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the first job instance returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsMonitorsGet = function(start_date = NULL, end_date = NULL, status = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsMonitorsGetWithHttpInfo(start_date, end_date, status, limit, offset, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Job Monitors
    #'
    #' @param start_date (optional) Sets the date to start retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the first completed job.
    #' @param end_date (optional) Sets the date to end retrieving uncompleted jobs, based on the date and time the job instance was created. Value must be in the format YYYY-MM-DDTHH:MM:SSZ. For example, for 7AM on January 15, 2016, use 2016-01-15T07:00:00Z. If omitted, defaults to the current date and time.
    #' @param status (optional) Filter to only retrieve jobs in a certain status. Allowed values are scheduled, queued, running. If omitted, retrieves all statuses.
    #' @param limit (optional) Paginate the results by specifying the maximum number of jobs per page in the response. This can be any value between 1 and 200. If omitted, defaults to 50.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the first job instance returned. If omitted, defaults to 0. If you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsMonitorsGetWithHttpInfo = function(start_date = NULL, end_date = NULL, status = NULL, limit = NULL, offset = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE









      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["status"]] <- `status`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/services/jobs/monitors"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Start Job
    #'
    #' @param job_id The ID of the scheduled job instance to start.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesJobsStartNowJobIdPost = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesJobsStartNowJobIdPostWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Start Job
    #'
    #' @param job_id The ID of the scheduled job instance to start.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesJobsStartNowJobIdPostWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/jobs/start_now/{job_id}"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Extract Data Files
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderExtractPost = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderExtractPostWithHttpInfo(authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Extract Data Files
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderExtractPostWithHttpInfo = function(authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/extract"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Load Failure Log Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderJobIdTasksTaskIdFailurelogGet = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderJobIdTasksTaskIdFailurelogGetWithHttpInfo(job_id, task_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Load Failure Log Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderJobIdTasksTaskIdFailurelogGetWithHttpInfo = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/{job_id}/tasks/{task_id}/failurelog"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Loader Extract Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderJobIdTasksTaskIdResultsGet = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderJobIdTasksTaskIdResultsGetWithHttpInfo(job_id, task_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Loader Extract Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderJobIdTasksTaskIdResultsGetWithHttpInfo = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/{job_id}/tasks/{task_id}/results"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Loader Extract Renditions Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderJobIdTasksTaskIdResultsRenditionsGet = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderJobIdTasksTaskIdResultsRenditionsGetWithHttpInfo(job_id, task_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Loader Extract Renditions Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderJobIdTasksTaskIdResultsRenditionsGetWithHttpInfo = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/{job_id}/tasks/{task_id}/results/renditions"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Load Success Log Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderJobIdTasksTaskIdSuccesslogGet = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderJobIdTasksTaskIdSuccesslogGetWithHttpInfo(job_id, task_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Load Success Log Results
    #'
    #' @param job_id The id value of the requested extract job.
    #' @param task_id The id value of the requested extract task.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderJobIdTasksTaskIdSuccesslogGetWithHttpInfo = function(job_id, task_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }

      if (missing(`task_id`)) {
        stop("Missing required parameter `task_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/{job_id}/tasks/{task_id}/successlog"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`task_id`)) {
        local_var_url_path <- gsub("\\{task_id\\}", URLencode(as.character(`task_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Load Data Objects
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesLoaderLoadPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesLoaderLoadPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Load Data Objects
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesLoaderLoadPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/loader/load"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Validate Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesPackageActionsValidatePost = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesPackageActionsValidatePostWithHttpInfo(authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Validate Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesPackageActionsValidatePostWithHttpInfo = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/package/actions/validate"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Export Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesPackagePost = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesPackagePostWithHttpInfo(authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Export Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesPackagePostWithHttpInfo = function(authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/package"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Import Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesPackagePut = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesPackagePutWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Import Package
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesPackagePutWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/package"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Queues
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesQueuesGet = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesQueuesGetWithHttpInfo(authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Queues
    #'
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesQueuesGetWithHttpInfo = function(authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/queues"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Disable Delivery
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesQueuesQueueNameActionsDisableDeliveryPut = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesQueuesQueueNameActionsDisableDeliveryPutWithHttpInfo(queue_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Disable Delivery
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesQueuesQueueNameActionsDisableDeliveryPutWithHttpInfo = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_name`)) {
        stop("Missing required parameter `queue_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/queues/{queue_name}/actions/disable_delivery"
      if (!missing(`queue_name`)) {
        local_var_url_path <- gsub("\\{queue_name\\}", URLencode(as.character(`queue_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Enable Delivery
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesQueuesQueueNameActionsEnableDeliveryPut = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesQueuesQueueNameActionsEnableDeliveryPutWithHttpInfo(queue_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Enable Delivery
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesQueuesQueueNameActionsEnableDeliveryPutWithHttpInfo = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_name`)) {
        stop("Missing required parameter `queue_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/queues/{queue_name}/actions/enable_delivery"
      if (!missing(`queue_name`)) {
        local_var_url_path <- gsub("\\{queue_name\\}", URLencode(as.character(`queue_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Reset Queue
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesQueuesQueueNameActionsResetPut = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesQueuesQueueNameActionsResetPutWithHttpInfo(queue_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Reset Queue
    #'
    #' @param queue_name The name of a specific Queue.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesQueuesQueueNameActionsResetPutWithHttpInfo = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_name`)) {
        stop("Missing required parameter `queue_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/queues/{queue_name}/actions/reset"
      if (!missing(`queue_name`)) {
        local_var_url_path <- gsub("\\{queue_name\\}", URLencode(as.character(`queue_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Queue Status
    #'
    #' @param queue_name The name of a specific queue. For example, queue__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesQueuesQueueNameGet = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesQueuesQueueNameGetWithHttpInfo(queue_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Queue Status
    #'
    #' @param queue_name The name of a specific queue. For example, queue__c.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesQueuesQueueNameGetWithHttpInfo = function(queue_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`queue_name`)) {
        stop("Missing required parameter `queue_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/queues/{queue_name}"
      if (!missing(`queue_name`)) {
        local_var_url_path <- gsub("\\{queue_name\\}", URLencode(as.character(`queue_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Validate Imported Package
    #'
    #' @param package_id The ID of the package to validate. You can find this in the API response of a package import, or in the URL of package in the Vault UI.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    ServicesVobjectVaultPackageVPackageIdActionsValidatePost = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$ServicesVobjectVaultPackageVPackageIdActionsValidatePostWithHttpInfo(package_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Validate Imported Package
    #'
    #' @param package_id The ID of the package to validate. You can find this in the API response of a package import, or in the URL of package in the Vault UI.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    ServicesVobjectVaultPackageVPackageIdActionsValidatePostWithHttpInfo = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package_id`)) {
        stop("Missing required parameter `package_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/services/vobject/vault_package__v/{package_id}/actions/validate"
      if (!missing(`package_id`)) {
        local_var_url_path <- gsub("\\{package_id\\}", URLencode(as.character(`package_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' End Session
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) The Vault sessionId to end.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    SessionDelete = function(accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$SessionDeleteWithHttpInfo(accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' End Session
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) The Vault sessionId to end.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    SessionDeleteWithHttpInfo = function(accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/session"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Single Client Code Distribution
    #'
    #' @param distribution_name The name attribute of the client code distribution to download.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    UicodeDistributionsDistributionNameCodeGet = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$UicodeDistributionsDistributionNameCodeGetWithHttpInfo(distribution_name, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Single Client Code Distribution
    #'
    #' @param distribution_name The name attribute of the client code distribution to download.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UicodeDistributionsDistributionNameCodeGetWithHttpInfo = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`distribution_name`)) {
        stop("Missing required parameter `distribution_name`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/uicode/distributions/{distribution_name}/code"
      if (!missing(`distribution_name`)) {
        local_var_url_path <- gsub("\\{distribution_name\\}", URLencode(as.character(`distribution_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Single Client Code Distribution
    #'
    #' @param distribution_name The name attribute of the client code distribution to delete.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    UicodeDistributionsDistributionNameDelete = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$UicodeDistributionsDistributionNameDeleteWithHttpInfo(distribution_name, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Single Client Code Distribution
    #'
    #' @param distribution_name The name attribute of the client code distribution to delete.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UicodeDistributionsDistributionNameDeleteWithHttpInfo = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`distribution_name`)) {
        stop("Missing required parameter `distribution_name`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/uicode/distributions/{distribution_name}"
      if (!missing(`distribution_name`)) {
        local_var_url_path <- gsub("\\{distribution_name\\}", URLencode(as.character(`distribution_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Single Client Code Distribution Metadata
    #'
    #' @param distribution_name The name attribute of the client code distribution to delete.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    UicodeDistributionsDistributionNameGet = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$UicodeDistributionsDistributionNameGetWithHttpInfo(distribution_name, accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Single Client Code Distribution Metadata
    #'
    #' @param distribution_name The name attribute of the client code distribution to delete.
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UicodeDistributionsDistributionNameGetWithHttpInfo = function(distribution_name, accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`distribution_name`)) {
        stop("Missing required parameter `distribution_name`.")
      }





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/uicode/distributions/{distribution_name}"
      if (!missing(`distribution_name`)) {
        local_var_url_path <- gsub("\\{distribution_name\\}", URLencode(as.character(`distribution_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve All Client Code Distribution Metadata
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    UicodeDistributionsGet = function(accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$UicodeDistributionsGetWithHttpInfo(accept, authorization, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve All Client Code Distribution Metadata
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UicodeDistributionsGetWithHttpInfo = function(accept = NULL, authorization = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE




      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/uicode/distributions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Add or Replace Single Client Code Distribution
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    UicodeDistributionsPost = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$UicodeDistributionsPostWithHttpInfo(accept, authorization, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Add or Replace Single Client Code Distribution
    #'
    #' @param accept (optional) No description
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    UicodeDistributionsPostWithHttpInfo = function(accept = NULL, authorization = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Accept"] <- `accept`

      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/uicode/distributions"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deploy Package
    #'
    #' @param package_id The id field value of the vault_package__v object record used for deployment. See Import Package.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectVaultPackageVPackageIdActionsDeployPost = function(package_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectVaultPackageVPackageIdActionsDeployPostWithHttpInfo(package_id, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deploy Package
    #'
    #' @param package_id The id field value of the vault_package__v object record used for deployment. See Import Package.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectVaultPackageVPackageIdActionsDeployPostWithHttpInfo = function(package_id, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package_id`)) {
        stop("Missing required parameter `package_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobject/vault_package__v/{package_id}/actions/deploy"
      if (!missing(`package_id`)) {
        local_var_url_path <- gsub("\\{package_id\\}", URLencode(as.character(`package_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Package Deploy Results
    #'
    #' @param package_id The id field value of the vault_package__v object record used for deployment. See Deploy Package.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectVaultPackageVPackageIdActionsDeployResultsGet = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectVaultPackageVPackageIdActionsDeployResultsGetWithHttpInfo(package_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Package Deploy Results
    #'
    #' @param package_id The id field value of the vault_package__v object record used for deployment. See Deploy Package.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectVaultPackageVPackageIdActionsDeployResultsGetWithHttpInfo = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package_id`)) {
        stop("Missing required parameter `package_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobject/vault_package__v/{package_id}/actions/deploy/results"
      if (!missing(`package_id`)) {
        local_var_url_path <- gsub("\\{package_id\\}", URLencode(as.character(`package_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Results of Cascade Delete Job
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param job_status 
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsCascadedeleteResultsObjectNameJobStatusJobIdGet = function(object_name, job_status, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsCascadedeleteResultsObjectNameJobStatusJobIdGetWithHttpInfo(object_name, job_status, job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Results of Cascade Delete Job
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param job_status 
    #' @param job_id 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsCascadedeleteResultsObjectNameJobStatusJobIdGetWithHttpInfo = function(object_name, job_status, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`job_status`)) {
        stop("Missing required parameter `job_status`.")
      }

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/cascadedelete/results/{object_name}/{job_status}/{job_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_status`)) {
        local_var_url_path <- gsub("\\{job_status\\}", URLencode(as.character(`job_status`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Results of Deep Copy Job
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param job_status The ID of the job, retrieved from the response of the job request.
    #' @param job_id Possible values are success or failure. Find if your job succeeded or failed by retrieving the job status.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsDeepcopyResultsObjectNameJobStatusJobIdGet = function(object_name, job_status, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsDeepcopyResultsObjectNameJobStatusJobIdGetWithHttpInfo(object_name, job_status, job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Results of Deep Copy Job
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param job_status The ID of the job, retrieved from the response of the job request.
    #' @param job_id Possible values are success or failure. Find if your job succeeded or failed by retrieving the job status.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsDeepcopyResultsObjectNameJobStatusJobIdGetWithHttpInfo = function(object_name, job_status, job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`job_status`)) {
        stop("Missing required parameter `job_status`.")
      }

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/deepcopy/results/{object_name}/{job_status}/{job_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_status`)) {
        local_var_url_path <- gsub("\\{job_status\\}", URLencode(as.character(`job_status`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create a Placeholder from an EDL Item
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsEdlItemVActionsCreateplaceholderPost = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsEdlItemVActionsCreateplaceholderPostWithHttpInfo(authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create a Placeholder from an EDL Item
    #'
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsEdlItemVActionsCreateplaceholderPostWithHttpInfo = function(authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/edl_item__v/actions/createplaceholder"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Merge Records Job Log
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsMergesJobIdLogGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsMergesJobIdLogGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Merge Records Job Log
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsMergesJobIdLogGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/merges/{job_id}/log"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Record Merge Results
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsMergesJobIdResultsGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsMergesJobIdResultsGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Record Merge Results
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsMergesJobIdResultsGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/merges/{job_id}/results"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Record Merge Status
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsMergesJobIdStatusGet = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsMergesJobIdStatusGetWithHttpInfo(job_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Record Merge Status
    #'
    #' @param job_id The job_id field value returned from the merge operation. You can start merge operations with the Initiate Record Merge API request or with the Vault Java SDK.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsMergesJobIdStatusGetWithHttpInfo = function(job_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/merges/{job_id}/status"
      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{job_id\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Object Action on Multiple Records
    #'
    #' @param object_name The object name__v field value.
    #' @param action_name Either the name of the Objectaction or Objectlifecyclestateuseraction to initiate. This is obtained from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsActionNamePost = function(object_name, action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsActionNamePostWithHttpInfo(object_name, action_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Object Action on Multiple Records
    #'
    #' @param object_name The object name__v field value.
    #' @param action_name Either the name of the Objectaction or Objectlifecyclestateuseraction to initiate. This is obtained from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsActionNamePostWithHttpInfo = function(object_name, action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`action_name`)) {
        stop("Missing required parameter `action_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/actions/{action_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`action_name`)) {
        local_var_url_path <- gsub("\\{action_name\\}", URLencode(as.character(`action_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Change Object Type
    #'
    #' @param object_name The name of the object.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsChangetypePost = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsChangetypePostWithHttpInfo(object_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Change Object Type
    #'
    #' @param object_name The name of the object.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsChangetypePostWithHttpInfo = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/actions/changetype"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Record Merge
    #'
    #' @param object_name The object name__v field value. For example, account__v. This object must have Enable Merges configured.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param _vobjects__object_name__actions_merge_post_request_inner (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsMergePost = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, _vobjects__object_name__actions_merge_post_request_inner = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsMergePostWithHttpInfo(object_name, authorization, content_type, accept, x_vault_api_client_id, _vobjects__object_name__actions_merge_post_request_inner, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Record Merge
    #'
    #' @param object_name The object name__v field value. For example, account__v. This object must have Enable Merges configured.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param _vobjects__object_name__actions_merge_post_request_inner (optional) No description
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsMergePostWithHttpInfo = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, _vobjects__object_name__actions_merge_post_request_inner = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      if (!is.null(`_vobjects__object_name__actions_merge_post_request_inner`)) {
        body.items <- paste(unlist(lapply(`_vobjects__object_name__actions_merge_post_request_inner`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        local_var_body <- NULL
      }

      local_var_url_path <- "/vobjects/{object_name}/actions/merge"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Roll-up Field Recalculation Status
    #'
    #' @param object_name The name of the object for which to check the status of a Roll-up field recalculation.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsRecalculaterollupsGet = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsRecalculaterollupsGetWithHttpInfo(object_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Roll-up Field Recalculation Status
    #'
    #' @param object_name The name of the object for which to check the status of a Roll-up field recalculation.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsRecalculaterollupsGetWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/actions/recalculaterollups"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Recalculate Roll-up Fields
    #'
    #' @param object_name The name of the object for which to check the status of a Roll-up field recalculation.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsRecalculaterollupsPost = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsRecalculaterollupsPostWithHttpInfo(object_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Recalculate Roll-up Fields
    #'
    #' @param object_name The name of the object for which to check the status of a Roll-up field recalculation.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsRecalculaterollupsPostWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/actions/recalculaterollups"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Corporate Currency Fields
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameActionsUpdatecorporatecurrencyPut = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameActionsUpdatecorporatecurrencyPutWithHttpInfo(object_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Corporate Currency Fields
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameActionsUpdatecorporatecurrencyPutWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/actions/updatecorporatecurrency"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Multiple Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param id_param (optional) No description
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameAttachmentsBatchDelete = function(object_name, id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameAttachmentsBatchDeleteWithHttpInfo(object_name, id_param, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Multiple Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param id_param (optional) No description
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameAttachmentsBatchDeleteWithHttpInfo = function(object_name, id_param = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      local_var_url_path <- "/vobjects/{object_name}/attachments/batch"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Multiple Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameAttachmentsBatchPost = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameAttachmentsBatchPostWithHttpInfo(object_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Multiple Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameAttachmentsBatchPostWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/attachments/batch"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Multiple Object Record Attachment Descriptions
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameAttachmentsBatchPut = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameAttachmentsBatchPutWithHttpInfo(object_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Multiple Object Record Attachment Descriptions
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameAttachmentsBatchPutWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/attachments/batch"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameDelete = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameDeleteWithHttpInfo(object_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameDeleteWithHttpInfo = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record Roles
    #'
    #' @param object_name The object name.
    #' @param id The id of the document, binder, or object record.
    #' @param role_name Optional: Include a role name to filter for a specific role. For example, owner__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameIdRolesRoleNameGet = function(object_name, id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameIdRolesRoleNameGetWithHttpInfo(object_name, id, role_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record Roles
    #'
    #' @param object_name The object name.
    #' @param id The id of the document, binder, or object record.
    #' @param role_name Optional: Include a role name to filter for a specific role. For example, owner__v.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameIdRolesRoleNameGetWithHttpInfo = function(object_name, id, role_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`role_name`)) {
        stop("Missing required parameter `role_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{id}/roles/{role_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`role_name`)) {
        local_var_url_path <- gsub("\\{role_name\\}", URLencode(as.character(`role_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object User Actions Details
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param action_name The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdActionsActionNameGet = function(object_name, object_record_id, action_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdActionsActionNameGetWithHttpInfo(object_name, object_record_id, action_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object User Actions Details
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param action_name The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdActionsActionNameGetWithHttpInfo = function(object_name, object_record_id, action_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`action_name`)) {
        stop("Missing required parameter `action_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/actions/{action_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`action_name`)) {
        local_var_url_path <- gsub("\\{action_name\\}", URLencode(as.character(`action_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Initiate Object Action on a Single Record
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param action_name The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdActionsActionNamePost = function(object_name, object_record_id, action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdActionsActionNamePostWithHttpInfo(object_name, object_record_id, action_name, authorization, content_type, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Initiate Object Action on a Single Record
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param action_name The name of the Objectaction or Objectlifecyclestateuseraction to initiate. You can retrieve this from the Retrieve User Actions request. The format for action_name is Objectaction.vobject.action or Objectlifecyclestateuseraction.vobject.state.action.
    #' @param authorization (optional) No description
    #' @param content_type (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdActionsActionNamePostWithHttpInfo = function(object_name, object_record_id, action_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`action_name`)) {
        stop("Missing required parameter `action_name`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/actions/{action_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`action_name`)) {
        local_var_url_path <- gsub("\\{action_name\\}", URLencode(as.character(`action_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Cascade Delete Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdActionsCascadedeletePost = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdActionsCascadedeletePostWithHttpInfo(object_name, object_record_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Cascade Delete Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdActionsCascadedeletePostWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/actions/cascadedelete"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Deep Copy Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdActionsDeepcopyPost = function(object_name, object_record_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdActionsDeepcopyPostWithHttpInfo(object_name, object_record_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Deep Copy Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdActionsDeepcopyPostWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/actions/deepcopy"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record User Actions
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value.
    #' @param loc (optional) Optional: When true, retrieves localized (translated) strings for the label. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdActionsGet = function(object_name, object_record_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdActionsGetWithHttpInfo(object_name, object_record_id, loc, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record User Actions
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value.
    #' @param loc (optional) Optional: When true, retrieves localized (translated) strings for the label. 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdActionsGetWithHttpInfo = function(object_name, object_record_id, loc = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["loc"]] <- `loc`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/actions"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Attachment Field File
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param attachment_field_name The name of the Attachment field to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFileGet = function(object_name, object_record_id, attachment_field_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFileGetWithHttpInfo(object_name, object_record_id, attachment_field_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Attachment Field File
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param attachment_field_name The name of the Attachment field to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFileGetWithHttpInfo = function(object_name, object_record_id, attachment_field_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_field_name`)) {
        stop("Missing required parameter `attachment_field_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachment_fields/{attachment_field_name}/file"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_field_name`)) {
        local_var_url_path <- gsub("\\{attachment_field_name\\}", URLencode(as.character(`attachment_field_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Attachment Field File
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param attachment_field_name The name of the Attachment field to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFilePost = function(object_name, object_record_id, attachment_field_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFilePostWithHttpInfo(object_name, object_record_id, attachment_field_name, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Attachment Field File
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param attachment_field_name The name of the Attachment field to update.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentFieldsAttachmentFieldNameFilePostWithHttpInfo = function(object_name, object_record_id, attachment_field_name, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_field_name`)) {
        stop("Missing required parameter `attachment_field_name`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachment_fields/{attachment_field_name}/file"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_field_name`)) {
        local_var_url_path <- gsub("\\{attachment_field_name\\}", URLencode(as.character(`attachment_field_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download All Attachment Field Files
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentFieldsFileGet = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentFieldsFileGetWithHttpInfo(object_name, object_record_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download All Attachment Field Files
    #'
    #' @param object_name The object name__v field value. For example, product__v.
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentFieldsFileGetWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachment_fields/file"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Object Record Attachment
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdDelete = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdDeleteWithHttpInfo(object_name, object_record_id, attachment_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Object Record Attachment
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdDeleteWithHttpInfo = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdGet = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdGetWithHttpInfo(object_name, object_record_id, attachment_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdGetWithHttpInfo = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Object Record Attachment Description
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdPut = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdPutWithHttpInfo(object_name, object_record_id, attachment_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Object Record Attachment Description
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdPutWithHttpInfo = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Delete Object Record Attachment Version
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionDelete = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionDeleteWithHttpInfo(object_name, object_record_id, attachment_id, attachment_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Delete Object Record Attachment Version
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionDeleteWithHttpInfo = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download Object Record Attachment File
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGet = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo(object_name, object_record_id, attachment_id, attachment_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download Object Record Attachment File
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version 
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionFileGetWithHttpInfo = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}/versions/{attachment_version}/file"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Version Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionGet = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo(object_name, object_record_id, attachment_id, attachment_version, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Version Metadata
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionGetWithHttpInfo = function(object_name, object_record_id, attachment_id, attachment_version, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Restore Object Record Attachment Version
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param restore (optional) No description
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionPost = function(object_name, object_record_id, attachment_id, attachment_version, restore = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionPostWithHttpInfo(object_name, object_record_id, attachment_id, attachment_version, restore, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Restore Object Record Attachment Version
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param attachment_version The attachment version__v field value.
    #' @param restore (optional) No description
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsAttachmentVersionPostWithHttpInfo = function(object_name, object_record_id, attachment_id, attachment_version, restore = NULL, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }

      if (missing(`attachment_version`)) {
        stop("Missing required parameter `attachment_version`.")
      }










      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["restore"]] <- `restore`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}/versions/{attachment_version}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_version`)) {
        local_var_url_path <- gsub("\\{attachment_version\\}", URLencode(as.character(`attachment_version`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Versions
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsGet = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsGetWithHttpInfo(object_name, object_record_id, attachment_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record Attachment Versions
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param attachment_id The attachment id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsAttachmentIdVersionsGetWithHttpInfo = function(object_name, object_record_id, attachment_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }

      if (missing(`attachment_id`)) {
        stop("Missing required parameter `attachment_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/{attachment_id}/versions"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`attachment_id`)) {
        local_var_url_path <- gsub("\\{attachment_id\\}", URLencode(as.character(`attachment_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Download All Object Record Attachment Files
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsFileGet = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsFileGetWithHttpInfo(object_name, object_record_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Download All Object Record Attachment Files
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsFileGetWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments/file"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsGet = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsGetWithHttpInfo(object_name, object_record_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record Attachments
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsGetWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create Object Record Attachment
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAttachmentsPost = function(object_name, object_record_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAttachmentsPostWithHttpInfo(object_name, object_record_id, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create Object Record Attachment
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAttachmentsPostWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }







      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/attachments"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Complete Audit History for a Single Object Record
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param start_date (optional) Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vVault’s creation date. 
    #' @param end_date (optional) Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date. 
    #' @param format_result (optional) To request a CSV file of your audit history, use csv. 
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param events (optional) Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of object audit events. The values passed to this parameter are case sensitive. For example, events=Copy,Edit,Delete. If omitted, defaults to all audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdAudittrailGet = function(object_name, object_record_id, start_date = NULL, end_date = NULL, format_result = NULL, limit = NULL, offset = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdAudittrailGetWithHttpInfo(object_name, object_record_id, start_date, end_date, format_result, limit, offset, events, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Complete Audit History for a Single Object Record
    #'
    #' @param object_name The object name__v field value.
    #' @param object_record_id The object record id field value from which to retrieve user actions.
    #' @param start_date (optional) Specify a start date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to the vVault’s creation date. 
    #' @param end_date (optional) Specify an end date to retrieve audit history. This date cannot be more than 30 days ago. Dates must be YYYY-MM-DDTHH:MM:SSZ format, for example, 7AM on January 15, 2018 would use 2018-01-15T07:00:00Z. If omitted, defaults to today’s date. 
    #' @param format_result (optional) To request a CSV file of your audit history, use csv. 
    #' @param limit (optional) Paginate the results by specifying the maximum number of histories per page in the response. This can be any value between 1 and 1000. If omitted, defaults to 200.
    #' @param offset (optional) Paginate the results displayed per page by specifying the amount of offset from the entry returned. For example, if you are viewing the first 50 results (page 1) and want to see the next page, set this to offset=51. If omitted, defaults to 0.
    #' @param events (optional) Provide a comma-separated list of one or more audit events to retrieve their audit history. See Vault Help for a full list of object audit events. The values passed to this parameter are case sensitive. For example, events=Copy,Edit,Delete. If omitted, defaults to all audit events.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdAudittrailGetWithHttpInfo = function(object_name, object_record_id, start_date = NULL, end_date = NULL, format_result = NULL, limit = NULL, offset = NULL, events = NULL, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }












      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["start_date"]] <- `start_date`

      query_params[["end_date"]] <- `end_date`

      query_params[["format_result"]] <- `format_result`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      query_params[["events"]] <- `events`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}/audittrail"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameObjectRecordIdGet = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameObjectRecordIdGetWithHttpInfo(object_name, object_record_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Object Record
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param object_record_id The object record id field value.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameObjectRecordIdGetWithHttpInfo = function(object_name, object_record_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }

      if (missing(`object_record_id`)) {
        stop("Missing required parameter `object_record_id`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/{object_record_id}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`object_record_id`)) {
        local_var_url_path <- gsub("\\{object_record_id\\}", URLencode(as.character(`object_record_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Create & Upsert Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param id_param (optional) To upsert object records, add `idParam={field_name}` to the request endpoint. You can use any object field which has `unique` set to `true` in the object metadata. For example, `idParam=external_id__v`.
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_migration_mode (optional) If set to `true`, Vault allows you to create or update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header.
    #' @param x_vault_api_no_triggers (optional) If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNamePost = function(object_name, id_param = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_no_triggers = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNamePostWithHttpInfo(object_name, id_param, authorization, content_type, accept, x_vault_api_migration_mode, x_vault_api_no_triggers, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Create & Upsert Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param id_param (optional) To upsert object records, add `idParam={field_name}` to the request endpoint. You can use any object field which has `unique` set to `true` in the object metadata. For example, `idParam=external_id__v`.
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_migration_mode (optional) If set to `true`, Vault allows you to create or update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header.
    #' @param x_vault_api_no_triggers (optional) If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNamePostWithHttpInfo = function(object_name, id_param = NULL, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_no_triggers = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }









      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-NoTriggers"] <- `x_vault_api_no_triggers`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      query_params[["idParam"]] <- `id_param`

      local_var_url_path <- "/vobjects/{object_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Update Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_migration_mode (optional) If set to `true`, Vault allows you to update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header.
    #' @param x_vault_api_no_triggers (optional) If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNamePut = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_no_triggers = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNamePutWithHttpInfo(object_name, authorization, content_type, accept, x_vault_api_migration_mode, x_vault_api_no_triggers, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Update Object Records
    #'
    #' @param object_name The object name__v field value (product__v, country__v, custom_object__c, etc.).
    #' @param authorization (optional) No description
    #' @param content_type (optional) Can be text/csv or application/json
    #' @param accept (optional) Can be text/csv or application/json
    #' @param x_vault_api_migration_mode (optional) If set to `true`, Vault allows you to update object records in a noninitial state and with minimal validation, create inactive records, and set standard and system-managed fields such as `created_by__v`. Does not bypass record triggers. Use the `X-VaultAPI-NoTriggers` header to bypass all standard and custom SDK triggers. You must have the Record Migration permission to use this header.
    #' @param x_vault_api_no_triggers (optional) If set to `true` and Record Migration Mode is enabled, it bypasses all standard and custom SDK triggers.
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNamePutWithHttpInfo = function(object_name, authorization = NULL, content_type = NULL, accept = NULL, x_vault_api_migration_mode = NULL, x_vault_api_no_triggers = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }








      header_params["Authorization"] <- `authorization`

      header_params["Content-Type"] <- `content_type`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-MigrationMode"] <- `x_vault_api_migration_mode`

      header_params["X-VaultAPI-NoTriggers"] <- `x_vault_api_no_triggers`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on Object Records
    #'
    #' @param object_name The name of the object where you want to remove roles.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameRolesDelete = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameRolesDeleteWithHttpInfo(object_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Remove Users & Groups from Roles on Object Records
    #'
    #' @param object_name The name of the object where you want to remove roles.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameRolesDeleteWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/roles"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on Object Records
    #'
    #' @param object_name The name of the object where you want to remove roles.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsObjectNameRolesPost = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsObjectNameRolesPostWithHttpInfo(object_name, authorization, accept, content_type, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Assign Users & Groups to Roles on Object Records
    #'
    #' @param object_name The name of the object where you want to remove roles.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param content_type (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsObjectNameRolesPostWithHttpInfo = function(object_name, authorization = NULL, accept = NULL, content_type = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_name`)) {
        stop("Missing required parameter `object_name`.")
      }






      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["Content-Type"] <- `content_type`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/{object_name}/roles"
      if (!missing(`object_name`)) {
        local_var_url_path <- gsub("\\{object_name\\}", URLencode(as.character(`object_name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    },

    #' @description
    #' Retrieve Outbound Package Dependencies
    #'
    #' @param package_id The ID of the outbound_package__v record from which to retrieve dependencies.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return void
    VobjectsOutboundPackageVPackageIdDependenciesGet = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      local_var_response <- self$VobjectsOutboundPackageVPackageIdDependenciesGetWithHttpInfo(package_id, authorization, accept, x_vault_api_client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        return(local_var_response$content)
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        return(local_var_response)
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        return(local_var_response)
      }
    },

    #' @description
    #' Retrieve Outbound Package Dependencies
    #'
    #' @param package_id The ID of the outbound_package__v record from which to retrieve dependencies.
    #' @param authorization (optional) No description
    #' @param accept (optional) No description
    #' @param x_vault_api_client_id (optional) Include a Client ID to identify this request. This ID appears in the API Usage Logs, which is avaiable to download from Admin > Logs > API Usage Logs or through the Vault REST API with the Download Daily API Usage request. If omitted, the value will appear as `unknown` in the API Usage Log.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    VobjectsOutboundPackageVPackageIdDependenciesGetWithHttpInfo = function(package_id, authorization = NULL, accept = NULL, x_vault_api_client_id = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`package_id`)) {
        stop("Missing required parameter `package_id`.")
      }





      header_params["Authorization"] <- `authorization`

      header_params["Accept"] <- `accept`

      header_params["X-VaultAPI-ClientID"] <- `x_vault_api_client_id`

      local_var_url_path <- "/vobjects/outbound_package__v/{package_id}/dependencies"
      if (!missing(`package_id`)) {
        local_var_url_path <- gsub("\\{package_id\\}", URLencode(as.character(`package_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        return(local_var_resp)
      } 
      
      local_var_error_msg <- local_var_resp$response_as_text()      
      if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        return(local_var_resp)
      }
    }
  )
)
